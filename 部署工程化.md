## 一句话定义

**Agent 项目部署工程化** = 把前端、FastAPI 后端、Agent Runtime、模型服务、RAG/向量库、任务队列、数据库、缓存、审批与审计等组件，通过 **容器化、IaC、GitOps、CI/CD、K8s 编排、可观测、安全合规、灰度回滚、灾备与成本控制**，做成可重复、可审计、可回滚、可扩展的生产交付体系。

---

## 总体部署架构简图

```text
用户 / 渠道
   ↓
CDN + WAF + Nginx / Ingress / API 网关
   ↓
前端静态资源（Vue3 构建产物）        API 网关 / BFF
   ↓                                      ↓
                                    FastAPI 服务（无状态）
                                      ↓
                    ┌─────────────────┼─────────────────┐
                    ↓                 ↓                 ↓
              Agent Runtime      任务队列 Worker     审批/审计服务
                    ↓                 ↓                 ↓
        模型网关 → 模型服务      工具沙箱执行器       PostgreSQL / Redis
                    ↓                 ↓                 ↓
              GPU / 公有云 API    外部企业系统       向量库 / 对象存储 / MQ
                    ↓
        Prometheus / Grafana / Loki / Tempo / Sentry / Langfuse
```

**控制平面与数据平面分离：**
- 控制平面：Agent 配置、提示词、工具、知识库、策略、发布、评测。
- 数据平面：会话执行、推理、检索、工具调用、流式输出。

---

## 一、核心原则

| 原则 | 说明 |
|---|---|
| 一切皆代码 | 基础设施、配置、流水线、监控告警全部 IaC |
| 不可变基础设施 | 镜像构建一次，多环境晋级，不热改 |
| 环境一致性 | dev / staging / prod 同构，差异只在配置 |
| 无状态优先 | FastAPI、Worker 无状态，状态外置 |
| 控制面/数据面分离 | 配置发布与运行时隔离 |
| 默认安全 | 最小权限、密钥外置、网络隔离、镜像扫描 |
| 可观测内建 | 日志、指标、追踪、审计、成本从第一天就有 |
| 可回滚 | 任何发布必须能快速回滚 |
| 自动化 | 人工只做审批，不做重复操作 |

---

## 二、环境与配置管理

**环境划分：**
- `dev`：开发自测，可共用中间件。
- `staging`：预发，尽量贴近生产。
- `prod`：生产，多副本、高可用、严格权限。

**配置管理：**
- 配置外置：环境变量、ConfigMap、Secret。
- 敏感信息：Vault / K8s Secret / External Secrets / Sealed Secrets。
- 禁止把密钥写进镜像、代码、前端。
- 配置分层：全局 → 环境 → 服务 → 实例。
- 配置变更走 Git，可审计、可回滚。

**示例：**
```yaml
# k8s/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-backend-config
data:
  ENV: "prod"
  LOG_LEVEL: "INFO"
  VECTOR_DB_URL: "http://vector-db:6333"
```

```yaml
# k8s/secret.yaml（示例，实际用 External Secrets）
apiVersion: v1
kind: Secret
metadata:
  name: agent-backend-secret
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://..."
  REDIS_URL: "redis://..."
  JWT_SECRET: "..."
  OPENAI_API_KEY: "..."
```

---

## 三、容器化与镜像

**前端：**
- 多阶段构建：Node 构建 → Nginx 托管静态资源。
- 产物带版本号，CDN 缓存。
- 不把源码、node_modules 打进运行镜像。

```dockerfile
FROM node:20-alpine AS builder
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN corepack enable && pnpm install --frozen-lockfile
COPY . .
RUN pnpm build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
```

**后端：**
- 多阶段构建，依赖与运行分离。
- 使用非 root 用户。
- 固定基础镜像版本，定期扫描漏洞。
- 镜像标签：`service:git-sha`，禁止只用 `latest`。

```dockerfile
FROM python:3.11-slim AS builder
WORKDIR /app
COPY pyproject.toml poetry.lock ./
RUN pip install poetry && poetry export -f requirements.txt --output requirements.txt
RUN pip install --prefix=/install -r requirements.txt

FROM python:3.11-slim
RUN useradd -m appuser
COPY --from=builder /install /usr/local
COPY app /app
USER appuser
CMD ["gunicorn", "app.main:app", "-k", "uvicorn.workers.UvicornWorker", "-w", "4", "-b", "0.0.0.0:8000"]
```

**镜像安全：**
- 镜像扫描：Trivy / Clair / Grype。
- 签名：Cosign / Notary。
- 只允许可信仓库。
- 最小化基础镜像。

---

## 四、编排与调度：K8s 为主

**核心工作负载：**
- 前端：Nginx Deployment + Service + Ingress。
- FastAPI：Deployment + Service + HPA。
- Worker：Deployment / KEDA，按队列长度伸缩。
- Agent Runtime：可作为 FastAPI 内模块，也可独立 Deployment。
- 模型服务：GPU Deployment / StatefulSet，或外部 API。
- 数据库/Redis/向量库：优先托管服务，自建则 StatefulSet + Operator。

**关键配置：**
- 资源 requests/limits，防止抢占与 OOM。
- 就绪/存活探针：`/health`、`/ready`。
- 优雅停机：`terminationGracePeriodSeconds`，处理完进行中请求。
- PodDisruptionBudget：保证滚动更新时可用副本。
- 拓扑分布：多可用区。
- 网络策略：默认拒绝，按需放行。
- HPA：CPU / 内存 / 自定义指标（队列长度、QPS）。
- VPA / Cluster Autoscaler：按需扩容。
- KEDA：基于 Redis / MQ 队列长度伸缩 Worker。

**示例：**
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-backend
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    spec:
      containers:
      - name: app
        image: registry/agent-backend:${GIT_SHA}
        ports:
        - containerPort: 8000
        readinessProbe:
          httpGet: { path: /ready, port: 8000 }
        livenessProbe:
          httpGet: { path: /health, port: 8000 }
        resources:
          requests: { cpu: "500m", memory: "512Mi" }
          limits:   { cpu: "2",    memory: "2Gi" }
        envFrom:
        - configMapRef: { name: agent-backend-config }
        - secretRef:    { name: agent-backend-secret }
```

---

## 五、CI/CD 流水线

**CI 阶段：**
1. 代码检出。
2. 依赖缓存安装。
3. Lint：Ruff / ESLint。
4. 类型检查：mypy / vue-tsc。
5. 单元测试：pytest / Vitest。
6. 构建：前端 dist、后端镜像。
7. 镜像扫描。
8. 推送镜像。
9. 生成 SBOM。

**CD 阶段：**
- GitOps：ArgoCD / Flux，监听 Git 仓库，自动同步。
- 环境晋级：dev → staging → prod，手动审批生产。
- 数据库迁移：Alembic Job，先迁移后发布。
- 发布策略：滚动、蓝绿、金丝雀。
- 回滚：`kubectl rollout undo` 或 ArgoCD 回滚。

**示例 GitHub Actions：**
```yaml
name: ci
on: [push]
jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with: { python-version: "3.11" }
    - run: pip install poetry && poetry install
    - run: poetry run ruff check .
    - run: poetry run mypy app
    - run: poetry run pytest
    - run: docker build -t registry/agent-backend:${{ github.sha }} .
    - run: docker push registry/agent-backend:${{ github.sha }}
```

---

## 六、数据库与中间件部署

| 组件 | 部署建议 |
|---|---|
| PostgreSQL | 托管 RDS / Cloud SQL，或 Operator（CloudNativePG） |
| Redis | 托管 / Sentinel / Cluster |
| 向量库 | pgvector / Qdrant / Milvus，按规模选型 |
| 消息队列 | RabbitMQ / Kafka / Redis Streams |
| 对象存储 | S3 / OSS / MinIO |
| 任务队列 | Celery / ARQ + Redis / RabbitMQ |

**数据库迁移：**
- Alembic 版本化迁移。
- 迁移 Job 在发布前运行。
- 向后兼容：先加字段，再发代码，再删旧字段。
- 备份：每日全量 + WAL 归档。
- 恢复演练：定期做。

---

## 七、模型服务与 GPU 部署

**两种模式：**
1. 公有云 API：OpenAI / Claude / 通义等，通过模型网关统一接入。
2. 私有化模型：vLLM / TGI / TensorRT-LLM，部署在 GPU 节点。

**模型网关：**
- 统一入口，路由、Fallback、限流、配额、成本统计。
- API Key 集中管理，不散落到业务服务。
- 结果缓存、小模型路由。

**GPU 调度：**
- 节点池分离：GPU 节点专用。
- 资源申请：`nvidia.com/gpu: 1`。
- 亲和性与污点容忍。
- 模型服务独立 HPA，按 QPS / GPU 利用率伸缩。
- 冷启动优化：预热、常驻副本。

---

## 八、前端部署

- 构建产物上传 CDN / 对象存储。
- Nginx / Ingress 托管，history 模式回退 `index.html`。
- 缓存策略：HTML 不缓存，JS/CSS 带 hash 长缓存。
- 多环境：不同域名或路径。
- 灰度：按用户 / 比例切流量。
- 回滚：保留历史版本，一键切换。

```nginx
location / {
  try_files $uri $uri/ /index.html;
}
location /assets/ {
  expires 1y;
  add_header Cache-Control "public, immutable";
}
```

---

## 九、后端与异步任务部署

- FastAPI 无状态，多副本。
- Gunicorn + Uvicorn Worker，按 CPU 调优。
- 优雅停机，处理完进行中请求。
- Worker 独立部署，按队列长度伸缩。
- 长任务持久化状态，支持恢复。
- 定时任务：Celery Beat / K8s CronJob。
- 流式接口注意超时与缓冲。

---

## 十、流式与长连接部署

Agent 场景常见 SSE / WebSocket，部署要特别注意：

- Ingress / Nginx 关闭缓冲：`proxy_buffering off`。
- 超时调大：`proxy_read_timeout`、`send_timeout`。
- 支持 HTTP/2 或 WebSocket 升级。
- 多副本时，长连接需要共享状态或粘性会话。
- 事件 ID、断线重连、幂等。
- 长任务可转异步 + 轮询 / WebSocket。
- 负载均衡器空闲超时大于最长流式时间。

```nginx
location /api/v1/agent/stream {
  proxy_pass http://agent-backend;
  proxy_buffering off;
  proxy_cache off;
  proxy_read_timeout 3600s;
  proxy_set_header Connection '';
  chunked_transfer_encoding off;
}
```

---

## 十一、可观测性

**日志：**
- 结构化 JSON。
- 字段：`timestamp`、`level`、`service`、`trace_id`、`request_id`、`tenant_id`、`user_id`。
- 集中收集：Loki / ELK / ClickHouse。
- 审计日志单独存，不可篡改。

**指标：**
- Prometheus + Grafana。
- 业务：QPS、延迟、错误率、Token、成本、工具成功率、任务完成率。
- 资源：CPU、内存、GPU、队列长度、连接池。
- 告警：SLO 燃烧率、错误率、延迟、成本超预算。

**追踪：**
- OpenTelemetry。
- Agent 每一步：规划、工具调用、检索、模型调用。
- Langfuse / LangSmith 做 LLM Trace。

**前端监控：**
- Sentry：错误、sourcemap。
- Web Vitals：LCP、FID、CLS。
- 埋点：页面、按钮、核心流程。

---

## 十二、安全与合规

- 镜像扫描、依赖扫描、SBOM。
- 网络策略：默认拒绝，按需放行。
- RBAC：K8s 权限最小化。
- 密钥管理：Vault / External Secrets。
- 多租户隔离：命名空间 / 数据库 / 行级隔离。
- 审计：所有工具调用、审批、配置变更。
- 合规：等保、GDPR、SOC2、数据本地化。
- 运行时安全：Falco / Tetragon。
- 入口安全：WAF、限流、防重放。

---

## 十三、发布与回滚

| 策略 | 适用 |
|---|---|
| 滚动更新 | 常规无状态服务 |
| 蓝绿 | 大版本、风险高 |
| 金丝雀 | 新功能、灰度验证 |
| 功能开关 | 按租户 / 用户放量 |
| 影子流量 | 评估新 Agent 版本 |

**回滚：**
- 镜像版本回退。
- ArgoCD 回滚到上一 Git Commit。
- 数据库迁移向前兼容，避免回滚困难。
- 回滚后自动告警与验证。

---

## 十四、灾备与高可用

- 多可用区部署。
- 数据库主从 / 多副本，定期备份。
- Redis 持久化 + 哨兵 / 集群。
- 对象存储跨区域复制。
- 模型服务多副本，Fallback 到公有云。
- 定期故障演练：Pod 删除、节点宕机、区域故障。
- RTO / RPO 明确并验证。

---

## 十五、成本与容量

- 资源 requests/limits 合理设置。
- HPA / KEDA 按需伸缩。
- GPU 节点池，按需启停。
- 模型路由：小模型优先，大模型兜底。
- 缓存：检索结果、模型结果。
- 成本归因：按租户 / 部门 / Agent 统计 Token 与 GPU。
- 预算告警：超预算自动限流或降级。
- 定期 Review 闲置资源。

---

## 十六、组织与流程

- 平台团队：K8s、CI/CD、可观测、模型网关。
- 业务团队：Agent 服务、前端、工具。
- SRE：SLO、告警、容量、灾备。
- 安全合规：审计、扫描、合规认证。
- 流程：需求 → 开发 → CI → 镜像 → GitOps → 发布 → 监控 → 反馈。

---

## 检查清单

- 是否一切皆代码：IaC、配置、流水线、告警？
- 是否镜像不可变、带版本、扫描漏洞？
- 是否环境一致，配置外置，密钥不落地？
- 是否 K8s 部署，有探针、PDB、资源限制？
- 是否 CI 跑 lint、typecheck、test、build、扫描？
- 是否 GitOps 发布，可审计、可回滚？
- 是否数据库迁移版本化、向后兼容？
- 是否模型网关统一接入，密钥集中管理？
- 是否 GPU 节点池与 HPA/KEDA？
- 是否前端 CDN + Nginx，缓存策略正确？
- 是否 SSE/WebSocket 关闭缓冲、超时合理？
- 是否结构化日志、指标、追踪、审计？
- 是否 SLO、告警、成本归因？
- 是否灰度、蓝绿、金丝雀、回滚？
- 是否多可用区、备份、恢复演练？
- 是否网络策略、RBAC、运行时安全？
- 是否多租户隔离、审批、审计？
- 是否成本可控、容量可预测？

---

## 一句话总结

**Agent 项目部署工程化 = 容器化 + IaC + GitOps + K8s 编排 + 模型/GPU 调度 + 流式长连接优化 + 可观测 + 安全合规 + 灰度回滚 + 灾备与成本控制，最终实现可重复、可审计、可回滚、可扩展的生产交付。**