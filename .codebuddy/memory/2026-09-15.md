# 2026-09-15

## 需求完成度全量盘点（读代码 + FRD 交叉核对，非采信文档自述）

口径：`FRDv2 §4 FR-1~FR-13` 为需求分母，证据取代码实测（endpoint/service/composable/view 是否真接线），不采信 `执行步骤.md` 自述。

### 结论分布

- **P0（FRD §8）≈ 七成**：文本 SSE / 单图售后 / 退款审批 / 转人工最小台 / 多租户 / CI 门禁 **已通**；卡在 **生产存储（PG/Redis/MinIO 全未接）/ 可观测（空占位）/ 黄金集（0 条）**。
- **P1 ≈ 四成**：B 端一期（商品/库存/订单履约/营销券/评价/工单）已落地；坐席完整台部分；**混合检索 rerank 是 stub**；计费/OCR/以图搜款/大促预案/A-B/影子未做。
- **P2 ≈ 基本未开工**：采购（FR-10.3）/ 资金对账（FR-10.5）/ 大屏（FR-11）/ 风控（FR-12.1）/ 消息触达（FR-12.2）/ 组织绩效（FR-12.4）/ 多平台（FR-12.5）/ 大促保障（FR-12.6）**后端无端点**，前端只有演示页 + 画板。

### 六个真断点（按修复优先级）

1. **Runtime 未接 `/chat` 主链**（FR-3/FR-5）：内核齐（状态机/checkpoint/注册中心/策略/执行器/6 连接器），但真实买家对话仍走 `chat_service` services 直调，Runtime 只能由 `/agent/run` 独立驱动 → 能力孤岛。前端 `agent` 端点引用仅 1 处。
2. **rerank 是 stub**：`Settings.BGE_RERANKER = "rrf-cosine-stub"`，`rerank_service.rerank()` 只是对 RRF 融合分排序，非重排模型；**BM25 全仓 0 命中**（`kw_scores` 是字面打分，不是 BM25）。FR-4/FR-13「向量+BM25+rerank」只实现向量+关键词+RRF。
3. **可观测是空占位**：`core/observability.py::record()` 直接 `return None`（14 行）；无 `metrics.py`/Prometheus/OTel/Sentry/Web Vitals。FR-9 基本未做，只有 `trace_id` 贯穿。
4. **生产存储未接**：`redis` 仅在 `config.py` 出现（未引入客户端）→ 库存预占仍非原子（超卖率=0 无保障）、无热点缓存、无大屏缓存；`MinIO/S3` 无（`media_store.py` 本地盘）。
5. **大屏接口是占位**：`frontend/src/api/screen.ts` 调 `/api/v1/screen/summary`，**后端无 `screen` 路由** → 404 后由 `BizScreenView` 回退 `@/mock/screen`（页面 import mock 两次）→ FR-11 纯演示，无日快照宽表。
6. **长会话虚拟列表缺失**（FR-1.1 明文要求）：无 `VirtualList`，`ChatMessage` 全量渲染。

### 易误判口径（下次别再踩）

- **前端 view 里查 `@/api/` 命中 0≠没接后端**：页面经 `composables/useXxx` 间接调 api（ChatView 走 4 个 composable），`api=0` 只是风格。判断接线要查 composable 的 `from '@/api/...'` 导入。
- `frontend/src/api/` 缺 `purchase.ts`/`finance.ts`/`risk.ts`/`agent.ts`/`mining.ts`/`promos.ts`（promos 走 `marketing.ts`）→ 对应页面无后端接线。
- `chat` 端点前端 URL 计数 0 是模板拼接误判，实际走 `chat.ts::streamChat`（fetch + `Authorization`）。
- `scripts/eval_vision.py` 在 `scripts/` 不在 `tests/`（别按 tests/ 找不到就判缺失）。
- `docs/knowledge-base/` 实为 **29 篇**（与 FR-13.1 逐类数量一致）。

### 实测资产规模（供对齐口径）

- 后端：20 个 endpoint 模块 / **86 个路由装饰器**（`执行步骤.md` 记 78 openapi paths）、27 个 service（`chat_service` 663 行、`vision_service` 501、`inventory_service` 409）、`modules/agent/` 8 文件（`runtime` 404/`executor` 284/`connectors` 248/`contracts` 223）。
- 前端：24 个 view、23 个组件（`ToolCallCard` 已有但仅 17 行）、22 个 composable、19 个 api 模块、13 个 vitest 文件。
- 后端测试 15 个（`test_workbench` 249 行、`test_multimodal` 349、`test_b2b_services` 370）。

### 探查方法（可复用，省 token）

临时 Python 探针（`_probe*.py`，跑完即删）一次性输出：路由密度、view/composable 接线、api 导出清单、测试清单、关键词命中（bm25/redis/sentry/VirtualList）。**比派 code-explorer 子代理更稳**——本轮 3 个子代理有 2 个被限流失败。

## 提交推送 v0.2.6 + 重启（014ae05，已 push origin/main）

- `执行步骤.md` 原写「`test_agent_runtime.py` 已落盘待跑」＝**没跑过**。实跑 3 红，全是测试侧口径错，产品行为正确：
  1. `test_schema_validation` 超长用例写 `"太长了"`（3 字）未超 `maxLength: 4` → 换 6 字以上串；
  2. `GET /approvals` **不带 `page`/`size` 返回数组**（`list_recent` 存量兼容），带 page 才回 `{items,total}` → 断言取 `items` 必须传 `page=1&size=20`；
  3. `executor.call` **策略先行**（Scope 不命中 4006/403，之后才 Schema 校验 1001）→ 验 1001 分支必须换成有 `order:read` 的用户（`CS`），买家身份只会拿到 4006。
- 修后：`test_agent_runtime.py` 13 项全绿，后端全量 **`pytest tests` 138 项 0 失败 0 错误**（计数姿势：`--junit-xml=$env:TEMP\x.xml` + Python 解析 `tests/failures/errors`，本机管道会吞汇总行）。
- **并行窗口正在写「编排接进 `/chat`」**：`runtime.orchestrate()`（+110 行，不落 tasks/checkpoint，返回 `refs/tool_calls/tool_block/approval`）+ `chat_prompt.build_messages(tool_block=)` 新增【业务查询】段 + `chat_service._finalize_turn(tool_calls,notes)`。提交时**已把这 3 个在途文件撤出索引**（`git restore --staged`），保持 commit 只含已完成的 Agent Runtime；`runtime.py` 索引内是旧快照（不含 orchestrate）。
- 重启验证：后端 `/health` ok、`/ready` ready、无 token `/api/v1/sessions` → 401、运行中 `openapi.json` **78 paths**（含 8 条 `/api/v1/agent*`）；前端 `http://localhost:5173` → 200。日志 `$env:TEMP\reai-{uvicorn,vite}.{out,err}.log`。
- 坑复现：`git push` 的 stderr 进度被 PowerShell 判为 `NativeCommandError`（红字但 exit 0）——认 `d10b276..014ae05 main -> main` 与 `## main...origin/main` 为准。

## 在途「编排接进 /chat」（B 步余①）核实：代码已成，差收尾

- 服务实况：8000 `/health` `/ready` 200、5173 200，均在跑。
- 工作区未提交 5 文件（chat.py / runtime.py / chat_prompt.py / chat_service.py / test_agent_runtime.py，+354/-18）＝并行窗口写的 B 步余①：`runtime.orchestrate()`（不建 tasks、不落 checkpoint、敏感工具无 tool_args 不触发、BusinessError 转 rejected 不外抛、返回 None=内核未装载由调用方回落直调）+ `chat_service._assemble_generation` 优先编排/回落 `knowledge_service.retrieve` + 开关 `AGENT_CHAT_ORCHESTRATE`（默认 True，已在 Settings）+ done 帧新增 `tool_calls`/`orchestration.notes`。
- 实测：`test_agent_runtime.py` 17 项、chat 三件（stream/llm/token_stream）17 项全绿；`ruff check app tests` 0 error。
- **收尾缺口（下次从这里接）**：① 全量 `pytest tests`；② `smoke_chat*.py` 打真链路（8000 带 reload 已加载在途改动）；③ `API接口与SSE事件协议规范.md` §5 done 帧补 `tool_calls/orchestration` 字段（docs-guard 硬拦接口与文档不同改）；④ `执行步骤.md` B 步余① 划掉 + v0.2.7 头部；⑤ 前端 `ToolCallCard`（现仅 17 行）渲染 done.tool_calls 属后续任务。

## B 步余① 收尾完成（v0.2.7，未提交）

- 上条 ①~④ **全部做完**：`pytest` **142 项 0 失败 0 错误 / 覆盖率 85.79%**（阈值 70）；`smoke_chat.py` 打真链路 **14 passed 0 failed**（含 `done.tool_calls wired via kb.retrieve`、`done.orchestration.notes is list`）；API 规范 §4.2/§4.12/§5 与 `执行步骤.md`（v0.2.7 头部 + B 步缺口改「编排层规则机器人降级话术」+ 本周焦点换成 C 步触发规则表）已同改。
- 本轮我补的**代码**内容：`_finalize_turn`/`_persist_agent_turn`/`answer`/`run_text_turn`/`stream_text_turn` 全链透传 `tool_calls`+`orchestration.notes`（重放帧给空数组，注释里写明「溯源走同一 trace_id 查 tool_calls 表」）；`chat.py` 的 `_empty` 帧与 done 帧同步补两字段（帧形不随分支变化）；`runtime.orchestrate` 开头加**内核未装载守卫**（`registry.maybe_get(DEFAULT_TOOL) is None → return None`），把「工具没装上」挡在编排层，**绝不放大成 2001 拒答**（这是保住 test_chat_stream 等老用例的关键）。
- `test_agent_runtime.py` 13→17 项：新增「主链路真调 kb.retrieve + tool_calls.trace_id 与回答同源」「退款缺参不自动开审批单（断言 `action != "order.refund"`，**注意种子自带 1 条 `sku.price_change` 待审**，别断言 pending 为空）」「开关关闭 + registry 清空两条回退路」「orchestrate 业务事实进 tool_block 且 tasks 行数=0」。
- 环境定论（已回写 MEMORY.md）：全量 pytest 必挂是**中文路径**导致 shim `GetShortPathNameW` 失败，用 `--basetemp="$env:TEMP\pt"` 解决。
- 遗留：前端 `ToolCallCard` 渲染 `done.tool_calls`（ChatView 气泡内），**属 UI 改动 → 按设计先行先改 `design.pen` 的 `-/workbench` 画板再写码**。工作区改动全未提交（index 干净）。

## v0.2.7 已提交推送（20eb66b + d655aa9，origin/main 同步）

- 收尾复核全绿后才提交：`pytest tests` 142/0、`ruff` 0、**`mypy app` 0 issue（78 files，本机 mypy 2.3.1 可用——MEMORY 旧口径「本机缺走 CI」已过时）**、openapi 78 paths、`smoke_chat.py` 14/14（新断言 `done.tool_calls wired via kb.retrieve`，执行器成功态字面量是 `"ok"` 不是 `"success"`）。
- **又踩 `-m` 中文乱码**：功能提交 `20eb66b` 走 `-F .git/msg.txt` 干净；随手用 `-m "中文"` 的 memory 提交 `d655aa9` subject 在仓库里就是坏的（GBK 误解 UTF-8 字节），且已 push，修正需 force-push main（安全红线，不做）。**教训：任何中文提交信息一律 `-F`，没有例外**——小提交也不许用 `-m`。
- 乱码探测姿势：`git show` 输出经 PowerShell 看不可信，用 Python `subprocess.check_output` 拿原始字节写临时文件再读。
- 垃圾文件随手清：cmd 重定向 `2>$null` 在 PowerShell 环境生成过字面文件 `$null`；被跳过的 `--junit-xml=_x.xml` 落过 `backend/_agent_rt_tmp.xml`——commit 前 `git status --short` 抓出来了。

## ArgoCD 清单核实 + 集群侧缺口回填（6585cb8，已 push）

- 用户报「ArgoCD 清单就绪、集群侧待接入」。核实：两份 Application 早在 `0df6b25` 就入库（dev `automated+prune+selfHeal`；prod 无 automated＝红线），`cd-update.yml` 联动逻辑完整（缺 `vars.DEPLOY_REPO`/`secrets.DEPLOY_PAT` 时 warn 跳过不阻断）——**仓内侧无未提交遗漏，工作区只有并行窗口的 `design.pen`**。
- **盘点发现真缺口＝部署仓 `reai-deploy` 不存在**：全仓 `kustomization.yaml` 0 个、`kind: Deployment` 只在部署工程化.md 示例里。`cd-update.yml` L57 缺 `envs/dev/kustomization.yaml` 会硬报错，所以接入第一步必须是建仓。已把四步清单（建仓→装 ArgoCD→配 vars/PAT→审批+回滚 RTO 演练）写进 `执行步骤.md` E 工序，部署行 ✅ 补「集群侧待接入」限定，防误读成 CD 已可用。
- 中文文件名再次踩坑：`python -c "...open(r'..\部署工程化.md')"` 路径里的中文被 GBK 截成 `部署工程?md`（OSError 22）——**Python 内联脚本也别带中文路径参数**，探测文档内容一律用 search_content 工具或 ASCII 通配。

## README 旧口径回填（0883af8，已 push）

- 用户引「Agent Runtime 状态机 ⬜ 待落地」——该句只存在于 `README.md` §16（v0.2.6 前旧口径），`执行步骤.md` 早已 🔶/✅。核实 `backend/app/modules/agent/` 实有 7 文件（contracts/registry/policy/executor/connectors/runtime/bootstrap），6 条 `/agent/*` 端点 + 主链 orchestrate 均已接线。
- 修了三处：§16 状态行 ⬜→✅（含缺口：FR-5 三级容错「规则机器人」降级话术）、「尚未落地的目标结构」段改成真实模块结构（原文件清单 `{state,orchestrator,tools,router,service}.py` 是设计期虚构，盘里根本没有）、头部版本 v0.1.0→v0.2.7。
- **教训：README §16「当前状态」表是第二事实源，容易漂**。每次版本推进（执行步骤.md 头部 bump）应顺检 README 头部版本行 + §16 表；grep「待落地/待建/P2 目标」是探测过期口径的快捷姿势。
