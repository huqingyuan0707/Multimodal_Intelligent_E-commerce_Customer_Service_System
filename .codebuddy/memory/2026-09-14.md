# 2026-09-14

## Pencil / design.pen：电商客服聊天界面草图

- 需求：用 pencil `execute` 在 `design.pen` 上画电商客服聊天界面粗略布局。
- **阻塞**：pencil MCP 调用全程失败 —— `MCP error -32603: failed to connect to running Pencil app: codebuddy after 3 retries: transport not connected to app: codebuddy`。工具定义可取（`mcp_get_tool_description` 正常），仅 app 传输层连不上；`get_app_state` / `read_skill` 同样失败。属扩展侧 IPC 问题，工具侧无法修复，已停止重试。
- **绕过**：`.pen` 是纯文本 JSON（schema 见扩展目录 `out/skills/pen-dev/pen-schema.md`，Document version `"2.17"`），已直接手写合法文档写入 `design.pen`（JSON 校验通过：version 2.17 / 根 frame「电商客服聊天界面」/ 3 个主面板）。
- **布局**：根 frame 1280x800、horizontal、三栏 —— 左侧「会话列表」(300 宽：搜索框 + 3 个会话项) / 中间「聊天区」(聊天头 + 转人工、消息流：机器人气泡+商品卡、用户气泡、图片缩略图、输入区快捷回复+发送) / 右侧「当前订单卡片 + AI 辅助/会话洞察（推荐回复）」。
- 修正记录：文本节点不能做徽标背景，未读数改用 frame 包裹 text（原误写非法字段 `fill2` 已删）。
- 待用户操作：需在 pen.dev 画布中重开/重载 `design.pen` 才会渲染出新内容。

## 设计先行门禁落地（design-first：`.pen` 出图 → 按图写代码）

- 用户规则：先 `design.pen` 出设计图，AI 再按图写代码；输入 = `多模态智能电商客服系统需求文档-FRDv2.md` + 现有代码。固化为**设计先行**，`.pen` 是 UI 唯一事实源。
- **核心决策**：不把约束写成「必须调用 pencil `execute` 工具」——工具调用不进仓库、无法审计，且本机 MCP 已实测连不上，规则会恒真失败。改为检查**可机检的产物不变式**（命名 / 覆盖棘轮 / 同改 / 色值）。
- 新增与改动：
  - `.codebuddy/rules/design-first.mdc`（alwaysApply）：工作流顺序、画板命名、色值唯一事实源、MCP 降级、豁免 trailer、门禁四项、自检命令。
  - `skills/design-first/SKILL.md` + 镜像 `.codebuddy/skills/design-first/SKILL.md`（按既有惯例两处各一份）：输入清单、`.pen` 字段速查、**深色 token→画板色值映射表**、状态完整性清单（三态/分页/权限/破坏性二次确认/多模态溯源）、画板落到代码的对照写法。
  - `scripts/check_design.py`（纯标准库、**不依赖 MCP**）：① JSON 合法 + 未闭合容器定位；② 画板 = 名字带 `-/<route>` 后缀的 frame，route 须在 `页面设计.md` §1 路由表内且不重复；③ 路由覆盖棘轮 `ROUTE_COVERAGE_MIN=3`；④ 顶层 frame 命名棘轮 `LEGACY_UNNAMED_MAX=1`；⑤ 画板色值 ∈ `tokens.css`（先 WARN，`--strict-color` 转硬拦）；⑥ `--staged` / `--range` 查「改 `frontend/src/{views,components,shared/components}` 必须同改 `design.pen`」，豁免写 `Design-Sync: skip reason="..."`。
  - `.github/workflows/ci.yml` 加 `design-guard` job（逻辑只在脚本里，yaml 不重复）；`AGENTS.md` §1 索引 + §2 工作流第 2 条 + §5 联动 + CI 说明同步。
- **首跑即抓到 3 个真问题**（当前 `python scripts/check_design.py`：PASS / ERROR 0 / WARN 3）：① 4 个画板全嵌在根 frame「电商客服聊天界面」内、非顶层并列；② 根 frame 缺 route 后缀，应为 `电商客服聊天界面-/workbench`；③ **画板 21 个色值与 `tokens.css` 全不一致**——画板浅色 SaaS（`#F5F6F8`/`#FFFFFF`/`#4C8DFF`）vs 代码深色科技风（`--reai-bg #0b1124`/`--reai-primary #4f6bff`），设计与实现已漂移。
- 路由覆盖 3/18（knowledge / approvals / screen），缺口 `/admin /chat /dashboard /finance /goods /inventory /login /logistics ...`。
- **坑**：画布打开时 `design.pen` 会被 pen.dev 应用自动写盘——本次两次运行之间该文件从「非法 JSON（第 2046 行缺逗号）」变为可解析，说明外部进程正在改。**不要趁画布打开时手改 `design.pen`**，易被覆盖。
- 待用户决策：画板配色跟随代码的深色 token（规则当前口径），还是反过来调整 `tokens.css`。

## 画板切深色 token（已完成，门禁 PASS / ERROR 0 / WARN 0）

- 用户在画布上已自行修掉结构债：4 个画板现在都是**顶层并列**（x=0/1340/2680/4020）。剩命名 + 配色两项。
- `design.pen` 无 `variables`/`themes`（纯字面量色值），无法用变量切换主题 → 只能改字面量。
- **`#FFFFFF` 有两种语义，必须按上下文映射**（盲替会写坏）：容器面（卡片）→ `--reai-card #161d33`；文字/图标（彩色底上的白字）→ `#ffffff`。
- Agent 气泡判定：`type=frame` 且 name 含「气泡」且 fill=`#FFFFFF` → `--reai-bubble-agent #f2f5fc`，其子树文字改走 `--reai-text-on-light #1f2430`（与现有 `.bubble` / `--reai-bubble-user` 代码口径一致，见 `WorkbenchView.vue` / `ChatView.vue` / `WidgetView.vue`）。
- 迁移映射（22 色 169 处，全部落地）：语义色直替 `#4C8DFF→#4f6bff`、`#FF5A36/#FF8400→#fb9236`、`#27AE60→#34d399`、`#E8B44A/#B7791F→#e8a33d`、`#7FA8FF→#22d3ee`；容器面 `#FFFFFF→#161d33`、`#F5F6F8→#0b1124`、`#F0F3F7/#F7F8FA/#B7C0CC/#D9DEE5→#1e2742`、`#EAECEF→#ffffff14`、`#D9E6FF→#4f6bff`、`#EAF3FF/#F0F7FF→#4f6bff1f`、`#FFFBF0→#e8a33d1f`、`#FFF4F0→#fb92361f`；文字 `#1F2329→#eef1f8`、`#4A5260/#8A9099→#8b94ad`。
- 画板改名：`电商客服聊天界面` → `电商客服聊天界面-/workbench`（补 route 后缀，命名债清零，路由覆盖 4/18）。
- **`tokens.css` 新增 3 个语义卡底 token**（画板用到 12% 主色/金色/告警色叠底，token 里原本没有 → 属画板自造色，按「tokens.css 加变量 → 画板用值 → 代码用 var()」顺序补齐）：`--reai-primary-soft: rgb(79 107 255 / 12%)` / `--reai-gold-soft: rgb(232 163 61 / 12%)` / `--reai-notice-soft: rgb(251 146 54 / 12%)`。**前端实现 VLM 检测卡 / 引用溯源卡 / 预警卡时直接用这三个变量。**
- 门禁侧修正：`check_design.py::norm_color` 原来把 `#ffffff14` 与 `rgb(255 255 255 / 8%)` 判为不同色（假阳性）→ 改为统一归一到 RGBA 后输出 `#rrggbb` 或 `#rrggbb@aa`（`to_rgba()`）。色值 alpha 只能用 hex alpha 通道（schema 原文：Fill opacity can only be set via the hex alpha channel），`#RRGGBBAA` 合法。
- 一次性迁移脚本 `scripts/_pen_migrate.py` / 探针 `scripts/_pen_probe.py` **已删除**（避免形成 `design.pen` 的第二个事实源）。**注意：脚本第二次运行会把 hex 统一改写成大写**，故 `.pen` 内色值现在全大写，门禁归一化后不受影响。

## 画板在哪看 + 重载与记忆瘦身

- **pen.dev 扩展在 CodeBuddy 的安装位**：`C:\Users\qingy\.codebuddycn\extensions\highagency.pencildev-0.6.71-universal`（VS Code 里另有一份 `.vscode\extensions\highagency.pencildev-0.6.71`）。MCP 服务由该扩展的 `out\mcp-server-windows-x64.exe --app codebuddy` 提供。
- **看画板的入口**（取自扩展 `package.json` contributes，非猜测）：`*.pen` 注册了 custom editor `pencil.designEditor`（"Pen.dev Design Editor"）→ 文件树双击 `design.pen` 即画布；显示成 JSON 时右键 **打开方式 → Pen.dev Design Editor**（右键菜单里那条 `pen.dev: Open Design File as Text` 是反向操作，别选）。活动栏有 **pen.dev** 容器 → `pencil.sidebar`「Recent Files」。快捷键 `Ctrl+Shift+\` = `pencil.toggleDesignMode`。
- **重载画布我无法代做**（IDE 侧动作），且 pencil MCP 第三次复测仍 `transport not connected to app: codebuddy` → 建议用户直接 `Developer: Reload Window`，一次同时解决「看到新画板」与「MCP 重新握手」。
- **重载前盘上校验（防被回写覆盖）**：mtime `16:28:32`、68159 B、4 个 route 齐全、浅色残留 0、深色 token 142 处、`check_design.py` = PASS / 0 / 0。顶层画板几何：`-/workbench` x=0、`-/knowledge` x=1340、`-/approvals` x=2680、`-/screen` x=4020，均 y=0 / 1280×800。
- **`MEMORY.md` 已瘦身重写**：原先把 `AGENTS.md` 的前后端红线与质量门禁整段抄进记忆，导致超限被截断。新口径——**记忆只存规则里没有的本机环境/复发坑/工具路径/当前进度，规范一律指向 `AGENTS.md` 不复制**。

## 画板补齐第 1 批（/chat /tasks /dashboard）

- 用户决策：**`/purchase` `/finance` `/risk` 本期做，画板先行**（§1 表已标「待实现」，代码后补）；出图顺序**按漂移危险度**：批1 `/chat /tasks /dashboard` → 批2 `/goods /orders /inventory` → 批3 `/studio /admin /marketing /logistics /login` → 批4 三张待实现页。
- `design.pen` 现 **7/18**：新增 3 张顶层画板 x=5360/6700/8040、y=0、1280×800、`clip:true`。棘轮同步收紧：`ROUTE_COVERAGE_MIN` 3→**7**、`LEGACY_UNNAMED_MAX` 1→**0**（命名债已清零）。门禁 `RESULT: PASS（ERROR 0 / WARN 0）`。
- **写画板的省 token 姿势**：临时 Python 生成器（`T/F/chip/bar/field/cell` 六个 helper）→ `json.load` → 按 route 幂等替换 `children` → `json.dumps(indent=2, ensure_ascii=False)` → **用完即删**。比手搓 JSON 省一大半 token，且色值走常量不会漂。
- **PowerShell 坑补记**：`python -c "…|…"` 里的管道符会被 PS 吞掉（报 `ModuleNotFoundError: frame`）→ 任何含 `|` 或中文的探测一律写成临时 `.py` 文件跑，或输出到 UTF-8 文件再 `read_file`。
- 同 commit 联动已做：`页面设计.md` §1 补 3 行「待实现」标注 + 画板覆盖台账；`执行步骤.md` 盘点表加「设计先行」行、新增 **G 步「画板补齐」**（含 T3 口径债：`/reviews /aftersales /widget` 代码有文档无，`/403 /404 /500` 建议豁免到组件级）、版本 v0.1.2→**v0.1.3**。
- 回滚保险：`.codebuddy/design.pen.bak`（`design.pen` 是 **untracked**，git 救不回来，故先留 bak；用户确认画布显示正常后删除，勿提交）。
- 意外产物：工作区多了 untracked `pencil-shadcn.pen` 与 `.github/{agents,hooks,skills}/`，**非本次改动**，疑为 pen.dev 扩展打开示例文档时生成，提交前需甄别。

## 画板补齐第 2 批（/goods /orders /inventory）→ 18/18 满覆盖

- 用户在我出第 2 批期间**在 pen.dev 画布上自己画了第 3/4 批**（`/studio /admin /marketing /logistics /login /purchase /finance /risk`，x=9380→18760），并给三张待实现页补了最小可用 views + 路由（演示数据 + 分页 20）。→ **写盘前实测覆盖已是 15/18**，我原计划的 x=9380 起会与之重叠，改放 **x=20100 / 21440 / 22780**。教训：并行编辑是这个项目的常态，坐标与棘轮基线都要先读盘再定。
- 第 2 批内容按真实 view 代码落板（不套 §3 文档泛写）：`/goods` = SPU 表 + SKU 子表（改价恒进审批、改条码行内直改）+ 颜色×尺码矩阵（库存 0 橙底）+ 下架 confirm + 权限差异（`cs` 只读不渲染操作列）；`/orders` = 7 态快捷筛选 + `allowed_actions` **置灰而非隐藏** + 3005 状态机口径 + 两步 prompt 发货 + 建售后抽屉（trace 跳 workbench、超阈值转审批 APV-xxxx）；`/inventory` = 缺货预警卡（一键生成采购→`/purchase`）+ **可用 = 在库 − 预占 − 锁定** 公式旁注 + 调拨三步 prompt 拆两行流水 + 盘点差异进审批 + 流水「无数据如实报错，不伪造」。
- 棘轮 `ROUTE_COVERAGE_MIN` **15→18**（满值，少一张即 CI `design-guard` ERROR）；`LEGACY_UNNAMED_MAX` 保持 0。门禁 `RESULT: PASS（ERROR 0 / WARN 0）`，`design.pen` 484,504 B。
- **新坑：单次 `write_to_file` 写 ~22KB Python 会被输出预算截断**（连续两次 `malformed JSON / Unterminated string`）。对策：**先写 helper + 第 1 个函数 + `BOARDS` 字典 + `main()`，再用 `replace_in_file` 往 `BOARDS = {...}` 那行前逐个插函数**，每片 ≤8KB。生成器本身仍用完即删（含 `scripts/__pycache__/_pen_*.pyc`，`scripts/__pycache__` 已 gitignore）。
- 文档同步：`页面设计.md` §1 覆盖台账改 18/18 + 加「覆盖满 ≠ 完成」注；`执行步骤.md` 盘点行缺口改「三页仅演示数据，接口与审批流待补」、G 步加**转常态维护**两条（棘轮满值 + views/画板同改）与坐标台账。

## 本轮改动提交入库（4 个 commit，门禁全绿）

- 提交前门禁：`pnpm lint` 0 error、`pnpm typecheck` 通过、`python scripts/check_design.py --staged` = **PASS / ERROR 0 / WARN 0（画板覆盖 18/18）**。
- 拆分：`chore(design)` 设计先行规范+`scripts/check_design.py`+CI design-guard → `feat(ui)` `design.pen` + 前端页面/tokens/router/mock（**画板与 views 同 commit**，满足 design-guard 同改约束）→ `chore(tools)` `.github/{agents,hooks,skills}` impeccable 资产（54 文件）→ `docs(memory)` 记忆文件。
- **新坑（重要）**：`git add --pathspec-from-file=.git/ps-a.txt` 之后 `git diff --cached` 出现 **83 个文件**（含只读的 `*.pen.bak`、`pencil-shadcn.pen`）——用户在并行窗口跑了 `git add -A`。对策已验证：`git reset -q`（不动工作区）清索引 → 再按 pathspec 精确暂存 → **每次 commit 前重跑 `git diff --cached --name-only` 核对**。
- `.gitignore` 新增 `*.pen.bak` 与 `pencil-shadcn.pen`（pen.dev 扩展产物，非事实源），避免误入库。
- 本机 git **钩子未生效**：`git rev-parse --git-path hooks` = `.git/hooks` 且只有 `*.sample`；`frontend/.husky/{pre-commit,commit-msg}` 存在但 `core.hooksPath` 未设 → lint-staged / commitlint 本地不跑，门禁实际靠 CI。要启用需在仓库根 `git config core.hooksPath frontend/.husky`。
- Chinese 路径提交姿势沿用：pathspec 与 commit message 均写 UTF-8 文件（`.git/ps-*.txt`、`.git/msg-*.txt`）→ `git add --pathspec-from-file` / `git commit -F`；`cd 'c:/…中文…'` 会让 PowerShell 报「字符串缺少终止符」，改用相对路径或不带中文的 cwd。
- 已 push：`010256b..563b40f main -> main`（4 commit）。`git push` 的 `To github.com:…` 走 stderr，PowerShell 会渲染成 NativeCommandError，**不是失败**，以 `main -> main` 那行为准。
- impeccable 资产不止 `.github/{skills,agents,hooks}`，还会往 `.claude/commands/impeccable.md`、`.opencode/command/impeccable.md`、`.codebuddy/commands/`、`.github/prompts/` 落文件（并行窗口在改，属跨工具各写一份的既有惯例）。

## 对话框命令弹窗：/impeccable 6 子命令拆成独立注册文件

- 需求：在 AI 对话框输 `/impeccable` 就弹出命令列表（audit/distill/clarify/bolder/quieter/animate）。上游 impeccable 只有**单入口 + argument-hint**（`SKILL.md` frontmatter 的 `argument-hint` 列了 24 命令），弹窗里不会出现子命令条目 → 靠 `reference/routing.md` §No-argument routing 让 AI **打印**菜单，不是 IDE 级弹窗。
- 做法：`.codebuddy/commands/impeccable-<子命令>.md` 各一份（共 6 份），命令名用**短横线**而非 `/impeccable <sub>`——前缀匹配保证输 `/impeccable` 时 7 条一起弹；每份正文固定 4 步（`impeccable context` → 读 `reference/<sub>.md` → 缺省目标取当前改动面 → **本项目附加约束**），并把「画板同改 / token 取色 / 状态完整性与分页不许删 / SSE 分帧不动 / `prefers-reduced-motion`」写进对应子命令。主入口 `impeccable.md` 的 6 条改成对照表，`AGENTS.md` §7 加登记行。
- **关键坑（会静默失效）**：这些命令文件原本首行是项目惯例的 `<!-- 职责… -->` 注释，**frontmatter 不在第 1 行 → 解析不到 `description`/`argument-hint`**，弹窗只剩命令名。已用一次性脚本把 frontmatter 提到首行、注释移到 `---` 之后（7 文件全 `fm@line1=OK`），脚本用完即删。→ 以后新写 `.codebuddy/commands/*.md`：**frontmatter 必须第 1 行，注释放它下面**。
- CodeBuddy 命令目录实测口径：项目级 `.codebuddy/commands/`；用户级 `~/.codebuddy/` 下**没有** commands 目录（只有 skills/plugins/mcp.json 等）。官方文档「斜杠命令」页是 VitePress JS 渲染，`web_fetch` 拿不到正文（10s 超时 / 只回骨架）→ 别再走文档，直接看本地文件与 IDE 弹窗验证。
- 待用户验证：对话框输 `/impeccable` 应弹 7 条；若一条不弹＝本机 CodeBuddy 不读该目录或需 `Developer: Reload Window` 重扫。

## 对话助手/坐席工作台前后端联调（转人工全链路，v0.2.5）

- **与并行窗口的分工实况**：我写 `api/workbench.ts`/`useWorkbenchQueue.ts`/`WorkbenchQueue.vue` 期间，用户另一窗口同步交付了更完整版本（幂等键、服务端分页/筛选/keep 策略、状态页签、WorkbenchChat/WorkbenchNotes/WorkbenchTrace 拆分组件、smoke_workbench.py、API 规范 §4.11、执行步骤 v0.2.5、画板重画）。**我的版本被磁盘覆盖 → 不恢复、以磁盘为准适配**：WorkbenchView 对齐新解构 + watch(currentId) 联动 loadHistory、useHumanHandoff 改调 `handoffWorkbenchApi`、两份测试重写对齐。教训固化：**动手写共享层前先 git status + 全文搜索目标名，确认并行窗口是否已有版本**。
- 我的净贡献：`useHumanHandoff.ts`（买家侧转人工：t- 占位先 createSessionApi 落库 → adoptSession → handoffWorkbenchApi → 系统提示行）+ 3 项单测（ElMessage 必须 mock，node 无 DOM；session store 用真实 Pinia `$patch`）；`WorkbenchChat.vue` v-for key 修复（QUICK_REPLIES 已是 `{key,label}` 对象数组）。
- 全链路验证全绿：前端 lint/typecheck/test 64 用例（13 文件）/build；后端 pytest 125 全过 + **smoke_workbench 15/15**（登录→队列鉴权→建会话→handoff→pending 入队→认领→备注→坐席代回→trace→resolve→重复解决 1001 拦截→过滤）+ smoke_chat 12/12 + smoke_sessions 9/9；design-guard `--staged` PASS 18/18 ERROR 0。
- vitest coverage 竞态：并行窗口同时在跑测试时 `coverage/.tmp/coverage-0.json` ENOENT 假失败 → 清 `coverage/` 重跑即绿，勿当代码问题排查。
- 新 shell 坑：`cd c:\…中文…` 后执行命令，行尾中文路径最后一个字符会被 GBK 截断吃掉（`;` 被吞进路径）→ **不 cd，直接相对路径执行**（初始 cwd 已是工作区根）。

## 全站去页面大标题（18 个业务页 h2 + 画板页头同改，门禁全绿）

- 需求：用户「所有页面去掉红框这种标题」＝各业务页顶部 `<div class="head"><h2>页面名</h2>…` 大标题。**取舍**：登录页（h1「智能客服系统」）、403/404/500 错误页标题是页面主体结构，内容区 h3（GMV 趋势等）与侧栏「历史对话」h3 不是这种标题 → 全保留；head 里的演示数据 tag / hint / 刷新按钮也保留（只删 h2）。
- 代码侧：18 个 view 删 h2 行 + 7 处专属 CSS 块（`.head h2` / ChatView `.top h2`，正则 `\n\.(head|top) h2 \{[^}]*\}\n`），删后把连续空行压一。admin/finance/goods/inventory/orders/purchase/risk/studio 这些页面 head 里只剩 `v-if="demo"` tag，平时 head 为零高空 div（`.head` 无 margin，无留白问题）。
- 画板侧（design-guard 同改约束）：只有 **7 张画板**有「页头」frame 且首个 text 是标题——approvals/screen/tasks/dashboard/goods/orders/inventory；其余画板顶栏是「路由缩写+注释」的信息架构示意图（studio/admin/…texts=['admin','/admin · 仅管理员…']），本就没有页面大标题，无漂移。workbench/chat/knowledge 无页头。**Approvals 页头是组合文案「审批中心 · 待办 2 · 分页20/批量批」整体删除**（代码里待办数不在 h2）。
- 门禁全绿：`pnpm lint` 0 error、`pnpm typecheck` ✓、`pnpm build` ✓ 8.97s、`python scripts/check_design.py` PASS 18/18 ERROR 0 WARN 0。棘轮不受影响（画板数没变）。
- 工具坑再确认：`search_content` 对 `<h2`/`h2>` 这类带尖括号的模式**返回 0 命中**（GoodsView 明明有）→ 模板结构扫描一律走临时 python 脚本 + `sys.stdout.reconfigure(encoding="utf-8")`（GBK UnicodeEncodeError 老坑）。
- 同会话前情：用户报 `ERR_CONNECTION_REFUSED :8000`，是上轮收尾把冒烟 uvicorn 停了 → 后台重启（`Start-Process backend\.venv\Scripts\python.exe -m uvicorn app.main:app`，**-WorkingDirectory 必须 backend，cwd 是根目录时相对 venv 路径找不到**），`/ready` 200、`/auth/me` 无 token 401 确认链路通。此 uvicorn 不常驻，机器重启后需手动拉起。



- 按 `AGENTS.md` §8 跑完 `impeccable context → critique → polish`（launcher `.github/skills/impeccable/scripts/impeccable.cmd` 本机可用，无需 `npx install`）。按 `reference/craft-floor.md` 逐条比对，**实际抓到 6 处真问题**（不是走过场）：
  1. **原生 `<button>` 没有键盘焦点环**：Element Plus 的 `:focus-visible` 只覆盖 EP 组件，`.quick`/`.play`/`.suggest` 这些自己写的 button 全裸奔 → 各补 `outline: 2px solid var(--reai-*) + outline-offset: 2px`，顺带补 hover（原来 hover 态也缺）。**以后在本项目手写 button 一律配 hover + focus-visible。**
  2. **保存失败会吞掉用户输入**：`WorkbenchNotes.submit` 里 `emit('add')` 后立即 `draft=''`，后端失败则坐席刚写的一段灰飞烟灭 → 改为**只有父级确认成功才由 `reset()` 清空**（`WorkbenchView.saveNote` 里 `if (await addNote(...)) notesRef.value?.reset()`，`ref<InstanceType<typeof WorkbenchNotes> | null>`）。`defineExpose({reset})` 由此从死代码变成真机制（此前用 `:key` 重挂，属冗余）。
  3. **右栏出现两种卡面**：`WorkbenchNotes` 用 `--reai-card` 实心卡，而 `WorkbenchSide` 其余 4 块是 `--reai-glass-bg` 玻璃卡 → 备注卡改玻璃卡对齐。
  4. `WorkbenchQueue` 选中页签 `.chip-on` 用了 `--reai-online`（绿字）+ 蓝色叠底 → 语义冲突，改 `--reai-primary`。
  5. **300px 窄栏里 `el-pagination` 的 4 段 layout 必然溢出** → `.pager` 加 `flex-wrap: wrap`（红线要求 layout 不许删，只能用换行消化）。
  6. `WorkbenchSide` 硬编码 `font-family: monospace` / `font-size: 12px` / `font-weight: 600` → 回到 `--reai-font-mono` / `--reai-fs-micro` / `--reai-fw-semibold`。
- **画板 vs 代码结构对齐（重要决策）**：画板原把右栏画成「一张大卡套 5 段」，代码实际是「5 张并列玻璃卡」。判定**以代码为准反修画板**（这 5 卡布局是 v0.2.1 起的既成视觉世界，且 craft-floor 明确「卡片套卡片恒错」）→ 临时生成器把 `WorkbenchSide` 自身卡面清空（只留 vertical 列 + gap 12），5 个子区块各挂 `--reai-glass-bg #161D3399` + `--reai-glass-border #FFFFFF1F` + radius 12。**板与码同结构，不再一卡套 5 段。**
- **`mypy app` 原来不是绿的**（1 error，`chat_service.py:492`）：`usage=dict(result["usage"]) if isinstance(result.get("usage"), dict) else {}` —— 守卫的是 `.get()` 表达式、取的却是 `[]` 表达式，mypy 收窄不传导 → `dict(object)` 无匹配重载。修法：同口径提局部变量（与相邻 `refs_raw`/`guard_raw` 风格一致）`usage_raw = result.get("usage")` → `dict(usage_raw) if isinstance(usage_raw, dict) else {}`。**规则：`isinstance` 守卫之后必须用同一个表达式取值，禁止 `.get()` 守卫 + `[]` 取值。** 修完 `mypy app` = `Success: no issues found in 68 source files`。
- 本轮最终门禁（全绿）：前端 `lint` 0 / `typecheck` 0 / vitest 13 文件 64 用例 / `build` ✓；后端 `py_compile` + `ruff check .`（All checks passed）+ `mypy app`（0）+ **pytest 125 用例 0 失败 0 跳错、覆盖率 85.67%（阈值 70%）** + `smoke_workbench.py` 15/15；`python scripts/check_design.py` = PASS / 18/18 / ERROR 0 / WARN 0。
- **误入暂存区事故（又一次并行 `git add -A`）**：临时文件 `backend/_pytest_out.txt`（我跑 pytest 落盘用）状态变 `AD` —— 已被并行窗口 `git add -A` 收进索引。用 `git rm --cached -q --ignore-unmatch -- backend/_pytest_out.txt` 撤出，复核 `git diff --cached --name-only` 已无 `_pytest`。**教训：跑测试/探针的输出文件一律写到 `$env:TEMP`，不要落仓库（哪怕 gitignore 了，索引里仍可能被扫进去）。**

