# 执行步骤 —— 从 FRD 到系统的实施路线

> 对齐文档：`多模态智能电商客服系统需求文档-FRDv2.md` §6 核心业务流程 / §7 验收标准 / §8 分期路线
> 本版日期：2026-09-16（v0.3.3）。上一版写于「前端页面已搭好、后端未开工」阶段；**现在 P0 文本主线与 B 端一期代码已落地并跑通容器化部署**，本文改为「现状盘点 + 剩余工序」。
> v0.3.3：**工程化加固两批（①husky 本地钩子 + ②缓存/限流/黄金集/迁移链）**——① `core.hooksPath` 指到 `frontend/.husky` 后本地提交真正过闸：pre-commit 修 monorepo 路径（`cd frontend` 再跑 lint-staged，此前从仓库根提交必挂）、commit-msg 双检 `commitlint` + 新增 `scripts/check_commit_sync.py`（R1 驼峰标识符必须与 diff 文件对得上、R2 scope↔路径互查、Design-Sync trailer 硬校验 WARN/ERROR，CI 同挂一个 job；软口径不碰 git hooks 本身）。② 新增 `core/cache.py` **Redis 适配层**（可用 → 真缓存/计数；不可用 → 进程内回退，`status()` 进 governance 七层巡检可见）+ chat 两端点挂**对话限流**（`CHAT_RATE_LIMIT_PER_MIN=30` 可热更，超限 `2002`/429 中文提示）+ 检索**黄金集 200 条**（`seed/golden_set.tsv`，六域+拒答+对抗）与 `scripts/eval_golden.py`（recall@k / 期望未命中 / 应拒未拒三口径，棘轮基线 + 验收线分离，CI 门禁跑检索模式无外依赖）+ **Alembic 迁移链 4→1 squash**（基线 `12cb8192e4f1` 全表，新库一条到位；CI 新增 PG 离线 upgrade + `alembic check` 双闸，防模型-迁移漂移）。验证：② 文件 ruff 0 error（顺带修 eval_golden/test_cache/squash 基线 10 项 import 排序与冗余 noqa）、`test_cache.py` 6 项过、`eval_golden.py` 本地 **RESULT: PASS**（棘轮达标，验收线差距如实输出——BGE/rerank/域守卫为收紧路径）。⚠️ 工作区另有**技能组路由半成品**（handoff_routing/workbench_service 等，`handoff_routing` 未导入即 NameError、pytest 3 红）属并行工序，**未随本批提交**。
> v0.3.2：**工作台组件测试 + E2E 脚手架**——根因是工作台三栏组件（队列/会话流/工具卡）零组件级测试，「守卫弹回 → 登录回跳 → 队列渲染」跨页链路完全没验证，改动只靠 lint/typecheck 兜底。落地：① vitest 组件测试 **19 项**——`ToolCallCard.test.ts` 7 项（空不渲染/成功·待审批·被拒 pill/meta 口径/默认展开可折叠/已知工具专属摘要/notes 合并编排说明）、`WorkbenchQueue.test.ts` 6 项（行渲染与空预览回落/选中态/页签 filter/搜索 300ms 防抖/空态与 loading 区分/头部计数）、`WorkbenchChat.test.ts` 6 项（角色行渲染/**ToolCallCard 挂消息行同级**——按画板挂法验不在气泡内/语音行上抛/锁定态输入·图片·语音·转人工全禁用/发送与快捷话术事件/流式阶段文案）。② **Playwright E2E 脚手架**：`playwright.config.ts`（chromium，webServer 复用 5173 dev）+ `e2e/workbench.spec.ts` 冒烟 2 项（未登录弹回 / 登录回跳后队列渲染），接口全走 `page.route` mock 且**形状对齐真实契约**（登录 `{token,user}`、队列 `items`+snake_case、`/auth/me` 身份），不依赖 8000 后端即可跑绿；`pnpm test:e2e` 本地跑，CI 暂不接（需浏览器下载，链路稳定后并入）。顺带清两笔小债：`el-pagination` 弃用属性 `small` → `size="small"`（测试输出刷弃用告警）；`.gitignore` 补 `test-results/`、`playwright-report/`。验证：`pnpm test` **18 文件 89 项全过** + `playwright test` **2/2 过** + `pnpm lint`/`typecheck` 0 error。
> v0.3.1：**E 步可观测地基落地——30s 接起率从「无从度量」到「有数据口径」**（FR-7 验收的前提）——根因是 `core/observability.py::record()` 一直是 `return None` 空占位，全站 30+ 处留痕（chat/llm/agent.tool/handoff/rag.*）全部落进黑洞，v0.2.9 规则表写的 `handoff` 审计同样没存下来。改动：record() 双出口——①进程内计数器/布尔标记/滑窗（deque 500，锁保护，重启清零）：`handoff(applied)` 记挂起时刻、`handoff.claim` 算「挂起→接起」耗时进样本；②守护线程（双检锁惰性起）攒批落 JSONL `data/observability/events-YYYYMMDD.jsonl`（每行 ts+event+语义字段，队列满即丢、目录炸只丢事件，**绝不抛错绝不阻塞业务**）；`OBSERVABILITY_ENABLED/DIR/ANSWER_TARGET_SECONDS(=30)` 进 Settings（前两个可热更）。`workbench_service.claim()` 成功路径补记 `handoff.claim` 事件（接起率分子来源）；新增 `GET /workbench/metrics`（cs/admin）：`observability.snapshot()`（接起率 avg/p95/达标率 + llm_ok_rate + tool_ok_rate + reject_count + 事件计数）+ 本租户队列四态存量。接 Prometheus/Langfuse 网关时只替换本模块落盘出口，事件名与字段口径不变。验证：`test_observability.py` 5 项（计数/标记、接起耗时管道、flush 真落盘、目录炸不抛、开关关不落盘）+ `test_workbench.py` 新增 metrics 端点项（形状+认领后计数即时+买家 1003）；全量 pytest **168 项 0 失败 0 错误**、覆盖率 86%、ruff/mypy/check_arch 全绿。API 规范 §4.11（80 paths）+ 数据模型 §5 同改。
> v0.3.0：**主题体系重设计——深色科技风 → 浅色默认 + 深色可切**（坐席反馈深色长时间盯屏累、观感发闷）——根因是全站只有一套深色 token：`main.ts` 无条件 `add('dark')`，`tokens.css` 深色值写死在 `:root`，画板 18 张全画深色。落地：① `tokens.css` 拆双层——`:root` 给浅色值（冷灰底 #f5f7fb + 纯白卡 + 墨字，语义色青/金/绿/橙加深一档保对比），`html.dark` 覆盖块保留原深色基线；新增 `--reai-topbar-bg/--reai-nav-*` 随主题翻（浅色白底深字蓝激活、深色渐变白字）与 `--reai-text-on-brand`（品牌蓝上前景恒白，气泡/头像/主按钮等 25 处原误吃 `nav-active` 的改吃它，不随主题翻转）；EP 主色/语义色/字号经 `:root:root`（提权压过 EP dark 包）+ `color-mix(×--reai-bg)` 归一品牌梯度。② 新增 `composables/useTheme.ts`（localStorage('reai_theme') 持久化 + `<html>` .dark 类，EP dark css-vars 同键天然联动；`initTheme()` 在 mount 前落类防深色闪白）+ `useTheme.test.ts` 3 项（node 桩 document/localStorage，重置模块态验默认浅色/切换持久化/读偏好）。③ `AppLayout.vue` 顶栏加 ☾/☀ 切换按钮 + 挂墙大屏 `/screen` 锁深色：路由 watcher 进 /screen 强挂 .dark、离开还原用户偏好（EP popper 传送 body，组件内挂类够不着）。④ **design.pen 18 张画板色值全量重映射为浅色调**（981 处 fill/stroke 单趟精确映射，含 白透明层翻墨透明层；品牌上白字/蓝柔底/白泡墨字不动；JSON 校验 + diff 复核仅色值变化），`/screen` 画板（22 节点）反向回填深色=锁深口径。⑤ 文档同改：DESIGN.md（front-matter 色板 + Overview「冷白光控台」+ Colors 双值 + **The Two-Theme Rule**）、页面设计.md §2 升 v1.6、design-first Skill §5 映射表改双列。验证：`pnpm lint` 0 error + `pnpm typecheck` 0 error + `pnpm build` ✓ + `pnpm test`（含新 useTheme 3 项）全过 + `check_design.py --strict-color` **PASS（ERROR 0 / WARN 0，18/18）**（画板色值与 tokens.css 双主题全集一致）。
> v0.2.10：**全站字体统一**——根因是三套字面口径打架：① tokens 栈「苹方→雅黑→…→system-ui」与 Element Plus 默认栈 `--el-font-family`（Helvetica 优先、末位中文雅黑）并存，EP 组件内部 `font-family: inherit` 时好时坏，浮层（popper 挂 body）走 EP 自己的栈；② 4 个视图硬编码 `font-family: monospace` 绕开 `--reai-font-mono`；③ DESIGN.md Typography 与 tokens.css 字阶漂移（Title 18 vs 16、Body 13/1.5 vs 14/1.6）。改动：选型**阿里巴巴普惠体 3.0**（电商同赛道事实标准、免费商用、9 字重），落地「**本地字面优先、系统栈回退、不下载 webfont**」（CJK 全量 5-10MB，本地部署客服台不值当付 FOIT/带宽）——新增 `shared/styles/fonts.css`（@font-face 只注册 local()，把 3.0 各字重独立族名归一成 CSS font-weight 可命中的 `Alibaba PuHuiTi 3.0`），main.ts 在 tokens 前 import；tokens 字体栈头部插普惠体，并覆写 `--el-font-family` + 6 档 `--el-font-size-*` 对齐 `--reai-fs-*`（EP 与业务从此同一梯度）；body 加 `font-variant-numeric: tabular-nums`（单号/金额/队列条数不跳动）；4 处硬编码 monospace 收敛 `var(--reai-font-mono)`；DESIGN.md front-matter 与 Typography 节回填为唯一口径（token 引用制，禁止再写字面量 px/字体名）。验证：`pnpm lint` 0 error + `pnpm typecheck` 0 error + `pnpm build` 成功 + `check_design.py` ERROR 0/WARN 0。Design-Sync: skip reason="字体族/字阶 token 收敛，无视觉布局变化，design.pen 无 fontFamily 字段不受影响"。
> v0.2.9：C 步（本周焦点）**转人工触发规则表落地**——根因是挂起判据散在链路里（chat 侧拒答/低置信各写死一句 reason、编排空手另有分支），「3 次不懂 / 愤怒 / 连续失败」根本没有，加一条规则就得改一处业务分支。改动：新增 `services/handoff_rules.py`（**9 条规则**带 `priority`：喊人工 / 情绪激烈 / 敏感送审 / 图检低置信 / 无据拒答 / 编排空手 / 工具空手 / 连续未解决 / 连续降级；命中多条取优先级最高者挂起、其余落 `matched` 审计；喊人工与愤怒词表、三个阈值全在 `Settings.HANDOFF_*` 且进 `_HOT_FIELDS` 可热更，阈值 0 = 关该规则）+ `services/handoff_service.py` 统一挂载点 `auto_handoff()`（先查历史 guard 补「连续未解决 / 连续降级」计数再判定，命中且会话仍空闲才 `none→pending`；已认领/已解决只记 `applied=false` 不抢，坐席代回即清零连续计数）+ 只读端点 `GET /workbench/handoff-rules`（规则清单+阈值+总开关，坐席可核对「这个会话为什么进了队列」）；自动挂起收口为两处（chat 落库段、`runtime._settle`），原 `mark_pending_if_idle` 直调全部改经规则表。同步修一处**文档-代码漂移**：API 规范 §5 早已声明 `done` 帧带 `handoff{...}` 但流式端点没透出，现补齐且帧形不随分支变化（重放 / 空问题给 `blank_handoff` 稳定形状）。顺带清偿架构债：`workbench_service.py` **450→330 行**（挂载点拆出 `handoff_service.py`，回到 400 行红线内）；`skills/anti-shit-code/scripts/check_arch.py` 棘轮给存量记名（`endpoint-db-op` 补 agent 1 处；`file-too-long` 4 处改成**行数预算**语义——文件涨一行即红、拆小后提示收紧，解决「记 1 条永不收紧」的空转），门禁由红转绿。验证：`py_compile` 全过 + `ruff check .` 0 error + `mypy app` 0 issue（80 files）+ `pytest tests` **161 项 0 失败 0 错误**、覆盖率 **86%**（阈值 70；新增 `test_handoff_rules.py` 17 项 + `test_workbench.py` 规则表挂载 2 项）+ `check_arch` **PASS（0 new / 14 baseline debt）** + `smoke_workbench.py` **17/17**（+2 条规则表检查）+ `smoke_chat.py` **17/17**（+3：`done.handoff` 字段齐全 / 喊人工真命中 `explicit_request` / 真挂起 `pending`）+ `check_design.py` ERROR 0 / WARN 0 + openapi **79** paths（API 规范 §4.11 自查口径由「73」更正为实际的 79）。API 规范 §4.2/§4.11/§5 同改。
> v0.2.8：前端 **`ToolCallCard` 落地**（消费 v0.2.7 的 `done.tool_calls[] + orchestration.notes[]`，兑现 B 步验收「ToolCallCard 透明展示」）——类型/透传/渲染四处接线：`types/agent.ts` 新增 `ToolCall` 形状（`tool/status/scope/idempotent/approval_required/approval_id/args/result/attempts/latency_ms/timeout_seconds/code/message/trace_id`，与 `POST /agent/tools/{name}/invoke` 出参同形）+ `AgentMessage.tool_calls/notes`；`api/chat.ts` `DonePayload` 增 `tool_calls` 与 `orchestration{notes[]}`（加法字段，不复刻 `toAgentMessages` 历史回放——工具审计不随消息落库）；`useAgentStream.toMessage` 原样透传；`ToolCallCard.vue` 按画板 `wtk00/wtk10` 渲染（头部「工具调用 · 工具名」+ 状态胶囊：`ok` 成功绿 / `approval_required` 待审批金 / `rejected` 被拒橙，箭头折叠参果**默认展开**；元信息 `scope · 耗时 · 次数 · trace_id` 短号；「参数」原始 JSON；「结果」一行中文摘要（`kb.retrieve` 命中条数+最高相似度、订单/物流/库存/优惠券字段、恒送审「已提交审批（账目未变动）+ 单号」、未成功显示后端中文 `message`）；下方「编排说明」橙条透出 `notes`），`WorkbenchChat` 把该卡挂在对应 Agent 消息行下方（`v-for` 改 `<template>` 包住消息行+卡）；买家侧 `/chat` 不渲染（内部审计信息不外露）。画板同改：`-/workbench` 补 `wtk00 工具调用卡`/`wtk10 编排说明行` + `wtk30 状态族`（待审批/被拒两态，色值全取 `tokens.css`）。验证：`pnpm lint` 0 error + `pnpm typecheck` 0 error + `pnpm test` 14 文件全过（新增 `useAgentStream.test.ts` 3 项：透传/空态回落/无 done）+ `pnpm build` 成功 + `scripts/check_design.py` **ERROR 0 / WARN 0（18/18）**；页面设计 §3.2/§4/§7 同改。
> 文档同步（0883af8，纯文档）：`README.md` §16「Agent Runtime 状态机 ⬜ 待落地（当前为 services + SSE 形态，P2 目标）」系 v0.2.6 前旧口径，已回填为 ✅（含真实缺口「规则机器人」降级话术）；§「尚未落地的目标结构」虚构文件清单（`state/orchestrator/tools/router/service.py`）改为 `modules/agent/` 真实 7 文件结构；README 头部版本行同步 v0.2.7。**README §16 是第二事实源、易漂：每次版本 bump 提交时须联动更新 README 头部版本行与 §16 表**（v0.2.8 已随 e28663d 补齐）。
> v0.2.7：B 步余项①**编排接进对话主链**（本周焦点，原「两条并行轨道」合流）——`/agent/chat`（`/chat` 别名）与 `/agent/chat/stream` 的检索段不再直调 `knowledge_service.retrieve`，改由新增的 `runtime.orchestrate()` 规划 → `executor.call()` 执行 → `kb.retrieve` 结果回喂**同一** `build_messages/validate_references` 口径（不建 `tasks` 行、不落检查点，对话轮次不是任务）；业务查询事实以 `【业务查询】` 块注入提示词（`chat_prompt.build_messages(tool_block=...)`，不占 `[n]` 引用编号）；`ok()` 与 `done` 帧新增 `tool_calls[] + orchestration{notes[]}`（加法字段向后兼容，`ToolCallCard` 直渲，中文说明如实透出「缺必填参数/无权调用/已转人工」）。安全兜底三条：① 对话链只给 `kb:read` 兜底 Scope，其余工具严格按 Token 角色判权（越权记 note 后回落检索，不泄漏存在性）；② 敏感工具（`refund.create`）缺必填参数即回落，**绝不自动替买家发起退款**（恒送审口径不变）；③ 一键回退开关 `AGENT_CHAT_ORCHESTRATE`（进 `_HOT_FIELDS`）+ 连接器未装载/内部异常两条路都回落直连检索并记 `agent.degraded`，**绝不 500、绝不把「工具没装上」放大成 `2001`**。验证：`py_compile` + `ruff check .` 0 error + `mypy app` 0 issue（78 files）+ `pytest` **142 项 0 失败 0 错误**、覆盖率 **85.79%**（阈值 70；`test_agent_runtime.py` 13→17 项，新增主链走编排/说明透出/双回退/业务事实注入四类）；API 规范 §4.2/§4.12/§5 同改。
> v0.2.6：B 步 Agent Runtime + 工具注册中心落地（FR-3/FR-5，原为「services 直调形态」）——新建 `backend/app/modules/agent/`：`contracts`（状态机白名单 `IDLE→PLANNING→ACTING→OBSERVING→REFLECTING→DONE` + 三分支、ToolSpec、无三方依赖的 JSON Schema 子集校验，全部中文报错）/ `registry`（注册中心，超时重试口径单源解析）/ `policy`（Scope 硬拦 + 敏感动作恒判送审）/ `executor`（30s 超时、幂等工具退避重试 3 次、非幂等恒 1 次、连续失败熔断 60s、全分支写 `tool_calls` 审计；**业务拒绝不重试不计熔断**，超时 4002 / 依赖失败 4008 分开）/ `connectors`（附录 A 其余 6 个连接器，Scope 逐条对齐）/ `runtime`（规则规划 → 逐步执行 → `tasks.checkpoint` 每步落盘提交 → `resume` 续跑不重放）/ `bootstrap`（启动幂等注册，失败只告警不阻断）。接线：`main.py::lifespan` 启动注册 + 新增 `endpoints/agent.py` 6 条端点（§4.12）。**恒送审口径**：给 `order_service.create_aftersale` 加 `force_approval` 开关，AI 发起的退款不看金额一律进审批（人工后台仍走阈值规则，存量行为不变）。新增错误码 4005-4009 落 4xxx 段。验证：`py_compile` 全过、`ruff check app tests` 0 error、`app.openapi()` 78 paths（+6 条 `/agent/*`，含 `tools`/`invoke`/`run`/`runtime`/`resume`）；`test_agent_runtime.py` 13 项**实跑全绿**（注册中心/Schema 校验/策略/超时重试熔断/恒送审/编排检查点/审批闸门；本轮修 3 处测试口径：超长用例数据未超 `maxLength`、`GET /approvals` 需带 `page` 才返回分页对象、参数校验在策略之后故 1001 分支须换有 Scope 的用户）；后端全量 `pytest tests` **138 项 0 失败 0 错误**。
> v0.2.5：坐席工作台前后端真联调（FR-7 转人工闭环）——根因是工作台三栏还是演示骨架（队列客户端假数据、无流转动作、无备注/用量），后端 `handoff_status` 与 `session_notes` 已就绪但前端未接线。后端：新增 8 条 `/workbench/*` 端点（队列服务端分页/状态筛选/关键字、handoff/claim/transfer/resolve/reply/notes/trace）+ `sessions.handoff_status`/`session_notes` 迁移与索引 + chat 侧 `mark_pending_if_idle` 自动挂起钩子（need_human/拒答→pending，不抢已认领）；前端：`api/workbench.ts` 重建 8 接口 + 4 个组合式（`useWorkbenchQueue` 服务端分页 20、`useWorkbenchTrace`、`useWorkbenchNotes`、`useWorkbenchSide` 含上下文用量）+ 4 个组件（`WorkbenchQueue`/`WorkbenchChat`/`WorkbenchSide`/`WorkbenchNotes`，拆组件顺带消 `max-lines` 红灯）+ `WorkbenchView` 三栏编排（按 `handoff_status` 切动作、输入区三态锁定、代回落 agent 行、AI 接待走流式、队列/Trace/备注三路失败各自降级）。验证：`pytest tests/test_workbench.py` 4 项 + `smoke_workbench.py` 15/15（真链路）+ `pnpm lint`/`typecheck`/`build` 全过 + vitest 13 文件 64 项；`design.pen` 画板 `-/workbench` 同改三栏（队列页签与分页、流转动作、Trace/用量/备注卡），`check_design.py` 18/18 ERROR 0/WARN 0；API 规范补 §4.11（openapi 自查 72 paths）、页面设计 §1 路由表 + §3.2 + §7 同步。
> v0.2.4：审批中心前后端联调——根因是列表无服务端分页（前端客户端裁剪）+ 客服可见性缺口（列表权限缺 `cs`）+ 批驳无审计。后端 `GET /approvals` 加 `page/size/action/keyword` 服务端分页（`list_page` + 总数，存量 `limit` 数组兼容）+ 列表放行 `cs` + `decide` 记 `approval.approve|reject` 审计 + 批准透传 `reason`；前端 `approvals.ts` 切服务端分页 20 + `useApproval` 抽确认框与幂等（批量逐条）+ `ApprovalCenterView` 重写筛选/批量/金额/证据图/等待耗时 + `useApproval.test.ts` 3 项 + mock 口径对齐后端四类 action。验证：pytest 12 + `smoke_approvals.py` 13/13 + `pnpm lint`/`typecheck`/`build` + vitest 3 + `check_design --strict-color` 全过（mypy 本机缺走 CI；pytest 曾因残留 pytest 进程锁 `.pytest-tmp` 全 E，已清进程重跑全过）。openapi 自查：72 paths，`GET /approvals` 含 `action/keyword/limit/page/size/status`。
> v0.2.3：`/chat` 真流式——根因是后端全文生成完才 burst 切片（首字等全文）+ 前端气泡 `white-space` 默认吞换行。后端 `llm_service.acomplete_stream`（OpenAI 兼容 SSE 解析）+ `chat_service.stream_text_turn`（与 `run_text_turn` 同语义同落库，增量外吐+终态 result 协议，流中断后缀明示照常 done）+ 端点切流（validating 后移，帧形/id 幂等不变）；前端气泡 `pre-wrap`（Chat/Workbench/Widget 等 6 处）+ `useStickToBottom` 粘底跟随（翻历史不抢滚动，vitest 3 项）。验证：pytest 12（新 5）+ test_api 2 + eslint 0 + design 18/18 + detect 0；typecheck 2 项（审批金额/等待 helper）系同工作区在途改动，非本次引入；mypy 本机缺走 CI。
> v0.2.2：全站字体清晰度重设计——根因是浅气泡套深卡深字糊（Vision/Citation 在白泡内用深底 `--reai-card`，截图红框处）+ 无字体栈走浏览器默认 + 10px 微文案发虚 + 顶栏 75% 白发灰。改动：tokens 加苹方/雅黑字体栈 + 字阶（正文 14/1.6 起跳、微文案最小 11）+ 浅底专用色（`--reai-text-on-light-muted/faint`、`--reai-notice-deep`、`--reai-surface-light`、`--reai-text-soft`）与顶栏提亮 88%；Vision/Citation 改浅底语义卡深字；Chat/会话列表/顶栏/工作台三栏字阶对比度对齐；`design.pen` 同改 `/chat`+`/workbench`（10px→11 起跳、正文 13→14、微文案提亮、商品卡深底糊块改白卡）。验证：`pnpm lint`/`typecheck`/`build` + `check_design --strict-color` 全过（`check_arch` 2 项后端超长系工作区存量，非本次引入）。
> v0.2.1：前端体检问题清零（1→2→3→4）——会话分页合规（sizes+size 交 store+单测）；工作台队列抽 `WorkbenchQueue` 组件（消 max-lines 红灯，附空态）+ 右栏订单 props 化挂演示标 + 本轮 Trace 概览（`useWorkbenchSide` 取流式真 trace_id+单测）；裸 el-button/el-input 收敛 Ai 系 16 处；Chat 引导卡经核 v0.2.0 已有等效实现不修；修 `toMessage` 缺 `followups` 类型红灯。lint/typecheck/vitest 36/build/design 18/18 全过。
> v0.2.0：`/chat` 新会话预设 + 追问延伸——空态欢迎卡 6 条预设一键发送，最后一条回复下 3 条猜你想问（`useSuggestedQuestions` 规则匹配，历史恢复补挂）；新增 `ChatSuggestions/ChatMessage` 组件（ChatView 拆到 lint 400 行内）+ vitest 4 项；画板 `ch_empty/ch_quick/ch_followup` 同改，design-guard strict 过；页面设计 §3.1/§7 同步。
> v0.1.9：`/impeccable audit` 修复三件套——403/500 渐变字改实色（detect 2→0）、6 处硬编码收敛 token（对齐画板 `#FB9236` 橙/VoicePanel 运行时读 `--reai-accent`）、审批/营销/售后/评价（双表）补客户端分页 20（服务端分页待接）、语音图片按钮补 `aria-label` 16 处；`DESIGN.md` + `.impeccable/design.json` + `PRODUCT.md` 扫描补齐。design-guard `--strict-color` PASS；lint 11/12 干净（ChatView max-lines 系工作区存量 +123 行所致）+ typecheck 2 项系同工作区在途改动（followups/WorkbenchSide props）所致，均非本次引入。
> v0.1.8：`/impeccable clarify` 三页文案——物流页示例占位+列表生命周期说明+异常类型映射表（含 confirm 带类型名）；任务页补指定渠道输入（原选渠道无处填）、类型列中文化（新增 `TASK_TYPE_TAG`）、失败指引指向刷新按钮；看板去内部黑话（P2/英文标题）。lint/typecheck/detect 全过。
> v0.1.7：`/impeccable animate` 三页克制动效——任务页状态变更行闪光（轮询 status 变化/新建提交）、物流页新行进入高亮、看板指标值变化闪光，全带 `prefers-reduced-motion` 降级；critique 拦掉进度条 `width` 过渡（layout thrash，已 revert）。lint/typecheck/detect 全过。
> v0.1.6：按画板重生成差距 3 页——物流页补全（查询沉淀列表+异常登记+差评SLA摘要+分页20，回复流仍归评价页）；任务页切服务端真实列表（状态筛选+分页+日志/下载/重试，顺手正名过期“无列表接口”备注，画板同改）；看板加慢Trace Top5跳转。门禁 lint/typecheck/build + design 18/18 全过。
> v0.1.5：设计先行画板全覆盖 18/18（第 2 批 `/goods` `/orders` `/inventory` 画板已在树上；第 4 批补齐 `/purchase` `/finance` `/risk` 待实现页画板），棘轮 `ROUTE_COVERAGE_MIN` 12→18；三页补最小可用 views+路由（演示数据+分页20，接口与审批流随后补）。
> v0.1.4：设计先行第三批画板补齐（`/studio` `/admin` `/marketing` `/logistics` `/login`），`design.pen` 覆盖 7/18→12/18，棘轮 `ROUTE_COVERAGE_MIN` 7→12；`StudioView` 由占位补齐三窗格（Prompt版本/工具试调/评测禁发布，演示数据+分页20）。
> v0.1.3：设计先行第一批画板补齐（`/chat` `/tasks` `/dashboard`），`design.pen` 覆盖 7/18，棘轮 `ROUTE_COVERAGE_MIN` 3→7，命名债上限收紧到 0；新增 G 步「画板补齐」。
> v0.1.2：历史对话三层闭环（Session→Message→Context：窗口 20 轮 + 预算 8000 token + 摘要压缩 + PII 清洗，多模态附件转写进历史）；v0.1.1：A 步图片链路 MVP 落地 + 语音 stub 可用。

先说结论不变：**不按页面顺序做，按数据流顺序做；先竖着打通一条线，再横着铺开所有面。** 区别是——主线（登录 → SSE 对话 → RAG 引用 → 落库）已经打通，现在的任务是**补 P0 的纵向缺口（多模态、Runtime、转人工），再横向铺 P1/P2**。

---

## 一、现状盘点（已完成的步骤）

| 原步骤                    | 状态 | 落地情况                                                                                                                                                                               |
| ------------------------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 第 0 步 对齐契约          | ✅   | `API接口与SSE事件协议规范.md` 为唯一契约源；CI `docs-guard` 硬门禁强制接口与文档同改                                                                                                   |
| 第 1 步 地基三件套        | ✅   | Alembic 迁移（v0.3.3 迁移链 4→1 squash 基线 + CI upgrade/check 双闸）+ `seed.py`；JWT（tenant/user/roles）+ `governance.access_context()` + `require_perm()` + 前端 `v-permission`；信封 `ok()/fail()` + 1xxx–5xxx 错误码 + 中央 `handle401()` |
| 第 2 步 文本对话 SSE 主线 | ✅   | `POST /api/v1/chat/stream`，事件 `source/phase/message/done`（done 含 references/guard/faithfulness/trace_id/context）；消息会话落库；断线幂等重放（client_msg_id）进行中                      |
| 历史对话三层闭环        | ✅   | Session（分页/改名/删除级联/活跃排序）→ Message（多模态附件/引用/trace 持久化）→ Context（窗口 20 轮 + 预算 8000 + 规则摘要 + PII 清洗进 LLM，`GET context` 同源可查）。验证：`test_sessions_history.py` 8 项 + `smoke_sessions.py` 9/9 + 文本冒烟 12/12 回归 |
| 第 3 步 RAG 真检索        | 🔶   | 已通：文档上传/解析/reindex、向量+关键词检索、租户+生效期过滤、无据拒答 2001、企业知识库种子 29 篇（`docs/knowledge-base/`）。缺口：BM25 + rerank 混合检索（FRD 列为 P1）              |
| 第 5 步 审批（部分）      | 🔶   | `approvals` 审批中心前后端联调已通（v0.2.4）：服务端分页 20 + 状态/类型/关键字筛选 + 批量批 + 改参批准 + 驳回必填留痕 + 批驳记审计；改价/盘点/补货/超阈值退款闭环（`decide` 先执行先生效动作再改状态，同事务回滚；`smoke_approvals.py` 13/13）。缺口：工具注册中心与其余 6 个连接器（见下表 B） |
| 第 6 步 转人工闭环（FR-7）| 🔶   | `handoff_status` 状态机（`none→pending→handling→resolved`，已解决可被买家再 `handoff` 重开）+ 10 条 `/workbench/*` 端点（队列服务端分页 20/状态筛选/关键字、认领/转接/解决/代回/内部备注/与买家侧同源 Trace/触发规则表/运营指标）+ 自动挂起统一经规则表（`handoff_rules` 9 条判据 → `handoff_service.auto_handoff` 挂载点：命中且会话空闲才 `pending`，坐席代回清零连续计数）+ 抢接原子化（claim() 条件 UPDATE + rowcount，两坐席同抢只一人赢）+ 接起率可度量（`handoff.claim` 事件 → `GET /workbench/metrics` 30s 接起率/队列存量）+ 前端三栏真联调（`WorkbenchQueue`/`WorkbenchChat`/`WorkbenchSide`/`WorkbenchNotes` 四组件 + 四组合式；按流转态切动作、输入区三态锁定、代回落 agent 行、三路失败各自降级）。验证：`test_workbench.py` 8 项 + `test_handoff_rules.py` 17 项 + `test_observability.py` 5 项 + `smoke_workbench.py` 17/17 + `smoke_chat.py` 喊人工端到端。缺口：技能组与负载均衡、质检打分/绩效 |
| B 步 Agent Runtime + 工具注册中心（FR-3/FR-5） | 🔶 | v0.2.6 落地：`backend/app/modules/agent/`（状态机 + checkpoint 恢复 / 工具注册中心 / Scope 策略 / 超时重试熔断执行器 / 6 个附录 A 连接器 / 规则规划编排）+ `main.py` 启动注册 + `endpoints/agent.py` 6 条端点；`refund.create` 恒送审（`create_aftersale(force_approval=True)`），审批未决 `resume` 直接 4003 不放行。**v0.2.7 接线**：对话主链检索段改走 `runtime.orchestrate()`（`AGENT_CHAT_ORCHESTRATE` 可一键回退），`ok()`/`done` 透出 `tool_calls[] + orchestration{notes[]}`。验证：`py_compile` + `ruff check .` 0 error + `mypy app` 0 issue + pytest 142 项 0 失败（覆盖率 85.79%）+ openapi 78 paths。缺口：编排层「规则机器人」降级话术（FR-5 三级容错第三级） |
| 第 7 步 B 端一期（部分）  | 🔶   | 商品/SKU、库存、订单履约、物流、营销发券、评价、工单、管理中心已落地（14 个前端域 + 16 组 endpoints）。缺口：采购审批、资金对账（FRD 列为 B 端二期/P2）                                |
| A 步图片链路 MVP          | ✅   | `POST /multimodal/images`（存→预检→VLM，一次回检测卡）→ inspections 透传 `chat/stream`（`inspecting` 相 + done.vision/need_human）→ 前端检测卡/预览标注/转人工；语音转写+TTS 配置可用。验证：`test_multimodal.py` 12 项 + `smoke_multimodal.py` 10/10 + `eval_vision.py` 管线 PASS（合成集，真集待采） |
| 部署                      | ✅   | compose + GHCR 镜像（CI 自动发布）+ `deploy.ps1` 端到端验证 + ArgoCD 清单就绪（dev `automated+prune+selfHeal` / prod 无 automated 恒手动 Sync，`cd-update.yml` 已联动部署仓 dev 叠加层）；v0.1.0 已发布。**集群侧待接入**：部署仓 `reai-deploy` 未建（全仓无 kustomize 清单）、无集群/ArgoCD、`vars.DEPLOY_REPO`/`secrets.DEPLOY_PAT` 未配（缺配时 cd-update warn 跳过，安全） |
| 设计先行（`design.pen`）   | 🔶   | 门禁已落地：`scripts/check_design.py` + CI `design-guard`（命名/覆盖棘轮/同改/色值）。画板 **18/18 全覆盖**（第 1-4 批齐），全部深色 token 口径、ERROR 0/WARN 0（`--strict-color` 过）。剩余债：`/purchase` `/finance` `/risk` 三页仅演示数据，接口与审批流待补（见 G 步） |

生成模型现状：本地 Ollama `qwen2.5:0.5b`（ADR-0001），唯一出口 `llm_service.py`，不可用自动降级模板摘要（不 500）。

---

## 二、剩余工序（按依赖顺序）

### A. 多模态接入（FR-1，P0 缺口，1-2 周）⭐ 最优先 —— 图片链路 MVP 已落地，余真机集 + 网关

文本线通了，按 FRD §6 P1 主流程补「图文售后」——这是系统的招牌场景：

- 图片 ✅：上传 → 对象存储 → NSFW/PII 预检 → VLM 瑕疵检测（`image.inspect`）→ 检测结果拼进 LLM 上下文 → 置信 <0.6 转人工（`vision_service` 单出口 + `chat/stream` 加法字段向后兼容）。
- 语音 🔶：ASR 转写 + 置信度 stub 可用；TTS 开关/音色；波形播放（SenseVoice/edge-tts 网关待接，`Settings` 占位名已留）。
- 前端 ✅：图片预览/缩放/标注（`ImagePreviewDialog`）+ 检测卡片（`VisionResultCard`），语音文字对照（`VoicePanel`）。
- 模型接入沿用 `llm_service` 单出口模式，VLM/ASR 已进 `Settings`，无散落硬编码。

**验收（FRD §7）：** 图片链路 `test_multimodal.py` 12 项 + `smoke_multimodal.py` 10/10 + `eval_vision.py` 管线 PASS（合成集 8 类×20 张 F1=1.0；真机 200 张/类集待采）；传图出检测结果、低置信转人工、三模态 E2E 通、弱网重连（`useAgentStream` 同 key 3 次退避）过。剩余：真机集复测 + Qwen3-VL 传图/ASR 网关替换 stub（签名不变）。

### B. Agent Runtime 状态机 + 工具注册中心（FR-3/FR-5）✅ v0.2.6 已落地

编排内核已建成（`backend/app/modules/agent/`）：

- ✅ 状态机 `IDLE→PLANNING→ACTING→OBSERVING→REFLECTING→DONE` + 三分支 `WAITING_APPROVAL / WAITING_HUMAN / FAILED`，流转白名单集中在 `contracts.TRANSITIONS`，非法流转 4009；每步 checkpoint 落 `tasks.checkpoint` 并提交，`resume` 从断点续跑不重放已完成步。
- ✅ 工具注册中心：JSON Schema 子集（无三方依赖，中文报错）+ Scope 鉴权（4006）+ 幂等键 + 超时 30s（4002）+ 幂等工具退避重试 3 次（非幂等恒 1 次）+ 熔断 60s（4007）+ 全分支 `tool_calls` 审计。
- ✅ 补齐附录 A 其余 6 连接器（Scope 逐条对齐）：`order.query / logistics.query / stock.query / coupon.query / kb.retrieve / refund.create`；`refund.create` 恒进审批（`force_approval=True`，账不动，批准后才 `apply_refund`）。
- ✅ 端点与接线：`endpoints/agent.py` 6 条（`GET /tools`、`GET /tools/{name}`、`POST /tools/{name}/invoke`、`POST /run`、`GET|POST /runtime/{task_id}[/resume]`）+ `main.py::lifespan` 启动幂等注册（失败只告警不阻断）。

- ~~① 把编排接进 `/chat` 主链~~（v0.2.7 已完成）：`runtime.orchestrate()` 成为对话轮次的编排入口（不建 `tasks` 行、不落检查点），检索段统一由 `kb.retrieve` 连接器出引用，业务工具事实以 `【业务查询】` 块注入提示词；`AGENT_CHAT_ORCHESTRATE=false` 一键回退直调，连接器未装载/内部异常同样回落，编排永远不阻断对话。

**余（下一轮）：** ~~① 把编排接进 `/chat` 主链~~；② 规划器由规则版升级为 LLM function-call（保留规则兜底）；③ 熔断/RAG 拒答在编排层切「规则机器人」降级话术（FR-5 三级容错第三级）。

**验收：** 敏感操作审批率 100%（审计扫漏 + 越权渗透 0 泄漏）；ToolCallCard 透明展示，审批中心能批/拒/改参。

### C. 转人工 + 坐席完整台（FR-7，1 周）

- ~~触发规则~~（v0.2.9 已抽表）：9 条判据（喊人工 / 情绪激烈 / 敏感送审 / 图检低置信 / 无据拒答 / 编排空手 / 工具空手 / 连续 3 次未解决 / 模型连续 3 次降级）收进 `services/handoff_rules.py`，全站唯一判据出口 `handoff_service.auto_handoff()`，词表与阈值在 `Settings.HANDOFF_*`（可热更、0=关规则），坐席可读 `GET /workbench/handoff-rules` 核对「为什么进队列」；新连接器接挂起只需加一条规则 + 阈值，不再改业务分支。~~抢接~~（v0.2.11 已修：claim() 条件 UPDATE + rowcount 原子比较交换，两坐席同抢只有一人赢，输家拿 1001「已被 X 接管」，重复认领幂等）。**余** 技能组与负载均衡、质检打分、绩效。
- ~~坐席工作台~~（v0.2.5 已通）：✅ 待接队列（服务端分页 20 + 状态页签 + 关键字）、✅ 认领/转接/解决状态机（`none→pending→handling→resolved`，可重开）、✅ 完整 Trace 查看（与买家侧同源）、✅ 内部备注（买家不可见）、✅ 代回落 agent 行；**余** 技能组、负载均衡、质检打分、绩效。
- 口径统一：代码用 `sessions.handoff_status ∈ {none,pending,handling,resolved}`（API 规范 §4.11），与本文原 `WAITING_HUMAN` 为同一状态机的不同写名，实现以代码为准。

**验收：** 转人工 30s 内接起率 ≥95%；坐席能看到全链路 Trace 并接管。

### D. 生产存储切换（附录 B/E/F，1 周）

现在 dev/compose 都是 SQLite + 本地文件，生产基线未接：

- PostgreSQL（附录 B/E/F DDL + Alembic，先加字段后发代码再删旧字段）、~~Redis 频控~~（v0.3.3 地基：`core/cache.py` 适配层 + 对话限流落地，Redis 不可用自动进程内回退；**余**库存预占原子扣减、大屏缓存接线）、MinIO/S3（图片与文档原件）。
- 库存口径：可用 = 在库 − 预占 − 锁定，Redis 原子操作保证零超卖。

**验收：** 并发下单超卖率 = 0；库存账实一致 ≥99.5%；跨租户检索/查询 0 串数。

### E. 评估与验收流水线（FR-9，持续）

- ~~可观测地基~~（v0.3.1 已落）：`core/observability.py::record()` 从空占位改为双出口——进程内计数器/滑窗（重启清零）+ 守护线程批量落 JSONL `data/observability/events-YYYYMMDD.jsonl`（重启不丢历史，`OBSERVABILITY_ENABLED/DIR/ANSWER_TARGET_SECONDS` 进 Settings）；`chat/llm/agent.tool/handoff/handoff.claim/rag.*` 全链路留痕；`GET /workbench/metrics` 透出 30s 接起率（FR-7 验收度量口）+ LLM/工具成功率 + 队列存量。**余**：Prometheus/Langfuse 网关（替换落盘出口即可，事件口径不变）、日志字段强制带 trace/tenant/user。
- ~~前端测试分层~~（v0.3.2 已起步）：组件层 vitest（工作台三件套 19 项）+ 跨页链路 Playwright 冒烟 2 项（mock 契约、不依赖后端）；**余**：CI 接入 e2e（浏览器缓存）、SSE 流式真链路 e2e、其余高频页（审批/知识库）组件测试。
- ~~黄金集 P0 200 条~~（v0.3.3：`seed/golden_set.tsv` 200 条 + `eval_golden.py` 检索链路 + CI 棘轮门已落；**余**扩到 500 条、LLM-as-judge + 人工抽检）；自动解决率 ≥80%、grounded ≥95%、幻觉 ≤2%。
- 1000 并发混合压测（文 70%/图 20%/音 10%），首字 P95 <2s；成本归因误差 <5%。
- ArgoCD 集群侧接入（回滚演示的前置，按序）：① 建部署仓 `reai-deploy`：`base/` + `envs/{dev,prod}/kustomization.yaml`（镜像 GHCR，`newTag` 短 sha，布局约定见 `cd-update.yml` 头注释——缺 `envs/dev/kustomization.yaml` 时该工作流会硬报错）；② 集群装 ArgoCD，`kubectl apply` `deploy/argocd/` 两份 Application（模板 repoURL 已指向 ①）；③ 应用仓配 `vars.DEPLOY_REPO` + `secrets.DEPLOY_PAT`（仅 CI bot 写部署仓，权限隔离见 ADR-0002）；④ prod 发布审批 + 回滚 RTO <5min 演练（ADR-0002 验收未勾项）。
- 跑 FRD 附录 C 发布门禁 Checklist（含 SBOM、扫描、ArgoCD 回滚 <5min 演示）。

### F. P1/P2 横向铺开（各 2-4 周）

- P1：以图搜款、OCR、混合检索 rerank、计费、大促预案、A/B + 影子。
- P2：B 端二期（采购审批→到货→质检→入库、资金对账）、经营大屏（准实时 <5min、PII 脱敏、异常下钻）、风控/绩效/多平台、多语言、微前端拆分。

### G. 画板补齐（design-first，随页推进）

`design.pen` 是 UI 唯一事实源，终态必须**一页一画板全覆盖**（分母 = `页面设计.md` §1 路由表 18 条）。当前 **18/18 全覆盖**（第 2 批画板已在树上，第 4 批补齐最后 3 张）：

| 批次 | 画板 | 为什么排这里 |
| --- | --- | --- |
| ✅ 第 1 批 | `/chat` `/tasks` `/dashboard` | 代码已跑通但视觉无事实源，改动最频繁 |
| ✅ 第 2 批 | `/goods` `/orders` `/inventory` | B 端一期主数据三页，`/goods` 已接真实接口（画板已在树上，`goodsRoot/ordersRoot/inventoryRoot`） |
| ✅ 第 3 批 | `/studio` `/admin` `/marketing` `/logistics` `/login` | 第 3 批先行补齐（含 `StudioView` 三窗格代码对齐）；`/admin` 权限差异已画全 |
| ✅ 第 4 批 | `/purchase` `/finance` `/risk` | **待实现页**（无 view/接口）：画板先行已出，最小可用 views+路由已补（演示数据+分页20），接口与审批流随后补 |

- **G 步转常态维护**（不再有「待出画板」）：① 棘轮 `ROUTE_COVERAGE_MIN=18` 硬拦覆盖回退，少一张画板即 CI `design-guard` 红；② 改 `frontend/src/{views,components}` 视觉必须同 commit 改对应画板。18 张顶层并列，x = 0 → 22780（间距 1340，均 1280×800）。
- 待决口径债：`/reviews` `/aftersales` `/widget` 代码有、§1 表没有；`/403` `/404` `/500` 建议豁免到组件级 —— 出图前先把 §1 表与 `router/index.ts` 对齐，否则分母失真。
- 每张画板必须含**状态完整性**：空态 / 加载 / 错误、分页（默认 20，可切 10/20/50/100）、权限差异、破坏性操作二次确认、多模态溯源（引用卡 + trace_id）。

---

## 三、如果本周只做一件事

~~**把 B 步的编排接进 `/chat` 主链**~~ ✅ v0.2.7 已完成（`runtime.orchestrate()` + `AGENT_CHAT_ORCHESTRATE` 一键回退 + `tool_calls/orchestration` 透出）。轨道已合流：买家对话与 `/agent/run` 现在共用同一套规划/工具执行/审计口径。

**~~本周新的焦点：C 步「转人工触发规则表」~~ ✅ v0.2.9 已完成**（`handoff_rules` 9 条判据 + `handoff_service.auto_handoff` 统一挂载点 + `GET /workbench/handoff-rules` 只读视图；喊人工 / 情绪 / 送审 / 低置信 / 无据 / 空手 / 连续未解决 / 连续降级走同一份判据）。

**本周新的焦点：C 步余项「技能组 + 负载均衡」——抢接竞态已修（v0.2.11：claim() 原子比较交换），把待接队列从「谁先看谁接」升级为按技能组路由、按在手会话数分配，兑现验收里的「30s 内接起率 ≥95%」。**

理由：规则表解决了「为什么转人工」，但「转给谁」还是全租户可见的裸队列——`claim` 只看状态不看人（谁手快谁接），会话一多就变成抢单，30s 接起率无从保证。技能组（按品类/渠道打标）+ 负载（在手会话数、最近接起时长）才是接起率的真正抓手；落点优先复用 `sessions.assignee/handoff_status` 与 `workbench_service` 现有流转动作，不另造一套状态。

**核心原则一句：主线已竖通、编排内核已接进主链，接下来按 C（触发规则表 → 技能组/负载）→ D（生产存储）补齐 P0 纵向缺口；E 的评估流水线并行推进，F 再横向铺面。**
