# 执行步骤 —— 从 FRD 到系统的实施路线

> 对齐文档：`多模态智能电商客服系统需求文档-FRDv2.md` §6 核心业务流程 / §7 验收标准 / §8 分期路线
> 本版日期：2026-09-13（v0.1.0）。上一版写于「前端页面已搭好、后端未开工」阶段；**现在 P0 文本主线与 B 端一期代码已落地并跑通容器化部署**，本文改为「现状盘点 + 剩余工序」。

先说结论不变：**不按页面顺序做，按数据流顺序做；先竖着打通一条线，再横着铺开所有面。** 区别是——主线（登录 → SSE 对话 → RAG 引用 → 落库）已经打通，现在的任务是**补 P0 的纵向缺口（多模态、Runtime、转人工），再横向铺 P1/P2**。

---

## 一、现状盘点（已完成的步骤）

| 原步骤                    | 状态 | 落地情况                                                                                                                                                                               |
| ------------------------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 第 0 步 对齐契约          | ✅   | `API接口与SSE事件协议规范.md` 为唯一契约源；CI `docs-guard` 硬门禁强制接口与文档同改                                                                                                   |
| 第 1 步 地基三件套        | ✅   | Alembic 迁移 + `seed.py`；JWT（tenant/user/roles）+ `governance.access_context()` + `require_perm()` + 前端 `v-permission`；信封 `ok()/fail()` + 1xxx–5xxx 错误码 + 中央 `handle401()` |
| 第 2 步 文本对话 SSE 主线 | ✅   | `POST /api/v1/chat/stream`，事件 `source/phase/message/done`（done 含 references/guard/faithfulness/trace_id）；消息会话落库；断线幂等重放（client_msg_id）进行中                      |
| 第 3 步 RAG 真检索        | 🔶   | 已通：文档上传/解析/reindex、向量+关键词检索、租户+生效期过滤、无据拒答 2001、企业知识库种子 29 篇（`docs/knowledge-base/`）。缺口：BM25 + rerank 混合检索（FRD 列为 P1）              |
| 第 5 步 审批（部分）      | 🔶   | `approvals` 模块已实现退款审批闭环（`order.refund` → WAITING → approve/reject → `apply_refund` → 审计）。缺口：工具注册中心与其余 6 个连接器（见下表 B）                               |
| 第 7 步 B 端一期（部分）  | 🔶   | 商品/SKU、库存、订单履约、物流、营销发券、评价、工单、管理中心已落地（14 个前端域 + 16 组 endpoints）。缺口：采购审批、资金对账（FRD 列为 B 端二期/P2）                                |
| 部署                      | ✅   | compose + GHCR 镜像（CI 自动发布）+ `deploy.ps1` 端到端验证 + ArgoCD 清单（dev 自动/prod 手动）；v0.1.0 已发布                                                                         |

生成模型现状：本地 Ollama `qwen2.5:0.5b`（ADR-0001），唯一出口 `llm_service.py`，不可用自动降级模板摘要（不 500）。

---

## 二、剩余工序（按依赖顺序）

### A. 多模态接入（FR-1，P0 缺口，1-2 周）⭐ 最优先

文本线通了，按 FRD §6 P1 主流程补「图文售后」——这是系统的招牌场景：

- 图片：上传 → 对象存储 → NSFW/PII 预检 → VLM 瑕疵检测（`image.inspect`）→ 检测结果拼进 LLM 上下文 → 置信 <0.6 转人工。
- 语音：ASR 转写 + 置信度；TTS 开关/音色；波形播放。
- 前端：图片预览/缩放/标注 + 检测卡片，语音文字对照。
- 模型接入沿用 `llm_service` 单出口模式，新增 VLM/ASR 配置进 `Settings`，禁止散落硬编码。

**验收（FRD §7）：** 瑕疵分类 F1 ≥0.85 / 召回 ≥0.9（200 张/类测试集）；传图能出检测结果、低置信能转人工；三模态 E2E 通、弱网重连过。

### B. Agent Runtime 状态机 + 工具注册中心（FR-3/FR-5，1-2 周）

当前对话是 services 直调形态，缺 FRD 要求的编排内核：

- `backend/app/modules/agent/`：状态机 `IDLE→PLANNING→ACTING→WAITING_APPROVAL/WAITING_HUMAN→DONE`，checkpoint 可恢复（tasks 表已有 `checkpoint` 字段）。
- 工具注册中心：JSON Schema + Scope + 幂等键 + 超时 30s 重试 3 次（仅幂等方法）+ 熔断。
- 补齐附录 A 其余 6 连接器：`order.query / logistics.query / stock.query / coupon.query / kb.retrieve`（image.inspect 随 A 步）；`refund.create` 恒进 WAITING_APPROVAL（审批侧已就绪）。

**验收：** 敏感操作审批率 100%（审计扫漏 + 越权渗透 0 泄漏）；ToolCallCard 透明展示，审批中心能批/拒/改参。

### C. 转人工 + 坐席完整台（FR-7，1 周）

- 触发规则：喊人工 / 3 次不懂 / 愤怒 / VLM 低置信 / 敏感退款 / 模型连续失败 / 无据拒答 → `WAITING_HUMAN`。
- 坐席工作台（页面骨架已有 `views/workbench/`）：待接队列、技能组、负载均衡、完整 Trace 查看、内部备注、抢接/转接、质检打分、绩效。

**验收：** 转人工 30s 内接起率 ≥95%；坐席能看到全链路 Trace 并接管。

### D. 生产存储切换（附录 B/E/F，1 周）

现在 dev/compose 都是 SQLite + 本地文件，生产基线未接：

- PostgreSQL（附录 B/E/F DDL + Alembic，先加字段后发代码再删旧字段）、Redis（库存预占原子扣减、大屏缓存、频控）、MinIO/S3（图片与文档原件）。
- 库存口径：可用 = 在库 − 预占 − 锁定，Redis 原子操作保证零超卖。

**验收：** 并发下单超卖率 = 0；库存账实一致 ≥99.5%；跨租户检索/查询 0 串数。

### E. 评估与验收流水线（FR-9，持续）

- 黄金集 ≥500 条（P0 先 200），LLM-as-judge + 人工抽检；自动解决率 ≥80%、grounded ≥95%、幻觉 ≤2%。
- 1000 并发混合压测（文 70%/图 20%/音 10%），首字 P95 <2s；成本归因误差 <5%。
- 跑 FRD 附录 C 发布门禁 Checklist（含 SBOM、扫描、ArgoCD 回滚 <5min 演示）。

### F. P1/P2 横向铺开（各 2-4 周）

- P1：以图搜款、OCR、混合检索 rerank、计费、大促预案、A/B + 影子。
- P2：B 端二期（采购审批→到货→质检→入库、资金对账）、经营大屏（准实时 <5min、PII 脱敏、异常下钻）、风控/绩效/多平台、多语言、微前端拆分。

---

## 三、如果本周只做一件事

**做 A 步的图片链路：传一张瑕疵图，走完「上传 → VLM 检测 → 拼上下文 → 流式回答带引用 → 低置信转人工」。**

理由：文本主线已可演示；图片是 FRD §6 P1 主流程的灵魂，也是当前与验收标准差距最大的一块。它同时倒逼 B 步的 `image.inspect` 工具化和 C 步的转人工触发，是剩余工序的依赖根。

**核心原则一句：主线已竖通，现在按 A→B→C→D 把 P0 的纵向缺口补齐，E 的评估流水线并行推进，F 再横向铺面。**
