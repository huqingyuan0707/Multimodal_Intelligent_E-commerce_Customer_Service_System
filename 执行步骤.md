先说结论：**不要按页面顺序做，要按数据流顺序做。** 前端页面已经搭好，现在最该做的不是"把每个页面的按钮都接上"，而是**先打通一条从浏览器到数据库的垂直主线**，让一个真实用户能完成一次真实对话。这条线通了，其余页面基本都是复制同一种模式。

下面按依赖关系排，从最该开始的地方讲。

---

## 第 0 步：对齐契约（1-2 天，必须先做）

前端页面已完成，说明 mock 数据已经跑起来了。现在第一步不是写业务，而是**把 mock 逐一替换成真接口之前，先确认契约一致**。

要做的事：
- 对照 FRD 附录 A/B/D，确认每个页面的 API 路径、入参、出参、错误码。
- 确认统一信封：`ok(data,msg)` / `fail(ErrorCode, 中文msg, http)`。
- 确认 SSE 事件：`source / phase / message / done`，`done` 必须含 `references + guard + faithfulness + trace_id`。
- 确认数据模型：附录 B、E、F 的表结构是否和前端字段对得上。
- 在前端把所有 `mock/` 调用点列出来，标注对应哪个真实接口。

**验收：** 有一份"页面 → 接口 → 数据表"的映射表，没有对不上的字段。

---

## 第 1 步：地基三件套（3-5 天）

这是所有页面的前置，做完之后每个页面才能开始接真数据。

1. **数据库 + 迁移**
   - 建附录 B 的核心表：tenants / users / sessions / messages / tasks / approvals / tool_calls / kb_docs / audit_logs / cost_records。
   - 用 Alembic 管理迁移，先跑通 dev 环境。

2. **鉴权 + 租户 + 权限**
   - 登录 → 签发 JWT（含 tenant/user/roles）。
   - 后端 `governance.access_context()` + `current_user()`，所有查询强制 `tenant_id`。
   - 前端 `handle401()` 中央处理，路由 `meta.roles`，按钮 `v-permission`。
   - 实现 `Depends(require_perm(...))` 和 Scope 校验。

3. **统一请求封装 + 错误码**
   - 前端 Axios 拦截器：带 token、解析信封、401 跳登录、错误码转中文提示。
   - 后端统一异常处理器，落 1xxx/2xxx/3xxx/4xxx/5xxx。

**验收：** 能登录、能拿到当前用户信息、401 能自动跳转、每个页面能加载真实空数据不报错。

---

## 第 2 步：文本对话 SSE 主线（1-2 周，心脏）

这是整个系统的核心。**先只做文本，不碰图片、语音、工具。** 打通它，其他都是扩展。

后端：
- `POST /api/v1/agent/chat/stream`，`text/event-stream`。
- 固定发四个事件：`source → phase → message → done`。
- `done` 载荷带 `references + guard + faithfulness + trace_id`。
- 消息落 `messages` 表，会话落 `sessions` 表。
- LLM 先接一个真实模型（Qwen2.5-7B 或模型网关），RAG 先返回**假引用**。

前端：
- SSE 客户端封装：按 `event:` / `data:` 分帧，`done` 的 `JSON.parse` 必须 try/catch。
- ChatView 增量渲染、引用列表、断线重连、事件幂等。
- 新会话本地先建 `t-${Date.now()}`，成功后以后端为准。

**验收：** 发一句话，能流式返回，引用能显示，刷新后历史还在，断网重连不重复消息。

---

## 第 3 步：RAG 真检索（3-5 天）

把第 2 步的假引用换成真检索。

- 知识 CRUD + 版本 + 生效期 + 渠道过滤。
- 混合检索：向量 + BM25 + rerank，TopK=5，强制 `tenant_id + 生效期`。
- 无依据拒答话术，引用可点跳原文。
- 异步 reindex，版本可回滚，运营可检索测试。

**验收：** 有据能答、无据拒答、引用可溯源、跨租户检索不到。

---

## 第 4 步：多模态（1-2 周）

文本线通了，再往上加图、加音。

- 图片：上传 → 对象存储 → NSFW/人脸检测 → VLM 瑕疵检测 → 结果拼进 LLM 上下文。
- 低置信 <0.6 转人工。
- 语音：ASR 转写 + 置信度，TTS 开关/音色，波形播放。
- 前端图片预览/缩放/标注/检测卡片，语音波形/文字对照。

**验收：** 传图能出检测结果，语音能转写能播报，低置信能转人工。

---

## 第 5 步：工具调用 + 审批（1 周）

这是"业务闭环"的开始。

- 工具注册中心：JSON Schema + Scope + 幂等键 + 超时重试 + 熔断。
- 先实现 7 个连接器：order.query / logistics.query / stock.query / coupon.query / refund.create / image.inspect / kb.retrieve。
- `refund.create` 恒进 `WAITING_APPROVAL`。
- 前端 ToolCallCard 透明展示，ApprovalCenter 能批/拒/改参。

**验收：** 退款 100% 进审批，审批后能执行，审计留痕。

---

## 第 6 步：转人工 + 坐席工作台（1 周）

- 触发规则：喊人工、3 次不懂、愤怒、VLM 低置信、敏感退款、模型连续失败、无据拒答。
- 坐席工作台：待接队列、技能组、负载均衡、Trace 查看、快捷回复、内部备注、抢接/转接、质检、绩效。

**验收：** 能转人工、能接管、能看到完整 Trace、能质检打分。

---

## 第 7 步：B 端业务（2-3 周）

到这里 C 端主线已经稳了，再补商家侧。

优先顺序：
1. 商品 + SKU 矩阵
2. 库存：可用库存 = 在库 − 预占 − 锁定，Redis 原子扣减，零超卖
3. 订单履约 + 售后单关联 trace
4. 采购审批 → 到货 → 质检 → 入库
5. 资金对账
6. 营销发券（幂等 + 预算原子扣减）
7. 物流、评价、风控、消息、工单

**验收：** 并发下单超卖率 0，采购/调拨/报损 100% 审批，发券预算超发 0。

---

## 第 8 步：横向 + 大屏（1-2 周）

- 风控、消息中心、工单、组织绩效、多平台、大促保障。
- 经营大屏：指标口径、Redis 缓存 1 分钟、30 秒轮询、PII 脱敏、异常下钻。

**验收：** 大屏 <5min 准实时，对账差异 <0.1%，差评 2h 响应。

---

## 第 9 步：可观测 + 评估 + 验收（持续）

- 结构化日志、Prometheus 指标、OTel + Langfuse 追踪、Sentry + Web Vitals。
- 黄金集 ≥500，LLM-as-judge + 人工抽检。
- 1000 并发压测、CI 全绿、SBOM、ArgoCD 灰度回滚。
- 跑附录 C 的验收 Checklist。

---

## 如果你只有一周，就做这一件事

**登录 + 一条文本对话端到端跑通。**

具体到今天的任务：
1. 后端起 FastAPI 骨架，建 `users / sessions / messages` 三张表。
2. 写 `/auth/login` 和 `/api/v1/agent/chat/stream`。
3. 前端把 ChatView 的 mock 换成真 SSE。
4. 发一句"你好"，能看到流式返回，消息落库，刷新还在。

这条线一通，你就有了一个**可演示、可迭代、可验证**的最小闭环。之后每一步都是在它上面加东西，而不是在页面里到处点。

**核心原则记住一句：先竖着打通一条线，再横着铺开所有面。**
