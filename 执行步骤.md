# 执行步骤 —— 从 FRD 到系统的实施路线

> 对齐文档：`多模态智能电商客服系统需求文档-FRDv2.md` §6 核心业务流程 / §7 验收标准 / §8 分期路线
> 本版日期：2026-09-15（v0.2.6）。上一版写于「前端页面已搭好、后端未开工」阶段；**现在 P0 文本主线与 B 端一期代码已落地并跑通容器化部署**，本文改为「现状盘点 + 剩余工序」。
> v0.2.6：B 步 Agent Runtime + 工具注册中心落地（FR-3/FR-5，原为「services 直调形态」）——新建 `backend/app/modules/agent/`：`contracts`（状态机白名单 `IDLE→PLANNING→ACTING→OBSERVING→REFLECTING→DONE` + 三分支、ToolSpec、无三方依赖的 JSON Schema 子集校验，全部中文报错）/ `registry`（注册中心，超时重试口径单源解析）/ `policy`（Scope 硬拦 + 敏感动作恒判送审）/ `executor`（30s 超时、幂等工具退避重试 3 次、非幂等恒 1 次、连续失败熔断 60s、全分支写 `tool_calls` 审计；**业务拒绝不重试不计熔断**，超时 4002 / 依赖失败 4008 分开）/ `connectors`（附录 A 其余 6 个连接器，Scope 逐条对齐）/ `runtime`（规则规划 → 逐步执行 → `tasks.checkpoint` 每步落盘提交 → `resume` 续跑不重放）/ `bootstrap`（启动幂等注册，失败只告警不阻断）。接线：`main.py::lifespan` 启动注册 + 新增 `endpoints/agent.py` 6 条端点（§4.12）。**恒送审口径**：给 `order_service.create_aftersale` 加 `force_approval` 开关，AI 发起的退款不看金额一律进审批（人工后台仍走阈值规则，存量行为不变）。新增错误码 4005-4009 落 4xxx 段。验证：`py_compile` 全过、`ruff check app tests` 0 error、`app.openapi()` 78 paths（+6 条 `/agent/*`，含 `tools`/`invoke`/`run`/`runtime`/`resume`）；`test_agent_runtime.py` 13 项**实跑全绿**（注册中心/Schema 校验/策略/超时重试熔断/恒送审/编排检查点/审批闸门；本轮修 3 处测试口径：超长用例数据未超 `maxLength`、`GET /approvals` 需带 `page` 才返回分页对象、参数校验在策略之后故 1001 分支须换有 Scope 的用户）；后端全量 `pytest tests` **138 项 0 失败 0 错误**。
> v0.2.5：坐席工作台前后端真联调（FR-7 转人工闭环）——根因是工作台三栏还是演示骨架（队列客户端假数据、无流转动作、无备注/用量），后端 `handoff_status` 与 `session_notes` 已就绪但前端未接线。后端：新增 8 条 `/workbench/*` 端点（队列服务端分页/状态筛选/关键字、handoff/claim/transfer/resolve/reply/notes/trace）+ `sessions.handoff_status`/`session_notes` 迁移与索引 + chat 侧 `mark_pending_if_idle` 自动挂起钩子（need_human/拒答→pending，不抢已认领）；前端：`api/workbench.ts` 重建 8 接口 + 4 个组合式（`useWorkbenchQueue` 服务端分页 20、`useWorkbenchTrace`、`useWorkbenchNotes`、`useWorkbenchSide` 含上下文用量）+ 4 个组件（`WorkbenchQueue`/`WorkbenchChat`/`WorkbenchSide`/`WorkbenchNotes`，拆组件顺带消 `max-lines` 红灯）+ `WorkbenchView` 三栏编排（按 `handoff_status` 切动作、输入区三态锁定、代回落 agent 行、AI 接待走流式、队列/Trace/备注三路失败各自降级）。验证：`pytest tests/test_workbench.py` 4 项 + `smoke_workbench.py` 15/15（真链路）+ `pnpm lint`/`typecheck`/`build` 全过 + vitest 13 文件 64 项；`design.pen` 画板 `-/workbench` 同改三栏（队列页签与分页、流转动作、Trace/用量/备注卡），`check_design.py` 18/18 ERROR 0/WARN 0；API 规范补 §4.11（openapi 自查 72 paths）、页面设计 §1 路由表 + §3.2 + §7 同步。
> v0.2.4：审批中心前后端联调——根因是列表无服务端分页（前端客户端裁剪）+ 客服可见性缺口（列表权限缺 `cs`）+ 批驳无审计。后端 `GET /approvals` 加 `page/size/action/keyword` 服务端分页（`list_page` + 总数，存量 `limit` 数组兼容）+ 列表放行 `cs` + `decide` 记 `approval.approve|reject` 审计 + 批准透传 `reason`；前端 `approvals.ts` 切服务端分页 20 + `useApproval` 抽确认框与幂等（批量逐条）+ `ApprovalCenterView` 重写筛选/批量/金额/证据图/等待耗时 + `useApproval.test.ts` 3 项 + mock 口径对齐后端四类 action。验证：pytest 12 + `smoke_approvals.py` 13/13 + `pnpm lint`/`typecheck`/`build` + vitest 3 + `check_design --strict-color` 全过（mypy 本机缺走 CI；pytest 曾因残留 pytest 进程锁 `.pytest-tmp` 全 E，已清进程重跑全过）。openapi 自查：72 paths，`GET /approvals` 含 `action/keyword/limit/page/size/status`。
> v0.2.3：`/chat` 真流式——根因是后端全文生成完才 burst 切片（首字等全文）+ 前端气泡 `white-space` 默认吞换行。后端 `llm_service.acomplete_stream`（OpenAI 兼容 SSE 解析）+ `chat_service.stream_text_turn`（与 `run_text_turn` 同语义同落库，增量外吐+终态 result 协议，流中断后缀明示照常 done）+ 端点切流（validating 后移，帧形/id 幂等不变）；前端气泡 `pre-wrap`（Chat/Workbench/Widget 等 6 处）+ `useStickToBottom` 粘底跟随（翻历史不抢滚动，vitest 3 项）。验证：pytest 12（新 5）+ test_api 2 + eslint 0 + design 18/18 + detect 0；typecheck 2 项（审批金额/等待 helper）系同工作区在途改动，非本次引入；mypy 本机缺走 CI。
> v0.2.2：全站字体清晰度重设计——根因是浅气泡套深卡深字糊（Vision/Citation 在白泡内用深底 `--reai-card`，截图红框处）+ 无字体栈走浏览器默认 + 10px 微文案发虚 + 顶栏 75% 白发灰。改动：tokens 加苹方/雅黑字体栈 + 字阶（正文 14/1.6 起跳、微文案最小 11）+ 浅底专用色（`--reai-text-on-light-muted/faint`、`--reai-notice-deep`、`--reai-surface-light`、`--reai-text-soft`）与顶栏提亮 88%；Vision/Citation 改浅底语义卡深字；Chat/会话列表/顶栏/工作台三栏字阶对比度对齐；`design.pen` 同改 `/chat`+`/workbench`（10px→11 起跳、正文 13→14、微文案提亮、商品卡深底糊块改白卡）。验证：`pnpm lint`/`typecheck`/`build` + `check_design --strict-color` 全过（`check_arch` 2 项后端超长系工作区存量，非本次引入）。
> v0.2.1：前端体检问题清零（1→2→3→4）——会话分页合规（sizes+size 交 store+单测）；工作台队列抽 `WorkbenchQueue` 组件（消 max-lines 红灯，附空态）+ 右栏订单 props 化挂演示标 + 本轮 Trace 概览（`useWorkbenchSide` 取流式真 trace_id+单测）；裸 el-button/el-input 收敛 Ai 系 16 处；Chat 引导卡经核 v0.2.0 已有等效实现不修；修 `toMessage` 缺 `followups` 类型红灯。lint/typecheck/vitest 36/build/design 18/18 全过。
> v0.2.0：`/chat` 新会话预设 + 追问延伸——空态欢迎卡 6 条预设一键发送，最后一条回复下 3 条猜你想问（`useSuggestedQuestions` 规则匹配，历史恢复补挂）；新增 `ChatSuggestions/ChatMessage` 组件（ChatView 拆到 lint 400 行内）+ vitest 4 项；画板 `ch_empty/ch_quick/ch_followup` 同改，design-guard strict 过；页面设计 §3.1/§7 同步。
> v0.1.9：`/impeccable audit` 修复三件套——403/500 渐变字改实色（detect 2→0）、6 处硬编码收敛 token（对齐画板 `#FB9236` 橙/VoicePanel 运行时读 `--reai-accent`）、审批/营销/售后/评价（双表）补客户端分页 20（服务端分页待接）、语音图片按钮补 `aria-label` 16 处；`DESIGN.md` + `.impeccable/design.json` + `PRODUCT.md` 扫描补齐。design-guard `--strict-color` PASS；lint 11/12 干净（ChatView max-lines 系工作区存量 +123 行所致）+ typecheck 2 项系同工作区在途改动（followups/WorkbenchSide props）所致，均非本次引入。
> v0.1.8：`/impeccable clarify` 三页文案——物流页示例占位+列表生命周期说明+异常类型映射表（含 confirm 带类型名）；任务页补指定渠道输入（原选渠道无处填）、类型列中文化（新增 `TASK_TYPE_TAG`）、失败指引指向刷新按钮；看板去内部黑话（P2/英文标题）。lint/typecheck/detect 全过。
> v0.1.7：`/impeccable animate` 三页克制动效——任务页状态变更行闪光（轮询 status 变化/新建提交）、物流页新行进入高亮、看板指标值变化闪光，全带 `prefers-reduced-motion` 降级；critique 拦掉进度条 `width` 过渡（layout thrash，已 revert）。lint/typecheck/detect 全过。
> v0.1.6：按画板重生成差距 3 页——物流页补全（查询沉淀列表+异常登记+差评SLA摘要+分页20，回复流仍归评价页）；任务页切服务端真实列表（状态筛选+分页+日志/下载/重试，顺手正名过期“无列表接口”备注，画板同改）；看板加慢Trace Top5跳转。门禁 lint/typecheck/build + design 18/18 全过。
> v0.1.5：设计先行画板全覆盖 18/18（第 2 批 `/goods` `/orders` `/inventory` 画板已在树上；第 4 批补齐 `/purchase` `/finance` `/risk` 待实现页画板），棘轮 `ROUTE_COVERAGE_MIN` 12→18；三页补最小可用 views+路由（演示数据+分页20，接口与审批流随后补）。
> v0.1.4：设计先行第三批画板补齐（`/studio` `/admin` `/marketing` `/logistics` `/login`），`design.pen` 覆盖 7/18→12/18，棘轮 `ROUTE_COVERAGE_MIN` 7→12；`StudioView` 由占位补齐三窗格（Prompt版本/工具试调/评测禁发布，演示数据+分页20）。
> v0.1.3：设计先行第一批画板补齐（`/chat` `/tasks` `/dashboard`），`design.pen` 覆盖 7/18，棘轮 `ROUTE_COVERAGE_MIN` 3→7，命名债上限收紧到 0；新增 G 步「画板补齐」。
> v0.1.2：历史对话三层闭环（Session→Message→Context：窗口 20 轮 + 预算 8000 token + 摘要压缩 + PII 清洗，多模态附件转写进历史）；v0.1.1：A 步图片链路 MVP 落地 + 语音 stub 可用。

先说结论不变：**不按页面顺序做，按数据流顺序做；先竖着打通一条线，再横着铺开所有面。** 区别是——主线（登录 → SSE 对话 → RAG 引用 → 落库）已经打通，现在的任务是**补 P0 的纵向缺口（多模态、Runtime、转人工），再横向铺 P1/P2**。

---

## 一、现状盘点（已完成的步骤）

| 原步骤                    | 状态 | 落地情况                                                                                                                                                                               |
| ------------------------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 第 0 步 对齐契约          | ✅   | `API接口与SSE事件协议规范.md` 为唯一契约源；CI `docs-guard` 硬门禁强制接口与文档同改                                                                                                   |
| 第 1 步 地基三件套        | ✅   | Alembic 迁移 + `seed.py`；JWT（tenant/user/roles）+ `governance.access_context()` + `require_perm()` + 前端 `v-permission`；信封 `ok()/fail()` + 1xxx–5xxx 错误码 + 中央 `handle401()` |
| 第 2 步 文本对话 SSE 主线 | ✅   | `POST /api/v1/chat/stream`，事件 `source/phase/message/done`（done 含 references/guard/faithfulness/trace_id/context）；消息会话落库；断线幂等重放（client_msg_id）进行中                      |
| 历史对话三层闭环        | ✅   | Session（分页/改名/删除级联/活跃排序）→ Message（多模态附件/引用/trace 持久化）→ Context（窗口 20 轮 + 预算 8000 + 规则摘要 + PII 清洗进 LLM，`GET context` 同源可查）。验证：`test_sessions_history.py` 8 项 + `smoke_sessions.py` 9/9 + 文本冒烟 12/12 回归 |
| 第 3 步 RAG 真检索        | 🔶   | 已通：文档上传/解析/reindex、向量+关键词检索、租户+生效期过滤、无据拒答 2001、企业知识库种子 29 篇（`docs/knowledge-base/`）。缺口：BM25 + rerank 混合检索（FRD 列为 P1）              |
| 第 5 步 审批（部分）      | 🔶   | `approvals` 审批中心前后端联调已通（v0.2.4）：服务端分页 20 + 状态/类型/关键字筛选 + 批量批 + 改参批准 + 驳回必填留痕 + 批驳记审计；改价/盘点/补货/超阈值退款闭环（`decide` 先执行先生效动作再改状态，同事务回滚；`smoke_approvals.py` 13/13）。缺口：工具注册中心与其余 6 个连接器（见下表 B） |
| 第 6 步 转人工闭环（FR-7）| 🔶   | `handoff_status` 状态机（`none→pending→handling→resolved`，已解决可被买家再 `handoff` 重开）+ 8 条 `/workbench/*` 端点（队列服务端分页 20/状态筛选/关键字、认领/转接/解决/代回/内部备注/与买家侧同源 Trace）+ chat 侧自动挂起（`mark_pending_if_idle`：need_human/拒答 → `pending`，不抢已认领会话）+ 前端三栏真联调（`WorkbenchQueue`/`WorkbenchChat`/`WorkbenchSide`/`WorkbenchNotes` 四组件 + 四组合式；按流转态切动作、输入区三态锁定、代回落 agent 行、三路失败各自降级）。验证：`test_workbench.py` 4 项 + `smoke_workbench.py` 15/15。缺口：触发规则表未抽（喊人工/3 次不懂/愤怒/连续失败仍散在链路里）、技能组与负载均衡、抢接、质检打分/绩效 |
| B 步 Agent Runtime + 工具注册中心（FR-3/FR-5） | 🔶 | v0.2.6 落地：`backend/app/modules/agent/`（状态机 + checkpoint 恢复 / 工具注册中心 / Scope 策略 / 超时重试熔断执行器 / 6 个附录 A 连接器 / 规则规划编排）+ `main.py` 启动注册 + `endpoints/agent.py` 6 条端点；`refund.create` 恒送审（`create_aftersale(force_approval=True)`），审批未决 `resume` 直接 4003 不放行。验证：`py_compile` + `ruff check` 0 error + openapi 78 paths。缺口：**编排尚未接进 `/chat` 主链**（当前对话仍走 services 直调，Runtime 由 `/agent/run` 独立驱动）、规划器为规则版（未接 LLM function-call）、RAG 拒答/工具熔断尚未在编排层切「规则机器人」降级话术 |
| 第 7 步 B 端一期（部分）  | 🔶   | 商品/SKU、库存、订单履约、物流、营销发券、评价、工单、管理中心已落地（14 个前端域 + 16 组 endpoints）。缺口：采购审批、资金对账（FRD 列为 B 端二期/P2）                                |
| A 步图片链路 MVP          | ✅   | `POST /multimodal/images`（存→预检→VLM，一次回检测卡）→ inspections 透传 `chat/stream`（`inspecting` 相 + done.vision/need_human）→ 前端检测卡/预览标注/转人工；语音转写+TTS 配置可用。验证：`test_multimodal.py` 12 项 + `smoke_multimodal.py` 10/10 + `eval_vision.py` 管线 PASS（合成集，真集待采） |
| 部署                      | ✅   | compose + GHCR 镜像（CI 自动发布）+ `deploy.ps1` 端到端验证 + ArgoCD 清单（dev 自动/prod 手动）；v0.1.0 已发布                                                                         |
| 设计先行（`design.pen`）   | 🔶   | 门禁已落地：`scripts/check_design.py` + CI `design-guard`（命名/覆盖棘轮/同改/色值）。画板 **18/18 全覆盖**（第 1-4 批齐），全部深色 token 口径、ERROR 0/WARN 0（`--strict-color` 过）。剩余债：`/purchase` `/finance` `/risk` 三页仅演示数据，接口与审批流待补（见 G 步） |

生成模型现状：本地 Ollama `qwen2.5:0.5b`（ADR-0001），唯一出口 `llm_service.py`，不可用自动降级模板摘要（不 500）。

---

## 二、剩余工序（按依赖顺序）

### A. 多模态接入（FR-1，P0 缺口，1-2 周）⭐ 最优先 —— 图片链路 MVP 已落地，余真机集 + 网关

文本线通了，按 FRD §6 P1 主流程补「图文售后」——这是系统的招牌场景：

- 图片 ✅：上传 → 对象存储 → NSFW/PII 预检 → VLM 瑕疵检测（`image.inspect`）→ 检测结果拼进 LLM 上下文 → 置信 <0.6 转人工（`vision_service` 单出口 + `chat/stream` 加法字段向后兼容）。
- 语音 🔶：ASR 转写 + 置信度 stub 可用；TTS 开关/音色；波形播放（SenseVoice/edge-tts 网关待接，`Settings` 占位名已留）。
- 前端 ✅：图片预览/缩放/标注（`ImagePreviewDialog`）+ 检测卡片（`VisionResultCard`），语音文字对照（`VoicePanel`）。
- 模型接入沿用 `llm_service` 单出口模式，VLM/ASR 已进 `Settings`，无散落硬编码。

**验收（FRD §7）：** 图片链路 `test_multimodal.py` 12 项 + `smoke_multimodal.py` 10/10 + `eval_vision.py` 管线 PASS（合成集 8 类×20 张 F1=1.0；真机 200 张/类集待采）；传图出检测结果、低置信转人工、三模态 E2E 通、弱网重连（`useAgentStream` 同 key 3 次退避）过。剩余：真机集复测 + Qwen3-VL 传图/ASR 网关替换 stub（签名不变）。

### B. Agent Runtime 状态机 + 工具注册中心（FR-3/FR-5）✅ v0.2.6 已落地

编排内核已建成（`backend/app/modules/agent/`）：

- ✅ 状态机 `IDLE→PLANNING→ACTING→OBSERVING→REFLECTING→DONE` + 三分支 `WAITING_APPROVAL / WAITING_HUMAN / FAILED`，流转白名单集中在 `contracts.TRANSITIONS`，非法流转 4009；每步 checkpoint 落 `tasks.checkpoint` 并提交，`resume` 从断点续跑不重放已完成步。
- ✅ 工具注册中心：JSON Schema 子集（无三方依赖，中文报错）+ Scope 鉴权（4006）+ 幂等键 + 超时 30s（4002）+ 幂等工具退避重试 3 次（非幂等恒 1 次）+ 熔断 60s（4007）+ 全分支 `tool_calls` 审计。
- ✅ 补齐附录 A 其余 6 连接器（Scope 逐条对齐）：`order.query / logistics.query / stock.query / coupon.query / kb.retrieve / refund.create`；`refund.create` 恒进审批（`force_approval=True`，账不动，批准后才 `apply_refund`）。
- ✅ 端点与接线：`endpoints/agent.py` 6 条（`GET /tools`、`GET /tools/{name}`、`POST /tools/{name}/invoke`、`POST /run`、`GET|POST /runtime/{task_id}[/resume]`）+ `main.py::lifespan` 启动幂等注册（失败只告警不阻断）。

**余（下一轮）：** ① 把编排接进 `/chat` 主链（现在对话仍走 services 直调，Runtime 由 `/agent/run` 独立驱动）；② 规划器由规则版升级为 LLM function-call（保留规则兜底）；③ 熔断/RAG 拒答在编排层切「规则机器人」降级话术（FR-5 三级容错第三级）。

**验收：** 敏感操作审批率 100%（审计扫漏 + 越权渗透 0 泄漏）；ToolCallCard 透明展示，审批中心能批/拒/改参。

### C. 转人工 + 坐席完整台（FR-7，1 周）

- ~~触发规则~~：喊人工 / 无据拒答已接（chat 侧 `mark_pending_if_idle` → `pending`，不抢已认领会话）；**余**「3 次不懂 / 愤怒 / VLM 低置信 / 敏感退款 / 模型连续失败」，且规则目前散在链路里，待抽成可配规则表。
- ~~坐席工作台~~（v0.2.5 已通）：✅ 待接队列（服务端分页 20 + 状态页签 + 关键字）、✅ 认领/转接/解决状态机（`none→pending→handling→resolved`，可重开）、✅ 完整 Trace 查看（与买家侧同源）、✅ 内部备注（买家不可见）、✅ 代回落 agent 行；**余** 技能组、负载均衡、抢接、质检打分、绩效。
- 口径统一：代码用 `sessions.handoff_status ∈ {none,pending,handling,resolved}`（API 规范 §4.11），与本文原 `WAITING_HUMAN` 为同一状态机的不同写名，实现以代码为准。

**验收：** 转人工 30s 内接起率 ≥95%；坐席能看到全链路 Trace 并接管。

### D. 生产存储切换（附录 B/E/F，1 周）

现在 dev/compose 都是 SQLite + 本地文件，生产基线未接：

- PostgreSQL（附录 B/E/F DDL + Alembic，先加字段后发代码再删旧字段）、Redis（库存预占原子扣减、大屏缓存、频控）、MinIO/S3（图片与文档原件）。
- 库存口径：可用 = 在库 − 预占 − 锁定，Redis 原子操作保证零超卖。

**验收：** 并发下单超卖率 = 0；库存账实一致 ≥99.5%；跨租户检索/查询 0 串数。

### E. 评估与验收流水线（FR-9，持续）

- 黄金集 ≥500 条（P0 先 200），LLM-as-judge + 人工抽检；自动解决率 ≥80%、grounded ≥95%、幻觉 ≤2%。
- 1000 并发混合压测（文 70%/图 20%/音 10%），首字 P95 <2s；成本归因误差 <5%。
- 跑 FRD 附录 C 发布门禁 Checklist（含 SBOM、扫描、ArgoCD 回滚 <5min 演示）。

### F. P1/P2 横向铺开（各 2-4 周）

- P1：以图搜款、OCR、混合检索 rerank、计费、大促预案、A/B + 影子。
- P2：B 端二期（采购审批→到货→质检→入库、资金对账）、经营大屏（准实时 <5min、PII 脱敏、异常下钻）、风控/绩效/多平台、多语言、微前端拆分。

### G. 画板补齐（design-first，随页推进）

`design.pen` 是 UI 唯一事实源，终态必须**一页一画板全覆盖**（分母 = `页面设计.md` §1 路由表 18 条）。当前 **18/18 全覆盖**（第 2 批画板已在树上，第 4 批补齐最后 3 张）：

| 批次 | 画板 | 为什么排这里 |
| --- | --- | --- |
| ✅ 第 1 批 | `/chat` `/tasks` `/dashboard` | 代码已跑通但视觉无事实源，改动最频繁 |
| ✅ 第 2 批 | `/goods` `/orders` `/inventory` | B 端一期主数据三页，`/goods` 已接真实接口（画板已在树上，`goodsRoot/ordersRoot/inventoryRoot`） |
| ✅ 第 3 批 | `/studio` `/admin` `/marketing` `/logistics` `/login` | 第 3 批先行补齐（含 `StudioView` 三窗格代码对齐）；`/admin` 权限差异已画全 |
| ✅ 第 4 批 | `/purchase` `/finance` `/risk` | **待实现页**（无 view/接口）：画板先行已出，最小可用 views+路由已补（演示数据+分页20），接口与审批流随后补 |

- **G 步转常态维护**（不再有「待出画板」）：① 棘轮 `ROUTE_COVERAGE_MIN=18` 硬拦覆盖回退，少一张画板即 CI `design-guard` 红；② 改 `frontend/src/{views,components}` 视觉必须同 commit 改对应画板。18 张顶层并列，x = 0 → 22780（间距 1340，均 1280×800）。
- 待决口径债：`/reviews` `/aftersales` `/widget` 代码有、§1 表没有；`/403` `/404` `/500` 建议豁免到组件级 —— 出图前先把 §1 表与 `router/index.ts` 对齐，否则分母失真。
- 每张画板必须含**状态完整性**：空态 / 加载 / 错误、分页（默认 20，可切 10/20/50/100）、权限差异、破坏性操作二次确认、多模态溯源（引用卡 + trace_id）。

---

## 三、如果本周只做一件事

**把 B 步的编排接进 `/chat` 主链：让 `/chat/stream` 从 services 直调切到 Runtime 驱动（规划 → 工具 → 引用 → 审批/转人工），保留现有 SSE 帧形与降级兜底。**

理由：A 步（图片链路）与 B 步（Runtime + 工具注册中心）都已落地，但两者目前是**两条并行轨道**——编排内核只能由 `/agent/run` 独立驱动，真实买家对话仍走 services 直调。不接线，工具注册中心与状态机就是「带验收的能力孤岛」；接上之后 C 步（转人工触发规则表）才有统一的挂载点，采购/售后等新连接器也才能零成本接入。

**核心原则一句：主线已竖通、编排内核已建成，现在的关键动作是「接线」——把 Runtime 接进对话主链，再按 C→D 补齐 P0 纵向缺口；E 的评估流水线并行推进，F 再横向铺面。**
