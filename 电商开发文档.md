# 标准企业级大模型驱动电商服装多模态智能客服系统 · 功能需求文档（FRD v3.0）

> 版本：v3.0 | 日期：2026-09-12 | 状态：基线（立项/研发/测试/验收统一依据）
> 合并来源：多模态智能电商客服系统需求文档.md / 需求文档-FRDv2.md / 后端工程化.md / 前端工程化.md / 部署工程化.md
> 定位：替代原《电商开发文档.md（架构指南版）》，升级为**企业级 FRD 标准版**：业务闭环 + 功能可测 + 架构可落 + 验收量化。
> 编号规范：FR-大模块 / FR-x.y 子功能 / BR 业务规则 / AC 验收口径。优先级：P0 MVP 必做 / P1 次迭代 / P2 持续。

一句话定义：**多模态交互（文本/图片/语音）+ Agent Runtime 状态机 + 场景化 RAG 与业务连接器 + 人机协同审批与坐席工作台 + 多租户与模型网关 + 全链路可观测与评估 + K8s 云原生交付 + 成本与 ROI 闭环 = 生产可用、可治理、可评估、成本可控的智能客服平台。**

---

## 0. 文档控制

| 项 | 内容 |
|---|---|
| 目标读者 | 产品 / 研发（前/后端）/ 测试 / SRE / 安全 / 甲方招投标 |
| 需求基线 | v3.0 冻结后变更走 CR，关联回归集与灰度策略 |
| 事件协议 | SSE 事件名以《API接口与SSE事件协议规范.md》§5 为唯一权威源（`source/phase/message/done`），本文 FR-1.1 仅为摘要，不得偏离 |
| 术语 | Agent Runtime：核心状态机；RAG：检索增强生成；VLM：视觉大模型；ASR/TTS：语音转写/合成；SLO：服务等级目标 |
| 参考 | IEEE 830 SRS 结构；企业招投标 FRD 惯例：范围 In/Out + 角色矩阵 + 可测验收 |

**标准企业级开发文档应该是什么样？** 本文档即示范：
1. 先讲清为什么做（背景/量化目标/范围边界），再讲给谁用（角色/权限/数据范围）。
2. 功能按模块编号、可测、可追溯（每个 FR 有输入/规则/异常/验收），不写空话。
3. 技术选型统一冲突点（开发默认 vs 生产标准），前后端契约先行（事件协议/API/建表）。
4. 非功能量化（延迟/并发/可用/安全/成本），验收一票否决项明确。
5. 分期可交付（P0 MVP 4-6周），风险有预案。

---

## 1. 项目概述

### 1.1 背景
电商服装（淘宝/抖店/拼多多/京东/独立站）日均咨询 800-1200 条，大促 5-10 倍。痛点：人工成本高、响应慢；尺码/面料/优惠/物流/退换高频重复；售后需传瑕疵图、发语音，传统文本机器人无法闭环；大模型直答幻觉率高，不可直接生产。

### 1.2 产品目标（量化）
1. 降本增效：自动解决率 ≥80%，单会话成本 ≤人工 1/10。
2. 多模态闭环：文本流式 + 图片瑕疵检测 + 语音转写合成，低置信兜底转人工。
3. 业务精准：RAG 强约束 + 引用必现 + 无据拒答，幻觉率 ≤2%。
4. 生产可控：多租户隔离、敏感操作审批 100%、全链路审计、成本归因、灰度回滚、大促可用 99.95%。

### 1.3 范围
**In：** C端咨询、图文售后、语音客服、订单/物流/库存/优惠/退款工具、审批转人工、坐席工作台、运营后台（知识/Prompt/评估）、模型网关/可观测、K8s 交付。
**Out（v1不做，预留接口）：** 实时电话外呼、视频客服、跨境多语言（仅预留 locale + 模型路由位）、以图搜款/OCR（P1）。

### 1.4 目标用户
买家 / 人工客服 / 运营 / 平台管理员 / SRE / 安全合规。

---

## 2. 技术选型基线（统一五篇冲突点）

| 维度 | 开发默认 | 生产标准 | 说明 |
|---|---|---|---|
| 对话LLM | Qwen2.5-7B-Instruct | 模型网关路由：小模型优先，复杂上大模型；支持硅基流动/OpenAI/通义/私有vLLM | 按租户配额+成本路由，Fallback必填 |
| 视觉VLM | Qwen3-VL-8B-Instruct | 同左，GPU节点池常驻+预热 | 挂了降级人工复核 |
| 语音 | SenseVoiceSmall ASR + edge-tts晓晓 | 网关封装，可切音色 | 低置信转文字兜底 |
| RAG向量 | ChromaDB + BGE-small-zh(CPU) | pgvector起步，>100万向量切Milvus/Qdrant | 统一RagService接口 |
| 业务DB | SQLite兼容 | PostgreSQL主从 + Alembic | 生产强制PG |
| 缓存/队列 | Redis单机 | Redis Cluster/Sentinel + Celery/ARQ + Streams/RabbitMQ | Worker用KEDA伸缩 |
| 对象存储 | MinIO | S3/OSS/MinIO（统一S3协议） | 存瑕疵图/语音，人脸打码 |
| 前端 | Vue3+TS strict+Element Plus+Pinia+Vite+pnpm | 同左 + CDN + Nginx | 按业务域分层，统一SSE事件协议 |
| 后端 | FastAPI+Pydantic v2+SQLAlchemy2.0 async | + Gunicorn/Uvicorn + K8s HPA | Router薄/Service厚/Runtime专 |
| 可观测 | OTel+Prom+Grafana+Loki/ELK+Sentry+Langfuse | 同左 | 日志强制trace/tenant/user |

原则：API薄、Service厚、Runtime专、工具可插拔；一切皆代码、不可变镜像、无状态优先、控制面/数据面分离、默认安全、可回滚。

---

## 3. 总体架构

```text
渠道层：淘宝/抖音/拼多多/京东/独立站/Web/工单/语音
  ↓
体验层：对话助手 | 任务中心 | 坐席工作台 | Agent运营后台
  ↓
接入层：API网关(BFF) | SSO | 多租户隔离 | 限流熔断 | 审计
  ↓
Agent平台层：Runtime状态机 | 多模态路由 | 工具注册 | RAG | 记忆 | 模型网关 | 策略引擎 | 审批 | 评估 | 可观测
  ↓
数据集成层：PostgreSQL | Redis | 向量库 | 对象存储 | MQ | 业务连接器(订单/物流/库存/优惠/退款)
  ↓
基础设施：K8s | GPU调度 | CI/CD | OTel/Prom/Loki/Sentry
横切治理：安全合规 | RBAC/ABAC | 审计 | 成本 | SLO
```

---

## 4. 角色与权限矩阵

| 角色 | 核心权限 | 数据范围 |
|---|---|---|
| 买家 | 发起会话、传图、发语音、看回复 | 仅本人user_id会话 |
| 人工客服 | 接管/抢接/转接、快捷回复、内部备注、审批、改参重试、看完整Trace | 所属tenant_id + 技能组（售前/售后/VIP） |
| 运营 | 知识CRUD/版本/检索测试、Prompt灰度、评估集、敏感词/极限词库、大促话术开关 | 所属租户知识与配置 |
| 管理员 | 租户/订阅/配额/网关/计费/SLO/审计 | 全局，操作留痕 |
| SRE/安全 | 发布/回滚/告警/密钥/网络策略 | 基础设施面 |

强制 BR-PERM-1~4：所有查询带tenant_id；订单/退款再校验订单归属==user_id；RAG带tenant+生效期+渠道过滤；工具按Scope鉴权；前端v-permission+路由meta.roles，后端Depends(get_current_user)双检。

---

## 5. 功能需求详述

### FR-1 多模态智能交互（P0）

**FR-1.1 文本客服**
- 多轮对话，意图识别：咨询/导购/投诉/售后；情绪：正常/不满/愤怒。
- RAG检索后生成，System Prompt强制“仅基于引用回答”。
- SSE流式，统一事件协议（前后端契约，**唯一权威源：《API接口与SSE事件协议规范.md》§5**，此处仅为摘要）：
  事件名固定 `source / phase(retrieving[/inspecting]/generating/validating) / message / done`（任务类另有 `progress/complete/error`；图文轮多一帧 `inspecting`）；`done` 载荷必含 `references + guard + faithfulness + trace_id`（另有 `session_id/tool_calls[]/orchestration/handoff/context` 等加法字段，老前端忽略即可）。前端按 `event: / data:` 正则分帧解析，`done` 的 `JSON.parse` 必须 try/catch，请求必带 `Authorization: Bearer <token>`。注意与旧设计差异：**工具调用不是独立事件类型**（经 `done.tool_calls[]` 透出），**审批不是事件**（走 `WAITING_APPROVAL` 状态机 + `/approvals` 接口，FR-2），错误不单独占事件（信封 `fail` + 错误码号段）。
- 断线重连+事件ID幂等，前端增量渲染+虚拟列表，乐观更新。
- AC：首字P95<2s，断网重连不丢消息。

**FR-1.2 图文客服 VLM瑕疵检测**
- 限制：≤9张/单张≤10M/JPG-PNG-WEBP；超限前端压缩+后端拒收码IMAGE_TOO_LARGE。
- 预处理BGR→RGB，Base64/S3 URL双入参；必经NSFW/黄赌毒/人脸检测，人脸打码后存。
- VLM输出结构化：`{category:污渍/破洞/脱线/色差/开线/尺寸不符/吊牌异常/无瑕疵, confidence:0-1, bbox?, desc}`。
- BR：confidence<0.6 或无瑕疵但用户坚持 → 自动转人工，不硬答。
- 结果拼入LLM上下文+RAG退换政策，生成`致歉+定级+方案{退/换/补/修}+时效`。
- 前端：图片预览/缩放/检测卡片+CitationList。P1预留OCR吊牌/以图搜款。
- AC：分类F1≥0.85，召回≥0.9（200张/类测试集）。

**FR-1.3 语音客服 ASR/TTS**
- ≤60s/≤5M，Mp3/Wav/M4A/Opus；ASR返回text+confidence，低置信回问确认。
- 回复支持TTS，可开关/切音色（默认晓晓），前端波形+播放+文字对照。
- 录音/转写需明示获同意；方言/噪音降级文字并告知。
- AC：三模态E2E全通，Playwright+MSW mock SSE通过。

**FR-1.4 会话管理**
- session_id隔离，PG持久化+Redis短期上下文。
- 双重修剪：轮数N默认20 + Token预算M默认8k，超限摘要压缩+正则清洗。
- 支持恢复、翻页、按单号关联多会话。

### FR-2 服装业务场景（P0，v1/v2差异最大处）

**售前导购：** 尺码推荐必填身高/体重/三围(可选)/版型偏好(修身/常规/宽松)/面料弹力；缺参追问，给范围不给绝对值（例M-L均可，建议M）；面料洗护、库存查询、优惠叠加规则引擎（满减/券/预售尾款），大促话术一键切换。
**售中：** 订单查询、物流追踪（节点卡片）、改地址、催发货；调业务连接器，敏感字段脱敏`138****1234`。
**售后：** 退换政策咨询、图文瑕疵定级、投诉。7天无理由/质量问题15天按知识生效期判断，过期不可召回。主动营销禁令：仅用户触发可推优惠。
闭环：发起→意图+情绪路由→RAG+工具→回复/转人工→持久化→mining进运营后台。

### FR-3 Agent Runtime 状态机（P0）
`IDLE→PLANNING→ACTING→OBSERVING→REFLECTING→DONE`，分支`WAITING_APPROVAL/WAITING_HUMAN/FAILED`。
- 检查点每步持久化tasks表，支持暂停/恢复/重试/超时+死信队列。
- 每步TraceID，工具必经policy.check。长任务>30s转异步+轮询/WebSocket，不阻塞SSE。
- 后端禁把Runtime写进Router/Service；前端Pinia映射IDLE/PLANNING/ACTING/WAITING_APPROVAL/DONE。

### FR-4 RAG知识库与记忆（P0）
- 知识JSON：`{title,content,tenant_id,适用渠道,生效起止,版本}`，按业务主题切分。
- 混合检索：向量+BM25+rerank，TopK=5，tenant+生效期过滤，热点缓存。
- 无据拒答话术：“这个问题我暂时没查到权威政策，已为你转人工…”，禁编造单号/政策。
- 前端必显引用来源可点跳原文；更新走异步reindex，版本可回滚，运营可检索测试。
- 记忆：短期Redis TTL 24h；长期偏好PG/向量，需授权，可一键遗忘（GDPR/个保）。

### FR-5 工具调用系统（P0，清单见§7）
- 注册中心：JSON Schema+Scope+幂等键+超时30s重试3次+熔断+沙箱+全审计。
- 三级容错：重试→降级预设话术“系统繁忙，已记录，转人工跟进”→网关熔断切规则机器人。
- 前端ToolCallCard透明展示，可展开参果。

### FR-6 模型网关与成本（P0）
- 统一入口：路由/Fallback/缓存/限流/配额/Token+GPU成本归因（按租户/部门/Agent）。
- 小模型优先，Key集中Vault/K8s Secret，不进代码/镜像/前端。超预算限流降级+告警。

### FR-7 人机协同、审批与坐席工作台（P0最小闭环）
触发转人工：用户喊人工/连续3次不懂/愤怒/VLM低置信/敏感退款/模型连续失败/无据拒答。
坐席台：待接队列+技能组+负载均衡+VIP优先+排队播报；完整Trace；快捷回复、内部备注（买家不可见）、抢接/转接、质检打分、绩效统计。
审批：退款/补偿/改价进WAITING_APPROVAL，`POST /approvals/{id}/approve|reject`，可改参重试，超时升级，记录不可篡改。

### FR-8 运营与管理后台（P0基础+P1完善）
- 租户订阅计费：按会话/Token/包年，试用/欠费停服/超额限流。
- Prompt版本+灰度+回滚；评估集；敏感词+广告极限词（最/第一/顶级）动态库。
- 大促开关、公告、OpenAPI/Webhook、企微/钉钉告警。
- 前端模块：ChatView/TaskCenter/ApprovalCenter/AgentStudio（Prompt/工具/知识画布）/AdminPanel（租户/配额/审计）。Vue3+TS strict+Element Plus按需引入，Axios统一封装，路由懒加载+v-permission。

---

## 6. 核心业务流程

**P1 图文售后主流程：** 买家传图+“破了怎么处理”→前端打包→网关鉴权→对象存储→NSFW/PII预检→VLM检测`袖口脱线2cm 0.82`→拼检测+RAG退换TopK→LLM生成致歉+方案+引用→SSE流式→追问是否代申请→确认→refund.create→WAITING_APPROVAL→坐席批→执行→审计→归档→mining。
**P2 转人工：** 3次不懂/愤怒→WAITING_HUMAN→坐席看Trace接管→内部备注+质检。

---

## 7. 数据与接口概要（v1必实现7工具）

通用：鉴权Bearer JWT(tenant/user/roles)；幂等头Idempotency-Key；超时30s重试3次（仅幂等方法）；错误码：**数字号段制（1xxx~5xxx），唯一权威源《API接口与SSE事件协议规范.md》§2（`core/exceptions.py::ErrorCode`，新增必须落号段）**；下述旧英文枚举（OK/PARAM_INVALID/AUTH_DENIED等）已废弃，语义对照以 §2 错误码表为准。

| 工具 | Scope | 入参 | 出参 | 备注 |
|---|---|---|---|---|
| order.query | order:read | {order_id,user_id} | {status,items,total,address_masked,tenant_id} | 必校验归属 |
| logistics.query | order:read | {order_id} | {nodes[{time,desc}],eta} | 节点卡片 |
| stock.query | stock:read | {sku,size} | {available,eta_restock} | 大促缓存30s |
| coupon.query | promo:read | {user_id} | {coupons[],rules} | 叠加引擎算 |
| refund.create | trade:refund | {order_id,reason,amount,evidence_urls[]} | {refund_id,status:WAITING_APPROVAL} | 恒进审批 |
| image.inspect | vision:inspect | {image_urls[],session_id} | {category,confidence,bbox?,desc,safe_pass} | <0.6转人工 |
| kb.retrieve | kb:read | {query,top_k=5} | {docs[{title,snippet,source,score}],trace} | tenant+生效期过滤 |

审批：`POST /approvals/{id}/approve {modified_args?}` / `.../reject {reason}`，需approval:write+同租户+技能组。
流式：`POST /api/v1/agent/chat/stream` SSE text/event-stream，事件见FR-1.1（唯一权威源：《API接口与SSE事件协议规范.md》§5）；>30s返回{task_id}转GET /tasks/{id}轮询/WS。
核心建表（PG最小闭环）：tenants/users/sessions/messages/tasks/approvals/tool_calls/kb_docs/audit_logs/cost_records，所有业务查询强制where tenant_id=:tid，审计只追加不改。详见FRDv2附录B。

---

## 8. 非功能需求 NFR（摘要，详见部署工程化）

性能SLO：首字<2s P95，完整<10s，坐席消息<1s；1000并发混合（文70%/图20%/音10%）；Nginx `proxy_buffering off`，`proxy_read_timeout 3600s`，多副本粘性或共享状态。
可用灾备：日常99.9%，大促99.95%，多AZ+PDB+优雅停机；DB每日全量+WAL，Redis持久化+哨兵/集群，OSS跨区复制；RTO<15min RPO<5min。
安全合规：等保/PIPL/GDPR/电商法留痕≥6月；PII脱敏；Prompt注入/越狱/图片对抗过滤；WAF+限流；镜像Trivy/Grype+Cosign+SBOM；K8s RBAC最小+NetworkPolicy默认拒绝；密钥Vault。
工程化：后端Ruff+mypy+pytest+coverage；前端ESLint+Prettier+Stylelint+Husky+commitlint+vue-tsc；CI lint→typecheck→test→build→扫描→SBOM；GitOps ArgoCD，滚动/蓝绿/金丝雀，DB先加字段后发代码再删旧字段。
成本：requests/limits必填，HPA/KEDA，GPU按需启停+预热，双缓存，月度ROI+预算告警。

---

## 9. 验收标准（可测口径，一票否决*）

| 项 | 阈值 | 测法 |
|---|---|---|
| 三模态闭环* | 文/图/音全通 | E2E Playwright+MSW mock SSE |
| 自动解决率* | ≥80% | 黄金集+线上抽1000条人工标 |
| RAG grounded/幻觉* | ≥95%/≤2% | judge+人工，引用可溯 |
| 瑕疵F1/召回 | ≥0.85/≥0.9 | 200张/类 |
| 审批率* | 100% | 审计扫漏+越权渗透 |
| 首字P95/可用/转人工接起 | <2s/99.9%/≥95% 30s内 | 压测+APM |
| 并发 | 1000混合 | k6/Locust，Ingress已关缓冲 |
| CI/回滚/灰度 | 全过，回滚<5min | 演示 |
| 成本归因误差 | <5%，单会话≤人工1/10 | 账单对账 |

黄金集≥500条（售前200/售中100/售后200含图文100），LLM-as-judge+人工抽检双轨；影子/A-B；不达标禁发布。

---

## 10. 分期、交付、风险

**P0 MVP 4-6周：** 文本+单图售后+ASR/TTS基线+RAG+退款审批+转人工最小台+多租户+网关+SSE+PG/Redis/MinIO+K8s+可观测+黄金集200条+CI门禁。
**P1 2-4周：** 以图搜款/OCR/坐席完整台/计费/混合检索rerank/大促预案/A-B/影子。
**P2：** 多语言/视频/挖掘闭环/微前端拆分/等保/灾备常态化。
交付物：前端（对话/审批/任务/Studio/Admin）/后端（Runtime/RAG/网关/工具/记忆/审批/评估）/K8s YAML+Dockerfile+Nginx+ArgoCD/API Swagger+事件协议/部署运维手册/pytest+vitest+E2E+黄金集+压测/SBOM+扫描报告。
Top风险：供应商挂→Fallback+规则机器人；幻觉赔付→引用强制+拒答；误判→阈值+复核；大促击穿→限流+预热+降级；串数→三重校验+渗透；泄漏→Vault+轮换。

---

## 附录 发布门禁Checklist

- [ ] 三模态E2E通，弱网/断线重连过
- [ ] 黄金集≥500，解决率/幻觉/引用达标
- [ ] 瑕疵集F1/召回达标，低置信转人工演示
- [ ] 退款100%进审批，越权0泄漏，极限词/PII过滤过
- [ ] 1000混合压测+首字P95+熔断降级演示
- [ ] CI全绿+SBOM+扫描+ArgoCD灰度回滚<5min
- [ ] 成本归因误差<5%，SLO大盘+告警+留痕≥6月可查
