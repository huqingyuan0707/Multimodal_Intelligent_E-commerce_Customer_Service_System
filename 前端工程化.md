## 一句话定义

**Agent + Vue3 + TS + Element Plus 前端工程化** = 在标准 Vue3 工程化基础上，围绕 **流式交互、Agent 状态机、工具调用、审批、引用、多租户权限、长会话、任务中心** 建立规范、类型、分层、组件、通信、构建、质量、交付、监控闭环，让 Agent 前端可维护、可扩展、可协作、可交付、可观测。

---

## 总体分层架构简图

```text
渠道层：Web / 移动端 / IM / 嵌入页
        ↓
体验层：对话助手 | 任务中心 | 审批中心 | Agent Studio | 管理后台
        ↓
前端应用层：Vue3 + TS + Element Plus + Pinia + Router
        ↓
通信层：REST | SSE | WebSocket | 统一事件协议 | 断线重连
        ↓
状态层：会话 | Agent 状态机 | 工具调用 | 审批 | 引用 | 权限
        ↓
工程层：规范 | 类型 | 组件 | 构建 | 测试 | CI/CD | 监控
```

---

## 一、技术栈与工程基线

| 维度 | 推荐 |
|---|---|
| 包管理 | pnpm |
| 构建 | Vite + TypeScript + vue-tsc |
| 框架 | Vue3 + `<script setup lang="ts">` |
| UI | Element Plus 按需引入 + 主题定制 |
| 路由 | Vue Router |
| 状态 | Pinia + 持久化插件 |
| 请求 | Axios + 统一封装 |
| 流式 | SSE / WebSocket + 统一事件协议 |
| 规范 | ESLint + Prettier + Stylelint + Husky + lint-staged + commitlint |
| 测试 | Vitest + Vue Test Utils + Playwright + MSW |
| 文档 | Storybook / VitePress |
| 监控 | Sentry + Web Vitals + 埋点 |
| CI/CD | GitHub Actions / GitLab CI + Docker + Nginx |
| 可选 | pnpm workspace + Turborepo、微前端 |

目标：  
**开发提效、协作统一、质量可控、性能可优化、部署可回滚、线上可观测。**

---

## 二、项目结构：按业务域分层

页面按域放 `views/<domain>`，逻辑按层放顶层 `components/composables/stores/types`，通用件下沉 `shared`：

```text
src/
  app/                     # 应用入口、全局配置、插件
  api/                     # 唯一请求入口 request<T>
  assets/
  components/              # 业务组件（ChatPanel/StreamMessage/ToolCallCard/ApprovalCard/CitationList/AgentStatus）
  composables/             # 可复用逻辑（useAgentStream/useChat/...），页面只做编排
  mock/                    # 降级演示数据
  router/                  # 路由表（懒加载 @/views/<domain>/XxxView.vue）
  stores/                  # Pinia setup 风格（session/...）
  types/                   # 跨文件共享类型先行（agent.ts：Message/Reference/AgentEvent）
  views/
    admin/                 # AdminView
    agent/                 # ChatView/TaskCenterView/ApprovalCenterView
    auth/                  # LoginView/NotFoundView
    dashboard/             # DashboardView
    knowledge/             # KnowledgeView
    studio/                # StudioView
    workbench/             # WorkbenchView
  entities/                # 业务实体、模型
  shared/
    components/
    composables/
    utils/
    types/
    styles/
```

原则：  
**页面按域、逻辑按层、通用下沉、页面薄、逻辑进 composables。**

---

## 三、工程规范与协作

- 使用 `pnpm`，锁定依赖，统一 Node 版本 `.nvmrc` / `volta`。
- `.editorconfig` 统一缩进、换行。
- ESLint + Prettier + Stylelint，保存自动修复。
- Husky + lint-staged：提交前只检查暂存文件。
- commitlint + commitizen：统一提交信息。
- 分支规范：`main` / `develop` / `feature/*` / `hotfix/*`。
- 环境变量：`.env.development`、`.env.staging`、`.env.production`，只暴露 `VITE_` 前缀。
- 统一命名：组件 PascalCase，composable `useXxx`，store `useXxxStore`。

---

## 四、类型安全：TS 宽松实用（注解可选）

- `tsconfig` 宽松模式：`strict: false`、`noImplicitAny: false`、`noUnusedLocals/Parameters: false`，不强制写类型注解。
- 类型注解一律可选、推断优先：`: void`、`: Promise<void>`、`: AgentMessage`、`<PromoItem>`、`<Record<string, unknown>>` 等返回值/泛型可省略；`any` 可用。
- `vue-tsc --noEmit` 仍纳入 CI，但缺注解不再报错，只拦截真正的类型冲突。
- 自动导入生成 `auto-imports.d.ts`、`components.d.ts`，加入 `tsconfig.include`。
- API 类型优先由 OpenAPI/Swagger 生成，避免手写 DTO。
- **Agent 事件协议必须类型化**：

```ts
export type AgentEvent =
  | { type: 'text'; content: string }
  | { type: 'tool_call'; name: string; args: Record<string, unknown> }
  | { type: 'tool_result'; name: string; result: unknown }
  | { type: 'approval'; action: string; args: Record<string, unknown> }
  | { type: 'error'; code: string; message: string }
  | { type: 'done'; traceId: string }
```

- 组件必须定义 `defineProps`、`defineEmits` 类型。
- Element Plus 类型：`FormInstance`、`FormRules`、`TableInstance` 直接复用。
- 避免 `any`，用 `unknown` + 类型守卫。

---

## 五、Element Plus 工程化优化

- 按需引入：

```ts
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    AutoImport({ resolvers: [ElementPlusResolver()] }),
    Components({ resolvers: [ElementPlusResolver()] }),
  ],
})
```

- 样式按需，避免全量引入。
- 主题定制：SCSS 变量覆盖 Element Plus 主题。
- 暗黑模式：`useDark` + `ElConfigProvider`。
- 国际化：`ElConfigProvider locale`。
- 图标按需：`@element-plus/icons-vue`。
- **封装 Agent 业务组件**：`ProTable`、`ProForm`、`SearchBar`、`DialogWrapper`、`ApprovalCard`、`ToolCallCard`、`CitationList`、`StreamMessage`。
- 不要直接在页面写大量表格/表单逻辑，抽成 composables。

---

## 六、Agent 交互协议：SSE / WebSocket 统一事件

Agent 前端不是简单请求响应，而是事件流。必须定义统一事件协议：

```json
{"type": "text", "content": "..."}
{"type": "tool_call", "name": "search", "args": {...}}
{"type": "tool_result", "name": "search", "result": {...}}
{"type": "approval", "action": "send_email", "args": {...}}
{"type": "error", "code": "TOOL_FAILED", "message": "..."}
{"type": "done", "trace_id": "..."}
```

**SSE composable 示例：**

```ts
export function useAgentStream() {
  const events = ref<AgentEvent[]>([])
  const status = ref<'idle' | 'streaming' | 'done' | 'error'>('idle')

  async function start(payload: ChatRequest) {
    status.value = 'streaming'
    const es = new EventSource(`/api/v1/agent/chat/stream?query=${payload.query}`)
    es.onmessage = (e) => {
      const event = JSON.parse(e.data) as AgentEvent
      events.value.push(event)
      if (event.type === 'done') {
        status.value = 'done'
        es.close()
      }
    }
    es.onerror = () => {
      status.value = 'error'
      es.close()
    }
  }

  return { events, status, start }
}
```

**要点：**
- 断线重连、事件 ID、幂等。
- 前端按事件类型渲染。
- 审批事件必须等待用户确认。
- 长任务可转 WebSocket 或轮询。
- 流式渲染与乐观更新。

---

## 七、状态管理：Pinia 按域拆分

- 局部状态优先用 `ref` / `reactive`。
- 跨页面共享、登录信息、权限、全局配置才进 Pinia。
- Store 按业务域拆分：

```ts
// stores/session.ts
export const useSessionStore = defineStore('session', () => {
  const sessions = ref<Session[]>([])
  const currentId = ref<string | null>(null)
  const messages = ref<Message[]>([])

  async function load(id: string) { /* ... */ }
  async function send(query: string) { /* ... */ }

  return { sessions, currentId, messages, load, send }
})
```

- `agentState.ts`：Agent 状态机。
- `approvals.ts`：审批列表与操作。
- `tools.ts`：工具调用记录。
- 持久化只存必要字段。
- 复杂流程用状态机思想，避免布尔值爆炸。
- Store 中不直接写 UI 逻辑。

---

## 八、路由与权限

- 路由懒加载：`() => import('@/views/xxx.vue')`。
- 动态路由：登录后根据权限生成菜单。
- 路由守卫：登录、权限、标题、进度条。
- 按钮级权限：`v-permission` 指令。
- 页面级权限：路由 `meta.roles`。
- 404 / 403 / 500 页面。
- 菜单、面包屑、标签页统一布局。
- Agent 场景：审批中心、任务中心、Agent Studio 按角色控制。

---

## 九、Agent 专属组件体系

| 组件 | 作用 |
|---|---|
| `ChatPanel` | 对话主面板 |
| `StreamMessage` | 流式消息渲染 |
| `ToolCallCard` | 工具调用展示 |
| `ToolResultCard` | 工具结果展示 |
| `ApprovalCard` | 审批确认与修改 |
| `CitationList` | RAG 引用来源 |
| `AgentStatus` | 规划/执行/等待审批状态 |
| `TaskCenter` | 长任务列表与状态 |
| `AgentStudio` | 提示词、工具、知识库、工作流画布 |
| `AdminPanel` | 租户、权限、配额、审计 |

设计原则：
- 透明：展示思考/执行/引用。
- 可控：可中断、可回滚、可确认。
- 可追溯：日志、引用、审计。
- 渐进披露：普通用户看结果，专家看细节。
- 多模态：文本、文件、图片、语音、表格。

---

## 十、请求与数据层

- Axios 统一封装：
  - 基础 URL、超时、Token 注入。
  - 响应拦截：统一错误码、消息提示、401 刷新 Token。
  - 请求取消、重试、并发控制。
- 请求 hooks：`useRequest` 统一 loading、error、data。
- DTO / VO 转换，避免后端字段污染页面。
- 错误分类：网络错误、业务错误、权限错误、服务错误。
- 文件上传、下载、SSE 单独封装。
- Agent 流式接口与普通接口分离。

---

## 十一、构建与性能优化

- Vite 分包：`manualChunks` 拆 vendor、Element Plus、业务包。
- 开启 gzip / brotli 压缩。
- 图片压缩、WebP、懒加载。
- 路由懒加载、组件异步加载。
- Tree Shaking，避免全量引入。
- 长会话虚拟列表。
- 频繁切换页面用 `keep-alive`。
- 防抖节流、Web Worker 处理重计算。
- 使用 `rollup-plugin-visualizer` 分析包体积。
- CDN 加速第三方库（按需）。
- 流式渲染优化：增量更新、批量合并、避免全量重绘。

---

## 十二、测试与质量门禁

- 单元测试：Vitest + Vue Test Utils，覆盖 utils、composables、stores。
- 组件测试：关键业务组件，如 `ApprovalCard`、`ToolCallCard`。
- E2E：Playwright / Cypress，覆盖登录、对话、工具调用、审批流程。
- MSW mock API 和 SSE。
- 覆盖率阈值，纳入 CI。
- CI 必须跑：`lint` → `typecheck` → `test` → `build`。
- 不通过不合并。

---

## 十三、CI/CD 与部署

- GitHub Actions / GitLab CI：
  - 安装依赖缓存。
  - lint、typecheck、test、build。
  - 产物上传、Docker 镜像构建。
- Docker + Nginx 部署。
- 多环境：dev / staging / prod。
- 版本号、回滚、灰度。
- Nginx 配置：history 模式、缓存、gzip、代理。
- 静态资源 CDN。

---

## 十四、监控与安全

- Sentry：错误监控、sourcemap。
- Web Vitals：LCP、FID、CLS。
- 埋点：页面、按钮、核心流程。
- 安全：XSS 过滤、CSRF Token、CSP、依赖漏洞扫描。
- 错误边界：全局 `errorHandler`、`window.onerror`。
- 敏感信息不打印、不存 localStorage。
- Agent 场景：敏感操作确认、操作留痕、审批记录。

---

## 十五、文档与组件库

- Storybook：组件开发、调试、文档。
- VitePress：项目文档、规范、接入指南。
- 组件 API、Props、Events、Slots 自动生成。
- 新成员 onboarding 文档。
- Agent 事件协议文档。

---

## 十六、Monorepo / 微前端（可选）

- 多项目共用组件、工具、类型：`pnpm workspace + Turborepo`。
- 微前端：qiankun / wujie，但不要为了微而微。
- 适合：多团队、多子系统、独立部署。
- 不适合：小项目，会增加复杂度。
- Agent 场景：可把 `ChatPanel`、`AgentStudio`、`AdminPanel` 拆成子应用。

---

## 十七、优先级路线图

**P0：立即做**
- pnpm + ESLint + Prettier + Husky + commitlint。
- TS strict + vue-tsc。
- 目录分层。
- Axios 封装。
- Element Plus 按需引入。
- Agent 事件协议类型化。
- CI：lint + typecheck + build。

**P1：2-4 周**
- Pinia 模块化。
- 动态路由与权限。
- 请求 hooks、业务组件封装。
- SSE / WebSocket 统一封装。
- 对话、工具调用、审批、引用组件。
- Vitest 单测。
- 性能优化：分包、懒加载、虚拟列表。
- Sentry + Web Vitals。

**P2：持续**
- E2E、Storybook。
- Monorepo / 微前端。
- 灰度、回滚、安全加固。
- 组件库与文档体系。
- Agent 评估与反馈闭环。

---

## 检查清单

- 是否用 pnpm 并锁定依赖？
- 是否 TS strict + vue-tsc？
- 是否有清晰目录分层？
- 是否 ESLint + Prettier + Husky + commitlint？
- Element Plus 是否按需引入与主题定制？
- 是否定义并类型化 Agent 事件协议？
- 是否封装 SSE / WebSocket 与断线重连？
- 是否 Pinia 按域拆分、避免滥用？
- 是否封装对话、工具调用、审批、引用组件？
- 路由与按钮权限是否完整？
- 是否有 CI 质量门禁？
- 是否有单测/E2E？
- 是否做分包、懒加载、虚拟列表？
- 是否有 Sentry、Web Vitals？
- 是否 Docker + Nginx + 多环境部署？
- 是否有文档与组件库？
- 是否支持多租户、审计、敏感操作确认？

---

## 一句话总结

**Agent + Vue3 + TS + Element Plus 前端工程化 = 标准 Vue3 工程化 + Agent 事件协议 + 流式交互 + 状态机 + 工具调用/审批/引用组件 + 权限审计 + 可观测交付闭环，最终实现可维护、可扩展、可协作、可交付、可观测。**