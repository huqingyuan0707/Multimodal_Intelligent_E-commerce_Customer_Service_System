# 测试、评估与验收方案（含 CI 门禁与 smoke 规范）

## 1. 金字塔

`pytest(后) / vitest(前) → 集成 → smoke_*.py(联调) → Playwright E2E → k6 压测 → 黄金集评估 → 发布门禁`。CI 任一红灯不合并，可一键回滚 <5min。

## 2. 后端

- `pytest + pytest-asyncio + httpx`，覆盖 Service/Runtime/工具/RAG；Mock LLM 与外部工具；Agent 回放固定输入验输出；PG 用容器库；覆盖率阈值进 CI。
```python
@pytest.mark.asyncio
async def test_chat_ok(client, mock_llm):
    r = await client.post("/api/v1/chat", json={"query": "退换政策", "thread_id": "t1"})
    assert r.json()["code"] == 0
```
- 改完必跑 `python -m py_compile <改动文件>` + `openapi.json` 自查 + 对应 `smoke_*.py`。

## 3. 前端

- `Vitest + Vue Test Utils` 覆盖 store/composable（改 store/composable 必加 `src/**/*.test.ts`）；关键组件 `ApprovalCard/ToolCallCard/CitationList` 快照 + 交互。
- `Playwright + MSW` 覆盖登录→对话→工具→审批→拒答（2001）→ 401 跳登录；mock SSE 分帧。
- 门禁：`pnpm lint` 0 errors（`no-explicit-any` warn，既有不扩散）+ `pnpm typecheck` + `pnpm build`；页面方法一律箭头函数（`func-style` 硬约束，注意先定义后调用）；样式用 `var(--reai-*)` tokens + `scoped`，禁硬编码主色；优先 `AiButton/AiInput`；破坏操作先 `ElMessageBox.confirm` 再 `ElMessage`。

## 4. Smoke 脚本风格（`backend/tests/smoke_*.py` 强制）

- 头 docstring 写覆盖点 + 用法 `python tests/smoke_x.py [http://127.0.0.1:8010]`。
- `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`；`httpx.Client(timeout=..., trust_env=False)`；先 `POST /auth/login` 取 token 组头。
- 输出 `PASS/FAIL` 明细 + `RESULT: N passed, M failed`，退出码反映成败。

## 5. 评估（黄金集 ≥500）

售前 200 / 售中 100 / 售后 200（含图文 100）+ 拒答 50 + 对抗注入 50。指标：自动解决率 ≥80%、grounded ≥95%、幻觉 ≤2%、瑕疵 F1 ≥0.85/召回 ≥0.9、`faithfulness` 均值 ≥0.85。`LLM-as-judge + 人工抽检` 双轨，影子/A-B，不达标禁发布。差评/踩自动 mining 待补知识。

## 6. 压测与验收 Checklist

- k6 1000 混合（文70/图20/音10），首字 P95 <2s，完整 <10s；Ingress `proxy_buffering off`，粘性会话。
- [ ] 三模态 E2E 通 + 断线重连；[ ] 退款 100% 进审批 + 越权 0 泄漏 + 极限词/PII 过滤；[ ] 留痕 ≥6 月可查 + 成本归因误差 <5%；[ ] 灰度/蓝绿/金丝雀 + DB 向前兼容 + 演练 RTO<15min/RPO<5min。
- [ ] B端闭环：并发下单超卖率 0 + 账实一致 ≥99.5% + 采购/调拨/报损 100% 审批 + 大屏延迟 <5min + 对账差异 <0.1% + 售后单↔会话 trace 双向可跳。
- [ ] 横向：发券预算超发 0 + 差评 2h 响应率 ≥95% + 风控 100% 人工复核 + 消息频控生效 + 工单 SLA 达标 + 大促一键切换 <5min。

## 7. CI 示例

后端：`ruff check → mypy app → pytest → docker build → Trivy 扫描 → push(git-sha tag)`；前端：`pnpm lint → typecheck → vitest → build → 镜像(Nginx) → CDN`；CD：ArgoCD GitOps，dev→staging→prod，生产需人工批。
