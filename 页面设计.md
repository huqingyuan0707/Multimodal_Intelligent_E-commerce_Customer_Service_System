# 多模态智能电商客服系统 页面设计（UX + IA + 前端落地）

> 版本：v1.4 | 日期：2026-09-12 | 状态：深色视觉基线·玻璃拟态 + B端商家后台 + 横向域（对齐 FRDv2 v2.2）
> 对齐：`前端工程化.md` + `skills/frontend-code-style/SKILL.md` + `API接口与SSE事件协议规范.md` + `FRDv2`
> 适用：Vue3 + TS strict + Element Plus + Pinia + Vite + pnpm，`<script setup lang="ts">`，页面方法一律箭头函数。

一句话：**买家看结果，坐席看过程，运营看效果**——同一 Agent 内核，三种渐进披露视图，全部走统一 SSE 事件 + 引用溯源 + 审批留痕。

---

## 1. 信息架构 IA 与路由表

```
登录 /login（公开）
├── 买家端 /chat（买家/游客）           # 对话助手主屏，多模态输入
├── 坐席工作台 /workbench（客服）       # 三栏：队列｜对话+Trace｜客户+订单
├── 任务中心 /tasks（客服/运营）        # 长任务列表+进度
├── 审批中心 /approvals（客服/管理员）  # 敏感动作批/驳/改参
├── 知识库 /knowledge（运营）           # 文档列表+上传+检索测试+版本
├── Agent Studio /studio（运营/管理员） # Prompt/工具/技能/评测
├── 数据看板 /dashboard（运营/管理员）  # QPS/延迟/解决率/幻觉/成本/工具成功率
├── 管理后台 /admin（管理员）           # 租户/用户/角色/配额/密钥/审计/SLO
├── 商品 /goods（店长/运营）            # SPU/SKU 矩阵/上下架/改价审批
├── 库存 /inventory（仓管/店长）        # 多仓可用量/预警/出入库/盘点
├── 采购 /purchase（店长/仓管）         # 供应商/采购单审批/到货质检
├── 订单履约 /orders（客服/仓管）       # 平台订单镜像/打单发货/售后单
├── 对账 /finance（财务/店长）          # 日结单/差异告警/导出
└── 经营大屏 /screen（店长/管理员）     # GMV/转化/客服/售后/库存投屏
├── 营销会员 /marketing（店长/客服）      # 发券/会员/核销（客服按模板发券）
├── 物流评价 /logistics（仓管/客服）      # 面单/异常件/差评响应工单
├── 风控工单 /risk（店长/客服）           # 拦截复核/协同工单/SLA
异常：/403 /404 /500
嵌入：/widget（独立站 iframe/JS-SDK，复用 /chat 精简版）
```

路由规范（本项目强制）：

- 懒加载 `() => import('@/views/<domain>/XxxView.vue')`，页面按域分目录到 `src/views/<domain>/`（admin/agent/auth/dashboard/knowledge/studio/workbench/widget/goods/inventory/orders）；逻辑按层放顶层 `src/{components,composables,stores,types}/`，通用件下沉 `src/shared/`（无 `features/`）。
- `meta.roles` 做页面级权限 + `v-permission` 做按钮级，401（含业务码 `1002`）走中央 `handle401()`，禁止页面自写跳转。
- 守卫行为（`router/index.ts`）：无 `reai_token` → `/login?redirect=`；有 token 但内存无身份 → 先 `loadMe()`，失败回 `/login`；`meta.roles` 不匹配 → 提示并回 `/chat`；已登录访问 `/login` → 直接进 `/chat`。`handle401()` 幂等（已在 `/login` 只清 token 不跳转）。
- 菜单/面包屑/标签页统一布局，`keep-alive` 只用于高频切换的 workbench/chat。

| 路由 | 角色 | 核心组件 |
|---|---|---|
| `/login` | 公开 | `LoginForm` |
| `/chat` | 买家 | `ChatPanel/StreamMessage/MultimodalInput/CitationList` |
| `/workbench` | 客服 | `SessionQueue/ChatPanel/TracePanel/CustomerPanel/QuickReply` |
| `/tasks` | 客服/运营 | `TaskCenter` |
| `/approvals` | 客服/管理员 | `ApprovalCard` 列表版 |
| `/knowledge` | 运营 | `ProTable/UploadCard/RetrievalTester/VersionDrawer` |
| `/studio` | 运营/管理员 | `PromptEditor/ToolList/EvalRunner` |
| `/dashboard` | 运营/管理员 | `MetricCards/Charts/CostTable` |
| `/admin` | 管理员 | `TenantTable/QuotaForm/AuditTable` |
| `/goods` | 店长/运营 | `GoodsTable/SkuMatrix/PriceForm` |
| `/inventory` | 仓管/店长 | `StockTable/StockWarnCard/MoveForm/StocktakeDrawer` |
| `/purchase` | 店长/仓管 | `SupplierTable/PurchaseForm/QcCard` |
| `/orders` | 客服/仓管 | `OrderTable/ShipForm/AftersaleDrawer` |
| `/finance` | 财务/店长 | `BillTable/SettleCard` |
| `/screen` | 店长/管理员 | `BizScreen`（GMV/转化/客服/售后/库存，投屏脱敏） |
| `/marketing` | 店长/客服 | `PromoTable/CouponGrantForm/MemberCard` |
| `/logistics` | 仓管/客服 | `ShipTable/ExceptionCard/ReviewReply` |
| `/risk` | 店长/客服 | `RiskTable/TicketCenter` |

---

## 2. 全局布局与设计语言（深色科技风）

**布局壳 `AppLayout.vue`：** 蓝紫渐变顶栏（品牌「智能客服工作台」+ 顶部 Tab 导航 + 环境 badge/坐席在线/消息通知/租户 + 用户下拉/退出）+ 主区（`router-view`）+ 全局 `errorHandler/window.onerror` + Sentry。导航由路由 `meta.title` 自动生成，按 `meta.roles` 过滤；`/login` 与异常页独立无壳。
用户下拉：身份头（租户 · 角色，只读）+ 「切换用户」+ 「退出登录」。「切换用户」按身份分流：admin 弹代入窗（`UserSwitchDialog`：本租户用户表 + 用户名搜索 + 分页默认 20，可切 10/20/50/100，当前行禁用；confirm 后免密代入并整页重载，新身份无权看当前页时守卫踢回 `/chat`），成功记 `auth.switch` 审计；非 admin 先 confirm 再退出并跳 `/login?redirect=<原页>` 重登。

**视觉（对齐深色参考稿）：** 深空蓝底 + 蓝紫弥散渐变（顶栏/登录底）+ 高饱和强调色（蓝/青/紫）；
玻璃拟态卡片（半透明底 + `backdrop-filter: blur(12px)` + 微发光边框）；用户泡品牌蓝（右）、AI 泡白色（左）；
VLM 检测卡蓝框、引用卡金框；登录为深色分屏（左品牌宣导 + 3D 等距 SVG 插画 + 右玻璃登录卡，大圆角无边框输入 + 青蓝渐变按钮）。

**设计 Tokens（禁止硬编码色值，色值只许出现在 `tokens.css`，`.vue` 里一律 `var()`，SVG 属性也用 `var()`）：**

```css
var(--reai-bg / --reai-card / --reai-card-2 / --reai-text-main / --reai-text-muted / --reai-border / --reai-shadow-*)
var(--reai-gradient / --reai-accent / --reai-gold / --reai-online / --reai-bubble-user / --reai-bubble-agent)
var(--reai-glass-bg / --reai-glass-border / --reai-glow / --reai-text-on-light / --reai-page-gradient / --reai-purple)
```

Element Plus 暗黑：`theme-chalk/dark/css-vars.css` + `html.dark`；`<style scoped>`，国际化 `ElConfigProvider locale`，图标 `@element-plus/icons-vue` 按需。

**通用规范：**

- 优先 `AiButton/AiInput`（`@/components`），不裸用 `el-button/el-input`；Element Plus 靠 auto-import 按需引入，禁止全局全量。
- 反馈：成功/失败一律 `ElMessage`；删除/停用/归档先 `ElMessageBox.confirm`。
- 枚举中文化走映射表，如 `LEVEL_TAG = { public: '公开', internal: '内部', confidential: '机密' }`，禁止模板散落字面量；界面文案与注释用中文。
- 类型先行 `src/types/agent.ts`（`Message/Reference/AgentEvent`），禁止各文件自造消息形状；`defineProps/defineEmits` 必写类型，无 `any`（既有 `any` 不扩散）。
- 数据与降级：列表页 `onMounted` 调真实接口，`catch` 回退 `@/mock`；新会话本地先建 `t-${Date.now()}`，成功后以后端为准；切会话优先 `api.getSession(id)`，404 回退 mock。

---

## 3. 核心页面详设

### 3.1 买家对话页 `/chat`（P0 最高优）

**目标：** 3 步内闭环售前/售中/售后，不过度暴露内部过程。

**布局（桌面双栏：左历史 + 右对话；<1024px 左栏收起为抽屉，移动单列）：**

```text
[侧边栏 280px：历史对话｜新会话｜改名/删除｜分页] [主区]
  点击切会话（恢复含引用/检测卡/上下文用量）      [顶栏：店铺名｜订单号关联｜语音开关｜转人工]
                                                  [主区：StreamMessage 流（虚拟列表）]
                                                    买家泡（右）｜Agent 泡（左，含引用角标/检测卡片/语音条）
                                                  [底栏 MultimodalInput：文本框 + 图片 + 语音按住说 + 发送]
```

**组件与交互：**

- `ChatPanel.vue`：会话容器，管虚拟列表 + 增量渲染（批量合并，避免全量重绘）+ 断线横条“重连中…”。
- `/chat` 会话归位：新会话本地先建 `t-${Date.now()}` 占位（`thread_id` 不传后端），首轮流式 `done.session_id` 回来后认领替换占位并回填标题；发送带 `clientMsgId` 幂等键，重连复用同一键，后端重放相同事件 id，前端同流去重 + 重试前清草稿，保证不重复消息；切会话优先 `api.getSession(id)` 恢复（含引用/trace/检测卡/上下文用量），404 回退 mock。- `StreamMessage.vue`：按 SSE 分帧渲染。`phase` 显示“检索中/检测中/生成中”弱提示；`message` 增量打字；`done` 合并 `references+guard+faithfulness+trace_id+context`，渲染 `CitationList` 角标 `[1][2]` 可点跳原文，气泡小字透出“上下文 N 轮/约 M token（已摘要）”。
- `SessionDrawer.vue`：历史会话抽屉（服务端分页默认 20 + 消息数 + 改名 + 删除 confirm）；恢复走详情翻页（`page=1&size=50` 倒序，反转渲染，`has_more` 顶部“加载更早消息”）；删除当前会话即清空消息区。
- 图文：`ImageUploader`（≤9 张/单张 ≤10M，超限前端压缩，FormData 上传绝不手设 Content-Type）→ 预览缩略图 → 发送后 `ToolCallCard` 显示“瑕疵检测中…” → 结果卡片 `类别/置信度/描述`（置信 `<0.6` 显示“已转人工复核”，不硬答）+ 引用政策。
- 语音：按住录 ≤60s，波形 + 倒计时 + 上滑取消；发送后 ASR 文本回显（可编辑重发）；Agent 回复含语音条 + 文字对照 + 语速/音色切换（默认晓晓）。
- 快捷入口：尺码助手（身高/体重/版型三步表单）、查物流（订单选择器）、退换申请（证据图+原因，提交即进审批，显示“待客服确认”）。
- 状态：空（新会话引导卡）、加载（骨架泡）、错误（`error` 事件转友好话术 + 重试 + 转人工按钮）、限流（2002 显示排队位）。
- 权限：游客仅本会话；`user_id` 隔离；PII 脱敏 `138****1234`。
- 埋点：发送/上传/播放/引用点击/转人工/反馈赞踩。

**关键 composable（箭头函数）：**

```ts
const sendChat = async () => { /* useChat 管发送/阶段/技能/反馈 */ };
const govPayload = () => ({ security_level: govForm.security_level });
```

### 3.2 坐席工作台 `/workbench`（人机协同核心）

**目标：** 10 秒内看懂 Agent 干了什么，一键接管。

**三栏布局（对齐深色参考稿）：**

```text
[左 280px 会话队列] [中 当前会话] [右 320px 客户+工具]
  王女士·VIP 置顶     用户泡（左，头像）    客户信息卡：等级/订单
  待处理/瑕疵咨询     Agent 泡（品牌蓝）    Trace 链路（规划→检索→工具）
  退款跟进           VLM 瑕疵检测卡（蓝框） 工具调用卡：trace_id/状态/发送
                     引用来源卡（金框）
                     输入区：回复框+上传图片+语音
```

- `SessionQueue`：VIP 置顶 + 状态标签（待处理/瑕疵咨询/退款跟进），点击切换当前会话；待接红点/排队位、技能组筛选、抢接/转接后续补。
- `VLM 瑕疵检测卡`（蓝框）：类别/置信度 + `AI 回复`标签 + 建议（如“拍照留存后申请换货”）；置信度 `<0.6` 显示“已转人工复核”，不硬答。
- `引用来源卡`（金框）：政策条目（如“售后政策第 3.2 条/换货处理流程”）可点跳原文，`trace_id` 一键复制。
- 右栏：客户信息卡（等级/订单）+ Trace 链路折线节点图（规划→检索→决策三节点，AI 过程透明可控）+ 工具调用卡（`trace_id`/状态/发送）。
- 气泡：用户品牌蓝（右）/AI 白（左），检测结果一律封装成状态标签卡片。
- 输入区：回复框 + 上传图片（青色按钮，FormData，绝不手设 Content-Type）+ 语音；发送先本地回显，真实流式复用 `/chat` 链路。

- `SessionQueue`：待接/进行中/排队位播报，VIP 置顶，技能组（售前/售后）筛选，抢接/转接。
- `TracePanel`（专家视图）：规划步骤 + RAG 引用（含分数/过滤原因）+ 工具参果 + Token/耗时 + `trace_id` 一键复制；普通买家不可见，渐进披露。
- `ApprovalCard`：退款/补偿详情 + 改参（金额/原因）+ 批准/驳回 + 超时升级倒计时；操作留痕。
- `QuickReply + 内部备注`：备注买家不可见；快捷回复支持变量 `{昵称}{订单号}`。
- 异常：冲突接管提示“已被 XX 接管，转为只读围观”。

### 3.3 任务中心 `/tasks`

长任务（批量导入/reindex/大图检测）列表：状态 `pending/running/done/error` + `progress` 进度条 + SSE `progress/complete/error` 实时推 + 结果下载 + 失败重试。短任务不进此页。

### 3.4 审批中心 `/approvals`

表格 + 详情抽屉：`action/金额/证据图/政策引用/申请人/超时`，批量批，改参重试，驳回必填理由。`approvals.ts` store 管列表与操作，`useApproval.ts` 封确认框 + 幂等（`Idempotency-Key`）。

### 3.5 知识库 `/knowledge`

`ProTable`（标题/密级 `LEVEL_TAG`/渠道/生效期/版本/命中）+ `SearchBar`（服务端标题筛选）+ 预览抽屉（含正文）+ `DialogWrapper` 编辑（标题/正文/密级/渠道/生效期，保存版本+1）+ `UploadCard`（FormData，重复 SHA256 提示“已跳过重复入库”）+ 服务端分页（默认 20，可切 10/20/50/100）+ `RetrievalTester`（输入 query 预览召回分数/过滤原因，P2）+ 版本抽屉（回滚，P2）。删除先 confirm；种子 29 篇随后端启动幂等入库（FR-13）。

### 3.6 Agent Studio `/studio`

三 Tab：Prompt（版本列表+灰度比例+回滚+变量）/ 工具（JSON Schema/ Scope/幂等/超时/熔断只读 + 试调）/ 评测（黄金集选择 + 一键跑 + faithfulness/guard 分布 + 不达标禁发布红条）。

### 3.7 数据看板 `/dashboard`

`MetricCards`（QPS/首字 P95/解决率/幻觉率/工具成功率/Token 成本）+ 趋势图 + 按租户/渠道归因表 + 超预算红线 + 慢 Trace 跳转。复杂指标旁写一行中文口径注释。

### 3.8 管理后台 `/admin`

租户（计划/试用/欠费停服）、用户角色、配额（Token/并发）、密钥（只显掩码）、审计（谁/何时/调什么/结果）、SLO 告警。危险操作双重 confirm。

### 3.9 登录/异常/嵌入

- `/login`：深色分屏（左品牌宣导「智能客服系统/多模态会话平台」+ 右登录卡：账号/密码/租户 ID + 忘记密码 + 登录），账密走 `request`，401 进 `handle401`；租户 ID 只本地记住（`sessionStorage`，下次回填），不进登录契约。登录成功优先回跳 `?redirect=`（仅站内 `/` 开头路径，防开放重定向），无则进 `/chat`（切换用户/守卫拦截的回来口）。
  左右两列**等高**：左品牌块与右登录卡顶边/底边齐平（Grid `align-items: stretch` 让卡片随左块拉高、`align-content: center` 让整组垂直居中），卡片内表单保持垂直居中不吃底部留白；`≤960px` 收起等距插画并改为单列堆叠。
- `/403/404/500`：插画 + 返回 + 报 `trace_id`。
- `/widget`：`/chat` 精简版（无 Trace/管理入口），JS-SDK `data-tenant` 挂载，高度自适应，H5 键盘顶起处理。

### 3.10 商品 `/goods`（对齐 FRDv2 FR-10.1）

`GoodsTable`（SPU/状态/销量）+ `SkuMatrix`（颜色×尺码价格库存矩阵，服装必需）+ 改价 `PriceForm`（恒进审批，资损红线）+ 上下架 confirm；商品变更提示“已同步客服知识”（面料/尺码/价格段），客服侧只读。

### 3.11 库存 `/inventory`（对齐 FR-10.2）

`StockTable`（仓×SKU：在库/预占/锁定/可用四列，公式旁注）+ `StockWarnCard`（低于安全线红条 + 一键生成采购）+ `MoveForm`（入/出/调拨，原因必填）+ `StocktakeDrawer`（导入盘点，差异进审批）；缺货 SKU 自动联动客服话术“补货中”。

### 3.12 采购 `/purchase`（对齐 FR-10.3）

`SupplierTable`（账期/合格率）+ 采购单状态机（草稿→审批→到货→质检→入库）`PurchaseForm` + `QcCard`（不合格→暂停销售公告 + 退供）；到货 ETA 供客服承诺交期。

### 3.13 订单履约 `/orders`（对齐 FR-10.4）

`OrderTable`（平台/状态/金额，本地镜像）+ `ShipForm`（打单发货，物流单号校验）+ `AftersaleDrawer`（售后单关联会话 trace，一键跳 workbench 对应会话）；状态非法操作后端 3005，前端置灰按钮。

### 3.14 对账 `/finance`（对齐 FR-10.5）

`BillTable`（应收/实收/退款/运费/扣点/差异）+ 差异超阈值红字告警 + 导出 + 日结确认（财务双人复核预留）；无 PII 列。

### 3.15 经营大屏 `/screen`（对齐 FR-11）

`BizScreen`：GMV/订单/转化/客单 + 客服解决率/转人工率 + 退货率 Top 原因 + 周转/缺货/滞销，今日 vs 昨日/上周，30 秒轮询；投屏模式隐藏金额小数外所有敏感列；异常卡片一键下钻（跳会话/质检/知识补录）。

### 3.16 营销会员 `/marketing`（对齐 FR-10.6）

`PromoTable`（预算/已发/剩余进度条，超预算置灰）+ `CouponGrantForm`（选模板+金额，客服安抚场景，阈值以上进审批）+ `MemberCard`（等级/积分/复购）；核销明细可查，发券幂等失败给中文可操作提示。

### 3.17 物流评价 `/logistics`（对齐 FR-10.7/10.8）

`ShipTable`（面单/揽收/在途）+ `ExceptionCard`（滞留/破损/拒收→一键建售后单）+ 差评流（`ReviewReply`：2h SLA 倒计时红条 + 回复 + 建工单）；评价打标反哺选品。

### 3.18 风控工单 `/risk`（对齐 FR-12.1/12.3）

`RiskTable`（关联图谱摘要 + 通过/拦截，禁全自动封号，拦截必进人工复核）+ `TicketCenter`（跨部门流转/SLA 倒计时/超时升级/关闭回填结论）；工单状态反写源单。

### 3.19 消息与组织（对齐 FR-12.2/12.4，挂 `/admin` 子 Tab，不单建路由）

消息模板 + 频控（营销 1 条/人/天）+ 到达率；排班表 + 绩效到人（脱敏）+ 离职冻结。P2 再拆独立路由。

---

## 4. 多模态与流式渲染映射（前后端契约）

- 请求：`fetch POST ${BASE}/chat` 必带 `Authorization`，`GET` 参数 `encodeURIComponent`，JSON 走 `request<T>` 自动解包，`code!==0` 抛错（2001 拒答当正常分支渲染，不抛）。
- 解析：`event: / data:` 正则分帧 → `source/phase/message/done` 分支；`done` 的 `JSON.parse` 必 try/catch；`done` 含 `references/guard/faithfulness/trace_id`。
- 渲染：`phase`→弱状态条；`message`→增量；`tool_call/result`→`ToolCallCard` 折叠；`approval`→`ApprovalCard` 阻塞等确认；`error`→友好话术+重试；`done`→引用角标+`trace_id`（坐席可复制）。
- 文件：`FormData` 上传；图片压缩/WebP/懒加载；语音波形 canvas，播放互斥（播 A 停 B）。

---

## 5. 交互与视觉细节

- 流式：首字 <2s 骨架→打字，批量 100ms 合并，长会话虚拟列表 + 回到底部浮钮 + 新消息红点。
- 图片：宫格/灯箱/缩放/标注（可选框选瑕疵区回传 VLM）；NSFW/人脸打码图显“已保护处理”。
- 语音：录音波形+转写可改，TTS 播完自动标已读。
- 空/错/断网：全页空插画 + 一键 action；SSE 断线顶部黄条 + 自动重连 3 次 + 手动重连。
- 无障碍：对比度 AA，键盘可达（Tab/Enter），焦点 trap 在抽屉/弹窗，`aria-label` 于语音/图片按钮。
- 响应式：≥1280 三栏，768-1280 右栏折叠为抽屉，<768 单列 + 底部 Tab（对话/任务/我的）。

---

## 6. 权限、埋点、性能

- 权限矩阵见路由表；页面 `meta.roles` + 按钮 `v-permission` + 接口 `require_perm` 三重。
- 埋点：页面/按钮/核心漏斗（发送→解决/转人工/审批/引用点击/赞踩），Web Vitals LCP/FID/CLS + Sentry sourcemap。
- 性能：Vite `manualChunks`（vendor/Element Plus/业务），gzip/brotli，路由懒加载，`keep-alive` 仅 chat/workbench，`rollup-visualizer` 卡包体积，CDN 第三方库按需。

---

## 7. 组件清单（Agent 专属，复用优先）

| 组件 | 归属 | 用途 |
|---|---|---|
| `ChatPanel/StreamMessage` | src/components | 对话容器/增量渲染 |
| `SessionList` | src/components | 会话列表本体（侧边栏与抽屉复用：分页/改名/删除/消息数） |
| `SessionDrawer` | src/components | 历史会话抽屉（窄屏回退，内嵌 SessionList） |
| `MultimodalInput/ImageUploader/VoiceRecorder` | src/components | 文/图/音输入 |
| `VisionResultCard` | src/components | VLM 检测卡（蓝框：类别/置信度/描述 + 低置信转人工） |
| `ImagePreviewDialog` | src/components | 图片预览/灯箱缩放/点击标注（bbox 相对坐标回传复检） |
| `VoicePanel` | src/components | 录音波形 + ASR 文字对照（可改填入）+ TTS 开关/音色 + 播放互斥 |
| `ToolCallCard/ToolResultCard` | src/components | 工具透明展示 |
| `ApprovalCard` | src/components | 批/驳/改参 |
| `CitationList` | src/components | RAG 引用溯源 |
| `AgentStatus/TracePanel` | src/components | 状态机/专家 Trace |
| `AppLayout` | src/components | 系统壳（顶栏+左侧菜单+主区） |
| `TaskCenter` | views/agent | 长任务 |
| `GoodsTable/SkuMatrix/PriceForm` | views/goods | 商品矩阵/改价审批 |
| `StockTable/StockWarnCard/MoveForm/StocktakeDrawer` | views/inventory | 四列库存/预警/出入库/盘点 |
| `SupplierTable/PurchaseForm/QcCard` | views/purchase | 供应商/采购单/质检 |
| `OrderTable/ShipForm/AftersaleDrawer` | views/orders | 履约/发货/售后关联会话 |
| `BillTable/SettleCard` | views/finance | 对账/日结 |
| `BizScreen` | views/screen | 经营大屏投屏 |
| `PromoTable/CouponGrantForm/MemberCard` | views/marketing | 预算进度/发券/会员 |
| `ShipTable/ExceptionCard/ReviewReply` | views/logistics | 物流异常/差评SLA |
| `RiskTable/TicketCenter` | views/risk | 拦截复核/协同工单 |
| `ProTable/ProForm/SearchBar/DialogWrapper` | shared | 后台三件套，页面禁堆表格逻辑，抽 `composables` |

---

## 8. 页面级验收（Definition of Done）

- [ ] 三角色走通：买家闭环/坐席接管+审批/运营改知识即生效。
- [ ] 三模态：文图音全 E2E，超限/低置信/拒答/401/断线分支有 UI。
- [ ] 引用必现可跳，`trace_id` 可复制定位服务端。
- [ ] `pnpm lint` 0 errors + `pnpm typecheck` + `pnpm build` + 关键 vitest + Playwright 冒烟过。
- [ ] LCP <2.5s，首字 P95 <2s，长会话 500 条不卡顿。
- [ ] B端：改价/采购/调拨/报损 100% 走审批；超卖压测 0 超卖；大屏 30s 刷新 + 投屏无 PII；售后单可跳回原会话。
- [ ] 横向：发券预算超发 0；差评 2h 响应；风控拦截 100% 人工复核；消息频控生效；工单 SLA 达标。

---

## 9. 落地顺序（P0→P1）

**P0：** `/login/chat` + 输入三件套 + 引用 + 转人工 + `useAgentStream/useChat/session` + API 唯一入口 + mock 降级。
**P1：** `/workbench/approvals/tasks` + Trace + 审批改参 + 排队 + B端一期（`/goods/inventory/orders` + 经营小屏 + `/marketing/logistics` 工单）。
**P2：** `/knowledge/studio/dashboard/admin/widget` + 灰度/评测/归因 + 微前端拆分预留 + B端二期（`/purchase/finance/screen` 投屏 + 补货预测 + `/risk` + 绩效/多平台/大促）。
